[
  {
    "id": "eval_0000",
    "text": "test travisci connection",
    "category": "eval"
  },
  {
    "id": "eval_0001",
    "text": "mvn clean install",
    "category": "eval"
  },
  {
    "id": "eval_0002",
    "text": "check tekton status",
    "category": "eval"
  },
  {
    "id": "eval_0003",
    "text": "configure alertmanager settings",
    "category": "eval"
  },
  {
    "id": "eval_0004",
    "text": "perf stat ./benchmark",
    "category": "eval"
  },
  {
    "id": "eval_0005",
    "text": "run kubernetes --help",
    "category": "eval"
  },
  {
    "id": "eval_0006",
    "text": "netlify deploy --prod",
    "category": "eval"
  },
  {
    "id": "eval_0007",
    "text": "check opentelemetry status",
    "category": "eval"
  },
  {
    "id": "eval_0008",
    "text": "python -m pytest --cov",
    "category": "eval"
  },
  {
    "id": "eval_0009",
    "text": "sqlite3 mydb.db '.tables'",
    "category": "eval"
  },
  {
    "id": "eval_0010",
    "text": "export kibana data",
    "category": "eval"
  },
  {
    "id": "eval_0011",
    "text": "apache2ctl configtest",
    "category": "eval"
  },
  {
    "id": "eval_0012",
    "text": "install helm plugin",
    "category": "eval"
  },
  {
    "id": "eval_0013",
    "text": "serverless invoke local",
    "category": "eval"
  },
  {
    "id": "eval_0014",
    "text": "sonarqube-scanner",
    "category": "eval"
  },
  {
    "id": "eval_0015",
    "text": "llamafile -m model.gguf --server",
    "category": "eval"
  },
  {
    "id": "eval_0016",
    "text": "check newrelic status",
    "category": "eval"
  },
  {
    "id": "eval_0017",
    "text": "install fluentd plugin",
    "category": "eval"
  },
  {
    "id": "eval_0018",
    "text": "install drone plugin",
    "category": "eval"
  },
  {
    "id": "eval_0019",
    "text": "logstash -f logstash.conf",
    "category": "eval"
  },
  {
    "id": "eval_0020",
    "text": "check linkerd status",
    "category": "eval"
  },
  {
    "id": "eval_0021",
    "text": "datadog-agent status",
    "category": "eval"
  },
  {
    "id": "eval_0022",
    "text": "npm run build",
    "category": "eval"
  },
  {
    "id": "eval_0023",
    "text": "age -r recipient.pub plaintext.txt > encrypted.txt",
    "category": "eval"
  },
  {
    "id": "eval_0024",
    "text": "run newrelic --help",
    "category": "eval"
  },
  {
    "id": "eval_0025",
    "text": "update tekton to latest",
    "category": "eval"
  },
  {
    "id": "eval_0026",
    "text": "mypy src/ --strict",
    "category": "eval"
  },
  {
    "id": "eval_0027",
    "text": "calico node status",
    "category": "eval"
  },
  {
    "id": "eval_0028",
    "text": "curl -s https://api.example.com/health | jq .",
    "category": "eval"
  },
  {
    "id": "eval_0029",
    "text": "update docker to latest",
    "category": "eval"
  },
  {
    "id": "eval_0030",
    "text": "mysql -u root -p mydb",
    "category": "eval"
  },
  {
    "id": "eval_0031",
    "text": "zipkin",
    "category": "eval"
  },
  {
    "id": "eval_0032",
    "text": "export splunk data",
    "category": "eval"
  },
  {
    "id": "eval_0033",
    "text": "boundary dev",
    "category": "eval"
  },
  {
    "id": "eval_0034",
    "text": "gh pr create --title 'feat: encoder port'",
    "category": "eval"
  },
  {
    "id": "eval_0035",
    "text": "hadolint Dockerfile",
    "category": "eval"
  },
  {
    "id": "eval_0036",
    "text": "deepspeed --num_gpus=4 train.py",
    "category": "eval"
  },
  {
    "id": "eval_0037",
    "text": "ls -la",
    "category": "eval"
  },
  {
    "id": "eval_0038",
    "text": "configure opentelemetry settings",
    "category": "eval"
  },
  {
    "id": "eval_0039",
    "text": "configure terraform settings",
    "category": "eval"
  },
  {
    "id": "eval_0040",
    "text": "ufw status numbered",
    "category": "eval"
  },
  {
    "id": "eval_0041",
    "text": "serverless deploy",
    "category": "eval"
  },
  {
    "id": "eval_0042",
    "text": "run helm --help",
    "category": "eval"
  },
  {
    "id": "eval_0043",
    "text": "configure docker settings",
    "category": "eval"
  },
  {
    "id": "eval_0044",
    "text": "rustc main.rs",
    "category": "eval"
  },
  {
    "id": "eval_0045",
    "text": "run logstash --help",
    "category": "eval"
  },
  {
    "id": "eval_0046",
    "text": "packer init .",
    "category": "eval"
  },
  {
    "id": "eval_0047",
    "text": "kubectl exec -it pod bash",
    "category": "eval"
  },
  {
    "id": "eval_0048",
    "text": "test packer connection",
    "category": "eval"
  },
  {
    "id": "eval_0049",
    "text": "build kibana from source",
    "category": "eval"
  },
  {
    "id": "eval_0050",
    "text": "check consul status",
    "category": "eval"
  },
  {
    "id": "eval_0051",
    "text": "check kibana status",
    "category": "eval"
  },
  {
    "id": "eval_0052",
    "text": "polaris audit --config polaris.yaml --audit-path .",
    "category": "eval"
  },
  {
    "id": "eval_0053",
    "text": "export drone data",
    "category": "eval"
  },
  {
    "id": "eval_0054",
    "text": "configure kubernetes settings",
    "category": "eval"
  },
  {
    "id": "eval_0055",
    "text": "wc -l *.py",
    "category": "eval"
  },
  {
    "id": "eval_0056",
    "text": "test docker connection",
    "category": "eval"
  },
  {
    "id": "eval_0057",
    "text": "vercel --prod",
    "category": "eval"
  },
  {
    "id": "eval_0058",
    "text": "test jaeger connection",
    "category": "eval"
  },
  {
    "id": "eval_0059",
    "text": "metasploit",
    "category": "eval"
  },
  {
    "id": "eval_0060",
    "text": "jpegoptim *.jpg",
    "category": "eval"
  },
  {
    "id": "eval_0061",
    "text": "test grafana connection",
    "category": "eval"
  },
  {
    "id": "eval_0062",
    "text": "gdb ./app core",
    "category": "eval"
  },
  {
    "id": "eval_0063",
    "text": "node -e 'console.log(process.version)'",
    "category": "eval"
  },
  {
    "id": "eval_0064",
    "text": "npm install",
    "category": "eval"
  },
  {
    "id": "eval_0065",
    "text": "pg_dump mydb > backup.sql",
    "category": "eval"
  },
  {
    "id": "eval_0066",
    "text": "qemu-system-x86_64 -m 4G -hda disk.img",
    "category": "eval"
  },
  {
    "id": "eval_0067",
    "text": "configure fluentd settings",
    "category": "eval"
  },
  {
    "id": "eval_0068",
    "text": "accelerate launch train.py",
    "category": "eval"
  },
  {
    "id": "eval_0069",
    "text": "env | grep MIND",
    "category": "eval"
  },
  {
    "id": "eval_0070",
    "text": "test logstash connection",
    "category": "eval"
  },
  {
    "id": "eval_0071",
    "text": "twine upload dist/*",
    "category": "eval"
  },
  {
    "id": "eval_0072",
    "text": "uniq -c words.txt | sort -rn",
    "category": "eval"
  },
  {
    "id": "eval_0073",
    "text": "install argocd plugin",
    "category": "eval"
  },
  {
    "id": "eval_0074",
    "text": "strings ./binary | grep -i password",
    "category": "eval"
  },
  {
    "id": "eval_0075",
    "text": "snakemake --cores 4",
    "category": "eval"
  },
  {
    "id": "eval_0076",
    "text": "1password-cli read 'op://vault/item/field'",
    "category": "eval"
  },
  {
    "id": "eval_0077",
    "text": "update packer to latest",
    "category": "eval"
  },
  {
    "id": "eval_0078",
    "text": "list terraform resources",
    "category": "eval"
  },
  {
    "id": "eval_0079",
    "text": "configure consul settings",
    "category": "eval"
  },
  {
    "id": "eval_0080",
    "text": "sqlmap -u 'http://localhost/user?id=1'",
    "category": "eval"
  },
  {
    "id": "eval_0081",
    "text": "kibana",
    "category": "eval"
  },
  {
    "id": "eval_0082",
    "text": "check knative status",
    "category": "eval"
  },
  {
    "id": "eval_0083",
    "text": "jupyter notebook",
    "category": "eval"
  },
  {
    "id": "eval_0084",
    "text": "test loki connection",
    "category": "eval"
  },
  {
    "id": "eval_0085",
    "text": "deploy drone to production",
    "category": "eval"
  },
  {
    "id": "eval_0086",
    "text": "list newrelic resources",
    "category": "eval"
  },
  {
    "id": "eval_0087",
    "text": "grype myapp:latest",
    "category": "eval"
  },
  {
    "id": "eval_0088",
    "text": "list ansible resources",
    "category": "eval"
  },
  {
    "id": "eval_0089",
    "text": "hive -e 'SELECT COUNT(*) FROM my_table'",
    "category": "eval"
  },
  {
    "id": "eval_0090",
    "text": "dask-worker scheduler:8786",
    "category": "eval"
  },
  {
    "id": "eval_0091",
    "text": "configure knative settings",
    "category": "eval"
  },
  {
    "id": "eval_0092",
    "text": "make test",
    "category": "eval"
  },
  {
    "id": "eval_0093",
    "text": "list alertmanager resources",
    "category": "eval"
  },
  {
    "id": "eval_0094",
    "text": "list prometheus resources",
    "category": "eval"
  },
  {
    "id": "eval_0095",
    "text": "configure nomad settings",
    "category": "eval"
  },
  {
    "id": "eval_0096",
    "text": "nvtop",
    "category": "eval"
  },
  {
    "id": "eval_0097",
    "text": "git stash pop",
    "category": "eval"
  },
  {
    "id": "eval_0098",
    "text": "pig -x local script.pig",
    "category": "eval"
  },
  {
    "id": "eval_0099",
    "text": "pandoc README.md -o README.pdf",
    "category": "eval"
  },
  {
    "id": "eval_0100",
    "text": "check tempo status",
    "category": "eval"
  },
  {
    "id": "eval_0101",
    "text": "unzip archive.zip",
    "category": "eval"
  },
  {
    "id": "eval_0102",
    "text": "fail2ban-client status",
    "category": "eval"
  },
  {
    "id": "eval_0103",
    "text": "gcloud auth login",
    "category": "eval"
  },
  {
    "id": "eval_0104",
    "text": "less CHANGELOG.md",
    "category": "eval"
  },
  {
    "id": "eval_0105",
    "text": "npm test",
    "category": "eval"
  },
  {
    "id": "eval_0106",
    "text": "airflow tasks test my_dag task_id 2024-01-01",
    "category": "eval"
  },
  {
    "id": "eval_0107",
    "text": "opa eval --data policy.rego --input input.json",
    "category": "eval"
  },
  {
    "id": "eval_0108",
    "text": "cloud-custodian run -s out/ policy.yml",
    "category": "eval"
  },
  {
    "id": "eval_0109",
    "text": "istioctl analyze",
    "category": "eval"
  },
  {
    "id": "eval_0110",
    "text": "terrascan scan -t aws -d .",
    "category": "eval"
  },
  {
    "id": "eval_0111",
    "text": "deploy packer to production",
    "category": "eval"
  },
  {
    "id": "eval_0112",
    "text": "mlflow ui",
    "category": "eval"
  },
  {
    "id": "eval_0113",
    "text": "conda create -n myenv python=3.11",
    "category": "eval"
  },
  {
    "id": "eval_0114",
    "text": "chkrootkit",
    "category": "eval"
  },
  {
    "id": "eval_0115",
    "text": "update kibana to latest",
    "category": "eval"
  },
  {
    "id": "eval_0116",
    "text": "checkmarx scan create",
    "category": "eval"
  },
  {
    "id": "eval_0117",
    "text": "deploy jenkins to production",
    "category": "eval"
  },
  {
    "id": "eval_0118",
    "text": "apache2ctl restart",
    "category": "eval"
  },
  {
    "id": "eval_0119",
    "text": "packer build template.json",
    "category": "eval"
  },
  {
    "id": "eval_0120",
    "text": "cargo clippy",
    "category": "eval"
  },
  {
    "id": "eval_0121",
    "text": "msfconsole",
    "category": "eval"
  },
  {
    "id": "eval_0122",
    "text": "pre-commit run --all-files",
    "category": "eval"
  },
  {
    "id": "eval_0123",
    "text": "run docker --help",
    "category": "eval"
  },
  {
    "id": "eval_0124",
    "text": "build jenkins from source",
    "category": "eval"
  },
  {
    "id": "eval_0125",
    "text": "build envoy from source",
    "category": "eval"
  },
  {
    "id": "eval_0126",
    "text": "nvidia-smi",
    "category": "eval"
  },
  {
    "id": "eval_0127",
    "text": "clamav --scan-dir=/tmp",
    "category": "eval"
  },
  {
    "id": "eval_0128",
    "text": "kubectl apply -f deployment.yaml",
    "category": "eval"
  },
  {
    "id": "eval_0129",
    "text": "export datadog data",
    "category": "eval"
  },
  {
    "id": "eval_0130",
    "text": "patch < changes.patch",
    "category": "eval"
  },
  {
    "id": "eval_0131",
    "text": "prowler -g check11,check12 -r us-east-1",
    "category": "eval"
  },
  {
    "id": "eval_0132",
    "text": "export vault data",
    "category": "eval"
  },
  {
    "id": "eval_0133",
    "text": "run tekton --help",
    "category": "eval"
  },
  {
    "id": "eval_0134",
    "text": "deploy jaeger to production",
    "category": "eval"
  },
  {
    "id": "eval_0135",
    "text": "update zipkin to latest",
    "category": "eval"
  },
  {
    "id": "eval_0136",
    "text": "update terraform to latest",
    "category": "eval"
  },
  {
    "id": "eval_0137",
    "text": "apptainer build myimage.sif def.def",
    "category": "eval"
  },
  {
    "id": "eval_0138",
    "text": "run github-actions --help",
    "category": "eval"
  },
  {
    "id": "eval_0139",
    "text": "update knative to latest",
    "category": "eval"
  },
  {
    "id": "eval_0140",
    "text": "head -100 large.log",
    "category": "eval"
  },
  {
    "id": "eval_0141",
    "text": "prometheus --config.file=prometheus.yml",
    "category": "eval"
  },
  {
    "id": "eval_0142",
    "text": "curl -X POST https://api.example.com/users -H 'Content-Type: application/json'",
    "category": "eval"
  },
  {
    "id": "eval_0143",
    "text": "run ansible --help",
    "category": "eval"
  },
  {
    "id": "eval_0144",
    "text": "install tempo plugin",
    "category": "eval"
  },
  {
    "id": "eval_0145",
    "text": "export DATABASE_URL=postgres://localhost/mydb",
    "category": "eval"
  },
  {
    "id": "eval_0146",
    "text": "nix-shell -p python3 numpy",
    "category": "eval"
  },
  {
    "id": "eval_0147",
    "text": "test nomad connection",
    "category": "eval"
  },
  {
    "id": "eval_0148",
    "text": "check drone status",
    "category": "eval"
  },
  {
    "id": "eval_0149",
    "text": "torchrun --nproc_per_node=4 train.py",
    "category": "eval"
  },
  {
    "id": "eval_0150",
    "text": "update linkerd to latest",
    "category": "eval"
  },
  {
    "id": "eval_0151",
    "text": "heroku logs --tail",
    "category": "eval"
  },
  {
    "id": "eval_0152",
    "text": "du -sh *",
    "category": "eval"
  },
  {
    "id": "eval_0153",
    "text": "free -h",
    "category": "eval"
  },
  {
    "id": "eval_0154",
    "text": "perf record -g ./app",
    "category": "eval"
  },
  {
    "id": "eval_0155",
    "text": "kill -9 1234",
    "category": "eval"
  },
  {
    "id": "eval_0156",
    "text": "update travisci to latest",
    "category": "eval"
  },
  {
    "id": "eval_0157",
    "text": "deploy helm to production",
    "category": "eval"
  },
  {
    "id": "eval_0158",
    "text": "flux reconcile source git flux-system",
    "category": "eval"
  },
  {
    "id": "eval_0159",
    "text": "nikto -h http://localhost",
    "category": "eval"
  },
  {
    "id": "eval_0160",
    "text": "run knative --help",
    "category": "eval"
  },
  {
    "id": "eval_0161",
    "text": "flit build",
    "category": "eval"
  },
  {
    "id": "eval_0162",
    "text": "jq '.data[] | .name' response.json",
    "category": "eval"
  },
  {
    "id": "eval_0163",
    "text": "terraform apply",
    "category": "eval"
  },
  {
    "id": "eval_0164",
    "text": "burpsuite",
    "category": "eval"
  },
  {
    "id": "eval_0165",
    "text": "python -m build",
    "category": "eval"
  },
  {
    "id": "eval_0166",
    "text": "mat2 --inplace document.pdf",
    "category": "eval"
  },
  {
    "id": "eval_0167",
    "text": "kubeaudit all",
    "category": "eval"
  },
  {
    "id": "eval_0168",
    "text": "ping google.com",
    "category": "eval"
  },
  {
    "id": "eval_0169",
    "text": "git stash",
    "category": "eval"
  },
  {
    "id": "eval_0170",
    "text": "configure istio settings",
    "category": "eval"
  },
  {
    "id": "eval_0171",
    "text": "tar -xzf archive.tar.gz",
    "category": "eval"
  },
  {
    "id": "eval_0172",
    "text": "exifprobe image.jpg",
    "category": "eval"
  },
  {
    "id": "eval_0173",
    "text": "kafka-topics --list",
    "category": "eval"
  },
  {
    "id": "eval_0174",
    "text": "envoy --config-path envoy.yaml",
    "category": "eval"
  },
  {
    "id": "eval_0175",
    "text": "ssh user@host",
    "category": "eval"
  },
  {
    "id": "eval_0176",
    "text": "list flux resources",
    "category": "eval"
  },
  {
    "id": "eval_0177",
    "text": "deploy travisci to production",
    "category": "eval"
  },
  {
    "id": "eval_0178",
    "text": "commitizen bump",
    "category": "eval"
  },
  {
    "id": "eval_0179",
    "text": "ray start --head",
    "category": "eval"
  },
  {
    "id": "eval_0180",
    "text": "masscan --rate=1000 -p1-65535 192.168.1.0/24",
    "category": "eval"
  },
  {
    "id": "eval_0181",
    "text": "update elasticsearch to latest",
    "category": "eval"
  },
  {
    "id": "eval_0182",
    "text": "pdm add requests",
    "category": "eval"
  },
  {
    "id": "eval_0183",
    "text": "list nomad resources",
    "category": "eval"
  },
  {
    "id": "eval_0184",
    "text": "update argocd to latest",
    "category": "eval"
  },
  {
    "id": "eval_0185",
    "text": "grep -r 'TODO' src/",
    "category": "eval"
  },
  {
    "id": "eval_0186",
    "text": "build argocd from source",
    "category": "eval"
  },
  {
    "id": "eval_0187",
    "text": "build docker from source",
    "category": "eval"
  },
  {
    "id": "eval_0188",
    "text": "ltrace ./binary",
    "category": "eval"
  },
  {
    "id": "eval_0189",
    "text": "autopsy",
    "category": "eval"
  },
  {
    "id": "eval_0190",
    "text": "singularity exec myimage.sif python train.py",
    "category": "eval"
  },
  {
    "id": "eval_0191",
    "text": "tcpdump -i eth0 -w capture.pcap",
    "category": "eval"
  },
  {
    "id": "eval_0192",
    "text": "bump2version patch",
    "category": "eval"
  },
  {
    "id": "eval_0193",
    "text": "salt '*' test.ping",
    "category": "eval"
  },
  {
    "id": "eval_0194",
    "text": "list helm resources",
    "category": "eval"
  },
  {
    "id": "eval_0195",
    "text": "kubectl get pods",
    "category": "eval"
  },
  {
    "id": "eval_0196",
    "text": "bitwarden-cli get password myitem",
    "category": "eval"
  },
  {
    "id": "eval_0197",
    "text": "update tempo to latest",
    "category": "eval"
  },
  {
    "id": "eval_0198",
    "text": "test consul connection",
    "category": "eval"
  },
  {
    "id": "eval_0199",
    "text": "mamba install numpy",
    "category": "eval"
  },
  {
    "id": "eval_0200",
    "text": "helm install myapp ./chart",
    "category": "eval"
  },
  {
    "id": "eval_0201",
    "text": "isort --check-only src/",
    "category": "eval"
  },
  {
    "id": "eval_0202",
    "text": "export github-actions data",
    "category": "eval"
  },
  {
    "id": "eval_0203",
    "text": "semantic-release version",
    "category": "eval"
  },
  {
    "id": "eval_0204",
    "text": "install docker plugin",
    "category": "eval"
  },
  {
    "id": "eval_0205",
    "text": "run prometheus --help",
    "category": "eval"
  },
  {
    "id": "eval_0206",
    "text": "snort -c /etc/snort/snort.conf -i eth0",
    "category": "eval"
  },
  {
    "id": "eval_0207",
    "text": "check kubernetes status",
    "category": "eval"
  },
  {
    "id": "eval_0208",
    "text": "check packer status",
    "category": "eval"
  },
  {
    "id": "eval_0209",
    "text": "lima start default.yaml",
    "category": "eval"
  },
  {
    "id": "eval_0210",
    "text": "test argocd connection",
    "category": "eval"
  },
  {
    "id": "eval_0211",
    "text": "build loki from source",
    "category": "eval"
  },
  {
    "id": "eval_0212",
    "text": "redis-cli ping",
    "category": "eval"
  },
  {
    "id": "eval_0213",
    "text": "deploy kibana to production",
    "category": "eval"
  },
  {
    "id": "eval_0214",
    "text": "hashcat -a 0 -m 0 hashes.txt wordlist.txt",
    "category": "eval"
  },
  {
    "id": "eval_0215",
    "text": "node server.js",
    "category": "eval"
  },
  {
    "id": "eval_0216",
    "text": "build linkerd from source",
    "category": "eval"
  },
  {
    "id": "eval_0217",
    "text": "ansible all -m ping",
    "category": "eval"
  },
  {
    "id": "eval_0218",
    "text": "wrangler deploy",
    "category": "eval"
  },
  {
    "id": "eval_0219",
    "text": "scp file.txt user@host:/tmp/",
    "category": "eval"
  },
  {
    "id": "eval_0220",
    "text": "nft list ruleset",
    "category": "eval"
  },
  {
    "id": "eval_0221",
    "text": "pacu modules list",
    "category": "eval"
  },
  {
    "id": "eval_0222",
    "text": "guix build python",
    "category": "eval"
  },
  {
    "id": "eval_0223",
    "text": "test opentelemetry connection",
    "category": "eval"
  },
  {
    "id": "eval_0224",
    "text": "elasticsearch --daemon",
    "category": "eval"
  },
  {
    "id": "eval_0225",
    "text": "heroku config:set KEY=value",
    "category": "eval"
  },
  {
    "id": "eval_0226",
    "text": "export linkerd data",
    "category": "eval"
  },
  {
    "id": "eval_0227",
    "text": "cdk deploy",
    "category": "eval"
  },
  {
    "id": "eval_0228",
    "text": "docker run -p 8080:80 myapp",
    "category": "eval"
  },
  {
    "id": "eval_0229",
    "text": "cloudmapper collect --account my-account",
    "category": "eval"
  },
  {
    "id": "eval_0230",
    "text": "install flux plugin",
    "category": "eval"
  },
  {
    "id": "eval_0231",
    "text": "export consul data",
    "category": "eval"
  },
  {
    "id": "eval_0232",
    "text": "objdump -d ./binary | head -50",
    "category": "eval"
  },
  {
    "id": "eval_0233",
    "text": "export jaeger data",
    "category": "eval"
  },
  {
    "id": "eval_0234",
    "text": "install github-actions plugin",
    "category": "eval"
  },
  {
    "id": "eval_0235",
    "text": "run opentelemetry --help",
    "category": "eval"
  },
  {
    "id": "eval_0236",
    "text": "optimum-cli export onnx --model bert-base-uncased bert_onnx",
    "category": "eval"
  },
  {
    "id": "eval_0237",
    "text": "flyctl status",
    "category": "eval"
  },
  {
    "id": "eval_0238",
    "text": "trivy image myapp:latest",
    "category": "eval"
  },
  {
    "id": "eval_0239",
    "text": "latex document.tex",
    "category": "eval"
  },
  {
    "id": "eval_0240",
    "text": "chef-client --local-mode",
    "category": "eval"
  },
  {
    "id": "eval_0241",
    "text": "openscap xccdf eval --profile xccdf_org",
    "category": "eval"
  },
  {
    "id": "eval_0242",
    "text": "npm run dev",
    "category": "eval"
  },
  {
    "id": "eval_0243",
    "text": "helm upgrade myapp ./chart",
    "category": "eval"
  },
  {
    "id": "eval_0244",
    "text": "terraform destroy",
    "category": "eval"
  },
  {
    "id": "eval_0245",
    "text": "build github-actions from source",
    "category": "eval"
  },
  {
    "id": "eval_0246",
    "text": "list jaeger resources",
    "category": "eval"
  },
  {
    "id": "eval_0247",
    "text": "test vault connection",
    "category": "eval"
  },
  {
    "id": "eval_0248",
    "text": "lynis audit system",
    "category": "eval"
  },
  {
    "id": "eval_0249",
    "text": "git checkout -b feature/new-api",
    "category": "eval"
  },
  {
    "id": "eval_0250",
    "text": "voila notebook.ipynb",
    "category": "eval"
  },
  {
    "id": "eval_0251",
    "text": "build ansible from source",
    "category": "eval"
  },
  {
    "id": "eval_0252",
    "text": "sops --decrypt secrets.enc.yaml",
    "category": "eval"
  },
  {
    "id": "eval_0253",
    "text": "run jaeger --help",
    "category": "eval"
  },
  {
    "id": "eval_0254",
    "text": "check terraform status",
    "category": "eval"
  },
  {
    "id": "eval_0255",
    "text": "openssl req -new -x509 -days 365 -keyout key.pem -out cert.pem",
    "category": "eval"
  },
  {
    "id": "eval_0256",
    "text": "flask run --debug",
    "category": "eval"
  },
  {
    "id": "eval_0257",
    "text": "pytest -k test_encoder",
    "category": "eval"
  },
  {
    "id": "eval_0258",
    "text": "configure vagrant settings",
    "category": "eval"
  },
  {
    "id": "eval_0259",
    "text": "suricata -c suricata.yaml -i eth0",
    "category": "eval"
  },
  {
    "id": "eval_0260",
    "text": "deploy gitlab to production",
    "category": "eval"
  },
  {
    "id": "eval_0261",
    "text": "git-crypt unlock",
    "category": "eval"
  },
  {
    "id": "eval_0262",
    "text": "systemctl status nginx",
    "category": "eval"
  },
  {
    "id": "eval_0263",
    "text": "openai api chat.completions.create -m gpt-4",
    "category": "eval"
  },
  {
    "id": "eval_0264",
    "text": "install terraform plugin",
    "category": "eval"
  },
  {
    "id": "eval_0265",
    "text": "cloudsplaining generate --input-file policy.json",
    "category": "eval"
  },
  {
    "id": "eval_0266",
    "text": "radare2 ./binary",
    "category": "eval"
  },
  {
    "id": "eval_0267",
    "text": "git merge main",
    "category": "eval"
  },
  {
    "id": "eval_0268",
    "text": "test kibana connection",
    "category": "eval"
  },
  {
    "id": "eval_0269",
    "text": "test vagrant connection",
    "category": "eval"
  },
  {
    "id": "eval_0270",
    "text": "solr start -p 8983",
    "category": "eval"
  },
  {
    "id": "eval_0271",
    "text": "git log --oneline -20",
    "category": "eval"
  },
  {
    "id": "eval_0272",
    "text": "export grafana data",
    "category": "eval"
  },
  {
    "id": "eval_0273",
    "text": "virsh start myvm",
    "category": "eval"
  },
  {
    "id": "eval_0274",
    "text": "multipass launch 22.04 --name myvm",
    "category": "eval"
  },
  {
    "id": "eval_0275",
    "text": "rkhunter --check",
    "category": "eval"
  },
  {
    "id": "eval_0276",
    "text": "transformers-cli download --model bert-base-uncased",
    "category": "eval"
  },
  {
    "id": "eval_0277",
    "text": "aws secretsmanager get-secret-value --secret-id myapp",
    "category": "eval"
  },
  {
    "id": "eval_0278",
    "text": "ruff format --check src/",
    "category": "eval"
  },
  {
    "id": "eval_0279",
    "text": "top",
    "category": "eval"
  },
  {
    "id": "eval_0280",
    "text": "deploy logstash to production",
    "category": "eval"
  },
  {
    "id": "eval_0281",
    "text": "update helm to latest",
    "category": "eval"
  },
  {
    "id": "eval_0282",
    "text": "configure logstash settings",
    "category": "eval"
  },
  {
    "id": "eval_0283",
    "text": "nmap -sS -O localhost",
    "category": "eval"
  },
  {
    "id": "eval_0284",
    "text": "deploy prometheus to production",
    "category": "eval"
  },
  {
    "id": "eval_0285",
    "text": "gpg --armor --export user@example.com",
    "category": "eval"
  },
  {
    "id": "eval_0286",
    "text": "export prometheus data",
    "category": "eval"
  },
  {
    "id": "eval_0287",
    "text": "test tekton connection",
    "category": "eval"
  },
  {
    "id": "eval_0288",
    "text": "update fluentd to latest",
    "category": "eval"
  },
  {
    "id": "eval_0289",
    "text": "cwltool workflow.cwl input.yml",
    "category": "eval"
  },
  {
    "id": "eval_0290",
    "text": "configure circleci settings",
    "category": "eval"
  },
  {
    "id": "eval_0291",
    "text": "rabbitmq-server start",
    "category": "eval"
  },
  {
    "id": "eval_0292",
    "text": "shodan search 'apache'",
    "category": "eval"
  },
  {
    "id": "eval_0293",
    "text": "papermill input.ipynb output.ipynb -p alpha 0.1",
    "category": "eval"
  },
  {
    "id": "eval_0294",
    "text": "valgrind --leak-check=full ./app",
    "category": "eval"
  },
  {
    "id": "eval_0295",
    "text": "celery worker -A app.celery",
    "category": "eval"
  },
  {
    "id": "eval_0296",
    "text": "jest --coverage",
    "category": "eval"
  },
  {
    "id": "eval_0297",
    "text": "dotnet test",
    "category": "eval"
  },
  {
    "id": "eval_0298",
    "text": "update ansible to latest",
    "category": "eval"
  },
  {
    "id": "eval_0299",
    "text": "tracepath example.com",
    "category": "eval"
  },
  {
    "id": "eval_0300",
    "text": "zap-cli quick-scan http://localhost",
    "category": "eval"
  },
  {
    "id": "eval_0301",
    "text": "grafana-server --config /etc/grafana/grafana.ini",
    "category": "eval"
  },
  {
    "id": "eval_0302",
    "text": "nmap -sV localhost",
    "category": "eval"
  },
  {
    "id": "eval_0303",
    "text": "rustup update",
    "category": "eval"
  },
  {
    "id": "eval_0304",
    "text": "k6 run script.js",
    "category": "eval"
  },
  {
    "id": "eval_0305",
    "text": "check jenkins status",
    "category": "eval"
  },
  {
    "id": "eval_0306",
    "text": "vitest run",
    "category": "eval"
  },
  {
    "id": "eval_0307",
    "text": "journalctl -f -u nginx",
    "category": "eval"
  },
  {
    "id": "eval_0308",
    "text": "kubectl logs -f pod-name",
    "category": "eval"
  },
  {
    "id": "eval_0309",
    "text": "configure loki settings",
    "category": "eval"
  },
  {
    "id": "eval_0310",
    "text": "yq '.spec.containers[0].image' pod.yaml",
    "category": "eval"
  },
  {
    "id": "eval_0311",
    "text": "zeek -r capture.pcap",
    "category": "eval"
  },
  {
    "id": "eval_0312",
    "text": "test fluentd connection",
    "category": "eval"
  },
  {
    "id": "eval_0313",
    "text": "steghide extract -sf image.jpg",
    "category": "eval"
  },
  {
    "id": "eval_0314",
    "text": "check elasticsearch status",
    "category": "eval"
  },
  {
    "id": "eval_0315",
    "text": "cdk synth",
    "category": "eval"
  },
  {
    "id": "eval_0316",
    "text": "run nomad --help",
    "category": "eval"
  },
  {
    "id": "eval_0317",
    "text": "deploy istio to production",
    "category": "eval"
  },
  {
    "id": "eval_0318",
    "text": "export circleci data",
    "category": "eval"
  },
  {
    "id": "eval_0319",
    "text": "configure flux settings",
    "category": "eval"
  },
  {
    "id": "eval_0320",
    "text": "update vault to latest",
    "category": "eval"
  },
  {
    "id": "eval_0321",
    "text": "python -m pytest tests/ -v",
    "category": "eval"
  },
  {
    "id": "eval_0322",
    "text": "run tempo --help",
    "category": "eval"
  },
  {
    "id": "eval_0323",
    "text": "configure travisci settings",
    "category": "eval"
  },
  {
    "id": "eval_0324",
    "text": "configure gitlab settings",
    "category": "eval"
  },
  {
    "id": "eval_0325",
    "text": "sam deploy",
    "category": "eval"
  },
  {
    "id": "eval_0326",
    "text": "jupyter lab",
    "category": "eval"
  },
  {
    "id": "eval_0327",
    "text": "dask-scheduler",
    "category": "eval"
  },
  {
    "id": "eval_0328",
    "text": "wrk -t4 -c100 -d30s http://localhost:8080/api/v1/",
    "category": "eval"
  },
  {
    "id": "eval_0329",
    "text": "strace -p 1234",
    "category": "eval"
  },
  {
    "id": "eval_0330",
    "text": "deploy ansible to production",
    "category": "eval"
  },
  {
    "id": "eval_0331",
    "text": "test github-actions connection",
    "category": "eval"
  },
  {
    "id": "eval_0332",
    "text": "ghidra",
    "category": "eval"
  },
  {
    "id": "eval_0333",
    "text": "mysqldump mydb > backup.sql",
    "category": "eval"
  },
  {
    "id": "eval_0334",
    "text": "configure ansible settings",
    "category": "eval"
  },
  {
    "id": "eval_0335",
    "text": "build flux from source",
    "category": "eval"
  },
  {
    "id": "eval_0336",
    "text": "go test ./...",
    "category": "eval"
  },
  {
    "id": "eval_0337",
    "text": "deploy envoy to production",
    "category": "eval"
  },
  {
    "id": "eval_0338",
    "text": "tshark -r capture.pcap -T fields -e http.request.uri",
    "category": "eval"
  },
  {
    "id": "eval_0339",
    "text": "eslint src/ --ext .ts",
    "category": "eval"
  },
  {
    "id": "eval_0340",
    "text": "run alertmanager --help",
    "category": "eval"
  },
  {
    "id": "eval_0341",
    "text": "nix build .#myapp",
    "category": "eval"
  },
  {
    "id": "eval_0342",
    "text": "zookeeper-server-start.sh config/zookeeper.properties",
    "category": "eval"
  },
  {
    "id": "eval_0343",
    "text": "build datadog from source",
    "category": "eval"
  },
  {
    "id": "eval_0344",
    "text": "pages-functions-build",
    "category": "eval"
  },
  {
    "id": "eval_0345",
    "text": "sentry-cli releases new 1.0.0",
    "category": "eval"
  },
  {
    "id": "eval_0346",
    "text": "gitleaks detect --source .",
    "category": "eval"
  },
  {
    "id": "eval_0347",
    "text": "rails db:migrate",
    "category": "eval"
  },
  {
    "id": "eval_0348",
    "text": "detect-secrets scan",
    "category": "eval"
  },
  {
    "id": "eval_0349",
    "text": "build istio from source",
    "category": "eval"
  },
  {
    "id": "eval_0350",
    "text": "linkerd check",
    "category": "eval"
  },
  {
    "id": "eval_0351",
    "text": "john --wordlist=rockyou.txt hashes.txt",
    "category": "eval"
  },
  {
    "id": "eval_0352",
    "text": "configure kibana settings",
    "category": "eval"
  },
  {
    "id": "eval_0353",
    "text": "test drone connection",
    "category": "eval"
  },
  {
    "id": "eval_0354",
    "text": "ab -n 1000 -c 10 http://localhost:8080/",
    "category": "eval"
  },
  {
    "id": "eval_0355",
    "text": "certbot renew",
    "category": "eval"
  },
  {
    "id": "eval_0356",
    "text": "export flux data",
    "category": "eval"
  },
  {
    "id": "eval_0357",
    "text": "which python3",
    "category": "eval"
  },
  {
    "id": "eval_0358",
    "text": "convert image.jpg -resize 50% thumbnail.jpg",
    "category": "eval"
  },
  {
    "id": "eval_0359",
    "text": "update jaeger to latest",
    "category": "eval"
  },
  {
    "id": "eval_0360",
    "text": "vault kv get secret/myapp",
    "category": "eval"
  },
  {
    "id": "eval_0361",
    "text": "configure tekton settings",
    "category": "eval"
  },
  {
    "id": "eval_0362",
    "text": "list zipkin resources",
    "category": "eval"
  },
  {
    "id": "eval_0363",
    "text": "dbt test",
    "category": "eval"
  },
  {
    "id": "eval_0364",
    "text": "newrelic-admin run-program gunicorn main:app",
    "category": "eval"
  },
  {
    "id": "eval_0365",
    "text": "gcloud compute instances list",
    "category": "eval"
  },
  {
    "id": "eval_0366",
    "text": "mongosh --eval 'db.stats()'",
    "category": "eval"
  },
  {
    "id": "eval_0367",
    "text": "build terraform from source",
    "category": "eval"
  },
  {
    "id": "eval_0368",
    "text": "export newrelic data",
    "category": "eval"
  },
  {
    "id": "eval_0369",
    "text": "exiftool image.jpg",
    "category": "eval"
  },
  {
    "id": "eval_0370",
    "text": "waypoint up",
    "category": "eval"
  },
  {
    "id": "eval_0371",
    "text": "binwalk -e firmware.bin",
    "category": "eval"
  },
  {
    "id": "eval_0372",
    "text": "run circleci --help",
    "category": "eval"
  },
  {
    "id": "eval_0373",
    "text": "rails server",
    "category": "eval"
  },
  {
    "id": "eval_0374",
    "text": "deploy datadog to production",
    "category": "eval"
  },
  {
    "id": "eval_0375",
    "text": "poetry add requests",
    "category": "eval"
  },
  {
    "id": "eval_0376",
    "text": "export elasticsearch data",
    "category": "eval"
  },
  {
    "id": "eval_0377",
    "text": "bandit -r src/",
    "category": "eval"
  },
  {
    "id": "eval_0378",
    "text": "check gitlab status",
    "category": "eval"
  },
  {
    "id": "eval_0379",
    "text": "anthropic-sdk --version",
    "category": "eval"
  },
  {
    "id": "eval_0380",
    "text": "test istio connection",
    "category": "eval"
  },
  {
    "id": "eval_0381",
    "text": "tflint --recursive",
    "category": "eval"
  },
  {
    "id": "eval_0382",
    "text": "export kubernetes data",
    "category": "eval"
  },
  {
    "id": "eval_0383",
    "text": "git commit -m 'feat: add user auth'",
    "category": "eval"
  },
  {
    "id": "eval_0384",
    "text": "redis-cli SET key value",
    "category": "eval"
  },
  {
    "id": "eval_0385",
    "text": "volatility -f memory.dmp --profile=Win7SP1x64 pslist",
    "category": "eval"
  },
  {
    "id": "eval_0386",
    "text": "uvicorn main:app --reload",
    "category": "eval"
  },
  {
    "id": "eval_0387",
    "text": "foremost -i image.dd -o output/",
    "category": "eval"
  },
  {
    "id": "eval_0388",
    "text": "build zipkin from source",
    "category": "eval"
  },
  {
    "id": "eval_0389",
    "text": "triton-server --model-repository=/models",
    "category": "eval"
  },
  {
    "id": "eval_0390",
    "text": "dotnet build",
    "category": "eval"
  },
  {
    "id": "eval_0391",
    "text": "git pull --rebase",
    "category": "eval"
  },
  {
    "id": "eval_0392",
    "text": "cargo build --release",
    "category": "eval"
  },
  {
    "id": "eval_0393",
    "text": "trufflehog git --repo https://github.com/org/repo",
    "category": "eval"
  },
  {
    "id": "eval_0394",
    "text": "install gitlab plugin",
    "category": "eval"
  },
  {
    "id": "eval_0395",
    "text": "run jenkins --help",
    "category": "eval"
  },
  {
    "id": "eval_0396",
    "text": "cargo test",
    "category": "eval"
  },
  {
    "id": "eval_0397",
    "text": "flyctl deploy",
    "category": "eval"
  },
  {
    "id": "eval_0398",
    "text": "artillery run load.yml",
    "category": "eval"
  },
  {
    "id": "eval_0399",
    "text": "deploy flux to production",
    "category": "eval"
  },
  {
    "id": "eval_0400",
    "text": "configure splunk settings",
    "category": "eval"
  },
  {
    "id": "eval_0401",
    "text": "strace -e trace=network ./app",
    "category": "eval"
  },
  {
    "id": "eval_0402",
    "text": "gradle build",
    "category": "eval"
  },
  {
    "id": "eval_0403",
    "text": "update vagrant to latest",
    "category": "eval"
  },
  {
    "id": "eval_0404",
    "text": "update flux to latest",
    "category": "eval"
  },
  {
    "id": "eval_0405",
    "text": "terraform plan",
    "category": "eval"
  },
  {
    "id": "eval_0406",
    "text": "nvcc --version",
    "category": "eval"
  },
  {
    "id": "eval_0407",
    "text": "hadoop jar wordcount.jar WordCount input output",
    "category": "eval"
  },
  {
    "id": "eval_0408",
    "text": "tail -f /var/log/app.log",
    "category": "eval"
  },
  {
    "id": "eval_0409",
    "text": "deploy terraform to production",
    "category": "eval"
  },
  {
    "id": "eval_0410",
    "text": "install vagrant plugin",
    "category": "eval"
  },
  {
    "id": "eval_0411",
    "text": "install prometheus plugin",
    "category": "eval"
  },
  {
    "id": "eval_0412",
    "text": "ollama pull mistral",
    "category": "eval"
  },
  {
    "id": "eval_0413",
    "text": "aws ec2 describe-instances",
    "category": "eval"
  },
  {
    "id": "eval_0414",
    "text": "uv pip install requests",
    "category": "eval"
  },
  {
    "id": "eval_0415",
    "text": "gh issue list",
    "category": "eval"
  },
  {
    "id": "eval_0416",
    "text": "test splunk connection",
    "category": "eval"
  },
  {
    "id": "eval_0417",
    "text": "list travisci resources",
    "category": "eval"
  },
  {
    "id": "eval_0418",
    "text": "update envoy to latest",
    "category": "eval"
  },
  {
    "id": "eval_0419",
    "text": "conftest test deployment.yaml",
    "category": "eval"
  },
  {
    "id": "eval_0420",
    "text": "export opentelemetry data",
    "category": "eval"
  },
  {
    "id": "eval_0421",
    "text": "snyk test",
    "category": "eval"
  },
  {
    "id": "eval_0422",
    "text": "chown -R user:group /path",
    "category": "eval"
  },
  {
    "id": "eval_0423",
    "text": "nextflow run pipeline.nf",
    "category": "eval"
  },
  {
    "id": "eval_0424",
    "text": "bibtex document",
    "category": "eval"
  },
  {
    "id": "eval_0425",
    "text": "pip install -r requirements.txt",
    "category": "eval"
  },
  {
    "id": "eval_0426",
    "text": "pulumi up",
    "category": "eval"
  },
  {
    "id": "eval_0427",
    "text": "run splunk --help",
    "category": "eval"
  },
  {
    "id": "eval_0428",
    "text": "lxc launch ubuntu:22.04 mycontainer",
    "category": "eval"
  },
  {
    "id": "eval_0429",
    "text": "update github-actions to latest",
    "category": "eval"
  },
  {
    "id": "eval_0430",
    "text": "ida64",
    "category": "eval"
  },
  {
    "id": "eval_0431",
    "text": "docker-compose down",
    "category": "eval"
  },
  {
    "id": "eval_0432",
    "text": "install loki plugin",
    "category": "eval"
  },
  {
    "id": "eval_0433",
    "text": "wireshark capture.pcap",
    "category": "eval"
  },
  {
    "id": "eval_0434",
    "text": "export tekton data",
    "category": "eval"
  },
  {
    "id": "eval_0435",
    "text": "build jaeger from source",
    "category": "eval"
  },
  {
    "id": "eval_0436",
    "text": "systemctl restart nginx",
    "category": "eval"
  },
  {
    "id": "eval_0437",
    "text": "ray up cluster.yaml",
    "category": "eval"
  },
  {
    "id": "eval_0438",
    "text": "run vault --help",
    "category": "eval"
  },
  {
    "id": "eval_0439",
    "text": "test newrelic connection",
    "category": "eval"
  },
  {
    "id": "eval_0440",
    "text": "consul agent -dev",
    "category": "eval"
  },
  {
    "id": "eval_0441",
    "text": "check envoy status",
    "category": "eval"
  },
  {
    "id": "eval_0442",
    "text": "test elasticsearch connection",
    "category": "eval"
  },
  {
    "id": "eval_0443",
    "text": "go build ./...",
    "category": "eval"
  },
  {
    "id": "eval_0444",
    "text": "check logstash status",
    "category": "eval"
  },
  {
    "id": "eval_0445",
    "text": "install grafana plugin",
    "category": "eval"
  },
  {
    "id": "eval_0446",
    "text": "jaeger-all-in-one",
    "category": "eval"
  },
  {
    "id": "eval_0447",
    "text": "list opentelemetry resources",
    "category": "eval"
  },
  {
    "id": "eval_0448",
    "text": "deploy tekton to production",
    "category": "eval"
  },
  {
    "id": "eval_0449",
    "text": "regula run .",
    "category": "eval"
  },
  {
    "id": "eval_0450",
    "text": "php artisan serve",
    "category": "eval"
  },
  {
    "id": "eval_0451",
    "text": "vagrant up",
    "category": "eval"
  },
  {
    "id": "eval_0452",
    "text": "pytest -x -v --tb=short",
    "category": "eval"
  },
  {
    "id": "eval_0453",
    "text": "ansible-playbook site.yml -i inventory",
    "category": "eval"
  },
  {
    "id": "eval_0454",
    "text": "list istio resources",
    "category": "eval"
  },
  {
    "id": "eval_0455",
    "text": "certbot certonly --standalone",
    "category": "eval"
  },
  {
    "id": "eval_0456",
    "text": "openssl enc -aes-256-cbc -in file -out file.enc",
    "category": "eval"
  },
  {
    "id": "eval_0457",
    "text": "pulumi stack select dev",
    "category": "eval"
  },
  {
    "id": "eval_0458",
    "text": "nginx -t",
    "category": "eval"
  },
  {
    "id": "eval_0459",
    "text": "test tempo connection",
    "category": "eval"
  },
  {
    "id": "eval_0460",
    "text": "aws s3 ls s3://my-bucket",
    "category": "eval"
  },
  {
    "id": "eval_0461",
    "text": "puppet apply site.pp",
    "category": "eval"
  },
  {
    "id": "eval_0462",
    "text": "install logstash plugin",
    "category": "eval"
  },
  {
    "id": "eval_0463",
    "text": "gunicorn -w 4 main:app",
    "category": "eval"
  },
  {
    "id": "eval_0464",
    "text": "list vault resources",
    "category": "eval"
  },
  {
    "id": "eval_0465",
    "text": "git diff HEAD",
    "category": "eval"
  },
  {
    "id": "eval_0466",
    "text": "python -c 'import torch; print(torch.cuda.is_available())'",
    "category": "eval"
  },
  {
    "id": "eval_0467",
    "text": "conda activate myenv",
    "category": "eval"
  },
  {
    "id": "eval_0468",
    "text": "nomad agent -dev",
    "category": "eval"
  },
  {
    "id": "eval_0469",
    "text": "vagrant ssh",
    "category": "eval"
  },
  {
    "id": "eval_0470",
    "text": "make clean",
    "category": "eval"
  },
  {
    "id": "eval_0471",
    "text": "optipng *.png",
    "category": "eval"
  },
  {
    "id": "eval_0472",
    "text": "configure jenkins settings",
    "category": "eval"
  },
  {
    "id": "eval_0473",
    "text": "go mod tidy",
    "category": "eval"
  },
  {
    "id": "eval_0474",
    "text": "test prometheus connection",
    "category": "eval"
  },
  {
    "id": "eval_0475",
    "text": "test knative connection",
    "category": "eval"
  },
  {
    "id": "eval_0476",
    "text": "ps aux",
    "category": "eval"
  },
  {
    "id": "eval_0477",
    "text": "black src/ --check",
    "category": "eval"
  },
  {
    "id": "eval_0478",
    "text": "veracode-api-signing",
    "category": "eval"
  },
  {
    "id": "eval_0479",
    "text": "configure linkerd settings",
    "category": "eval"
  },
  {
    "id": "eval_0480",
    "text": "docker-compose up -d",
    "category": "eval"
  },
  {
    "id": "eval_0481",
    "text": "update jenkins to latest",
    "category": "eval"
  },
  {
    "id": "eval_0482",
    "text": "check vault status",
    "category": "eval"
  },
  {
    "id": "eval_0483",
    "text": "export jenkins data",
    "category": "eval"
  },
  {
    "id": "eval_0484",
    "text": "run terraform --help",
    "category": "eval"
  },
  {
    "id": "eval_0485",
    "text": "az vm list",
    "category": "eval"
  },
  {
    "id": "eval_0486",
    "text": "list github-actions resources",
    "category": "eval"
  },
  {
    "id": "eval_0487",
    "text": "syft myapp:latest -o spdx-json",
    "category": "eval"
  },
  {
    "id": "eval_0488",
    "text": "export helm data",
    "category": "eval"
  },
  {
    "id": "eval_0489",
    "text": "diff file1.txt file2.txt",
    "category": "eval"
  },
  {
    "id": "eval_0490",
    "text": "mvn test",
    "category": "eval"
  },
  {
    "id": "eval_0491",
    "text": "istio analyze",
    "category": "eval"
  },
  {
    "id": "eval_0492",
    "text": "make build",
    "category": "eval"
  },
  {
    "id": "eval_0493",
    "text": "iptables -L -n",
    "category": "eval"
  },
  {
    "id": "eval_0494",
    "text": "htop",
    "category": "eval"
  },
  {
    "id": "eval_0495",
    "text": "export vagrant data",
    "category": "eval"
  },
  {
    "id": "eval_0496",
    "text": "kube-score score deployment.yaml",
    "category": "eval"
  },
  {
    "id": "eval_0497",
    "text": "tsc --noEmit",
    "category": "eval"
  },
  {
    "id": "eval_0498",
    "text": "psql -U postgres -d mydb",
    "category": "eval"
  },
  {
    "id": "eval_0499",
    "text": "run linkerd --help",
    "category": "eval"
  },
  {
    "id": "eval_0500",
    "text": "virt-install --name myvm",
    "category": "eval"
  },
  {
    "id": "eval_0501",
    "text": "chmod +x script.sh",
    "category": "eval"
  },
  {
    "id": "eval_0502",
    "text": "run elasticsearch --help",
    "category": "eval"
  },
  {
    "id": "eval_0503",
    "text": "run zipkin --help",
    "category": "eval"
  },
  {
    "id": "eval_0504",
    "text": "test datadog connection",
    "category": "eval"
  },
  {
    "id": "eval_0505",
    "text": "export alertmanager data",
    "category": "eval"
  },
  {
    "id": "eval_0506",
    "text": "ollama run llama2",
    "category": "eval"
  },
  {
    "id": "eval_0507",
    "text": "litellm --model gpt-4 --port 8000",
    "category": "eval"
  },
  {
    "id": "eval_0508",
    "text": "tar -czf archive.tar.gz dir/",
    "category": "eval"
  },
  {
    "id": "eval_0509",
    "text": "run drone --help",
    "category": "eval"
  },
  {
    "id": "eval_0510",
    "text": "zip -r archive.zip dir/",
    "category": "eval"
  },
  {
    "id": "eval_0511",
    "text": "ruff check src/",
    "category": "eval"
  },
  {
    "id": "eval_0512",
    "text": "sort -u names.txt",
    "category": "eval"
  },
  {
    "id": "eval_0513",
    "text": "list tekton resources",
    "category": "eval"
  },
  {
    "id": "eval_0514",
    "text": "configure vault settings",
    "category": "eval"
  },
  {
    "id": "eval_0515",
    "text": "vault server -dev",
    "category": "eval"
  },
  {
    "id": "eval_0516",
    "text": "sam build",
    "category": "eval"
  },
  {
    "id": "eval_0517",
    "text": "bun run start",
    "category": "eval"
  },
  {
    "id": "eval_0518",
    "text": "install zipkin plugin",
    "category": "eval"
  },
  {
    "id": "eval_0519",
    "text": "poetry run python main.py",
    "category": "eval"
  },
  {
    "id": "eval_0520",
    "text": "hydra -l admin -P passwords.txt http-get://localhost",
    "category": "eval"
  },
  {
    "id": "eval_0521",
    "text": "pipx install black",
    "category": "eval"
  },
  {
    "id": "eval_0522",
    "text": "crontab -e",
    "category": "eval"
  },
  {
    "id": "eval_0523",
    "text": "run packer --help",
    "category": "eval"
  },
  {
    "id": "eval_0524",
    "text": "configure jaeger settings",
    "category": "eval"
  },
  {
    "id": "eval_0525",
    "text": "dockle myapp:latest",
    "category": "eval"
  },
  {
    "id": "eval_0526",
    "text": "type git",
    "category": "eval"
  },
  {
    "id": "eval_0527",
    "text": "dbt docs generate",
    "category": "eval"
  },
  {
    "id": "eval_0528",
    "text": "az group create --name mygroup --location eastus",
    "category": "eval"
  },
  {
    "id": "eval_0529",
    "text": "keybase sign -m 'message'",
    "category": "eval"
  },
  {
    "id": "eval_0530",
    "text": "bentoml serve service.py:svc",
    "category": "eval"
  },
  {
    "id": "eval_0531",
    "text": "traceroute google.com",
    "category": "eval"
  },
  {
    "id": "eval_0532",
    "text": "gradle test",
    "category": "eval"
  },
  {
    "id": "eval_0533",
    "text": "locust -f locustfile.py --headless -u 100 -r 10",
    "category": "eval"
  },
  {
    "id": "eval_0534",
    "text": "export ansible data",
    "category": "eval"
  },
  {
    "id": "eval_0535",
    "text": "deno run main.ts",
    "category": "eval"
  },
  {
    "id": "eval_0536",
    "text": "awk '{print $1, $3}' access.log",
    "category": "eval"
  },
  {
    "id": "eval_0537",
    "text": "deploy vault to production",
    "category": "eval"
  },
  {
    "id": "eval_0538",
    "text": "grep -rn 'import' .",
    "category": "eval"
  },
  {
    "id": "eval_0539",
    "text": "pip install -e '.[dev]'",
    "category": "eval"
  },
  {
    "id": "eval_0540",
    "text": "at 2:00 AM tomorrow",
    "category": "eval"
  },
  {
    "id": "eval_0541",
    "text": "check circleci status",
    "category": "eval"
  },
  {
    "id": "eval_0542",
    "text": "checkov -d .",
    "category": "eval"
  },
  {
    "id": "eval_0543",
    "text": "tensorrt --help",
    "category": "eval"
  },
  {
    "id": "eval_0544",
    "text": "find . -name '*.py' -type f",
    "category": "eval"
  },
  {
    "id": "eval_0545",
    "text": "sleuthkit fls -r image.dd",
    "category": "eval"
  },
  {
    "id": "eval_0546",
    "text": "sed -i 's/old/new/g' file.txt",
    "category": "eval"
  },
  {
    "id": "eval_0547",
    "text": "install nomad plugin",
    "category": "eval"
  },
  {
    "id": "eval_0548",
    "text": "install kibana plugin",
    "category": "eval"
  },
  {
    "id": "eval_0549",
    "text": "fortify sca -b myapp -scan",
    "category": "eval"
  },
  {
    "id": "eval_0550",
    "text": "export gitlab data",
    "category": "eval"
  },
  {
    "id": "eval_0551",
    "text": "gh repo clone org/repo",
    "category": "eval"
  },
  {
    "id": "eval_0552",
    "text": "steampipe query 'select * from aws_iam_user'",
    "category": "eval"
  },
  {
    "id": "eval_0553",
    "text": "check travisci status",
    "category": "eval"
  },
  {
    "id": "eval_0554",
    "text": "build alertmanager from source",
    "category": "eval"
  },
  {
    "id": "eval_0555",
    "text": "spark-submit --master local[4] job.py",
    "category": "eval"
  },
  {
    "id": "eval_0556",
    "text": "ffmpeg -i input.mp4 -c:v h264 output.mp4",
    "category": "eval"
  },
  {
    "id": "eval_0557",
    "text": "install linkerd plugin",
    "category": "eval"
  },
  {
    "id": "eval_0558",
    "text": "mlflow run . -P alpha=0.5",
    "category": "eval"
  },
  {
    "id": "eval_0559",
    "text": "export knative data",
    "category": "eval"
  },
  {
    "id": "eval_0560",
    "text": "test gitlab connection",
    "category": "eval"
  },
  {
    "id": "eval_0561",
    "text": "falco --config /etc/falco/falco.yaml",
    "category": "eval"
  },
  {
    "id": "eval_0562",
    "text": "install splunk plugin",
    "category": "eval"
  },
  {
    "id": "eval_0563",
    "text": "argocd app sync myapp",
    "category": "eval"
  },
  {
    "id": "eval_0564",
    "text": "ss -tlnp",
    "category": "eval"
  },
  {
    "id": "eval_0565",
    "text": "printenv PATH",
    "category": "eval"
  },
  {
    "id": "eval_0566",
    "text": "php artisan migrate",
    "category": "eval"
  },
  {
    "id": "eval_0567",
    "text": "deploy kubernetes to production",
    "category": "eval"
  },
  {
    "id": "eval_0568",
    "text": "git push origin main",
    "category": "eval"
  },
  {
    "id": "eval_0569",
    "text": "celery beat -A app.celery",
    "category": "eval"
  },
  {
    "id": "eval_0570",
    "text": "docker build -t myapp .",
    "category": "eval"
  },
  {
    "id": "eval_0571",
    "text": "onnxruntime --help",
    "category": "eval"
  },
  {
    "id": "eval_0572",
    "text": "dbt run",
    "category": "eval"
  },
  {
    "id": "eval_0573",
    "text": "list linkerd resources",
    "category": "eval"
  },
  {
    "id": "eval_0574",
    "text": "configure prometheus settings",
    "category": "eval"
  },
  {
    "id": "eval_0575",
    "text": "checksec --file=./binary",
    "category": "eval"
  },
  {
    "id": "eval_0576",
    "text": "nbconvert --to html notebook.ipynb",
    "category": "eval"
  },
  {
    "id": "eval_0577",
    "text": "cilium status",
    "category": "eval"
  },
  {
    "id": "eval_0578",
    "text": "build knative from source",
    "category": "eval"
  },
  {
    "id": "eval_0579",
    "text": "cromwell run workflow.wdl inputs.json",
    "category": "eval"
  },
  {
    "id": "eval_0580",
    "text": "netstat -tulpn",
    "category": "eval"
  },
  {
    "id": "eval_0581",
    "text": "build drone from source",
    "category": "eval"
  },
  {
    "id": "eval_0582",
    "text": "aircrack-ng -w wordlist.txt -b 00:11:22:33:44:55 capture.cap",
    "category": "eval"
  },
  {
    "id": "eval_0583",
    "text": "yara rules.yar suspicious.exe",
    "category": "eval"
  },
  {
    "id": "eval_0584",
    "text": "huggingface-cli download bert-base-uncased",
    "category": "eval"
  },
  {
    "id": "eval_0585",
    "text": "pwndbg ./vulnerable",
    "category": "eval"
  },
  {
    "id": "eval_0586",
    "text": "semgrep --config=p/python .",
    "category": "eval"
  },
  {
    "id": "eval_0587",
    "text": "check argocd status",
    "category": "eval"
  },
  {
    "id": "eval_0588",
    "text": "cat README.md",
    "category": "eval"
  },
  {
    "id": "eval_0589",
    "text": "airflow dags list",
    "category": "eval"
  },
  {
    "id": "eval_0590",
    "text": "df -h",
    "category": "eval"
  },
  {
    "id": "eval_0591",
    "text": "check loki status",
    "category": "eval"
  },
  {
    "id": "eval_0592",
    "text": "install jenkins plugin",
    "category": "eval"
  },
  {
    "id": "eval_0593",
    "text": "kubectl rollout status deployment/myapp",
    "category": "eval"
  },
  {
    "id": "eval_0594",
    "text": "build logstash from source",
    "category": "eval"
  },
  {
    "id": "eval_0595",
    "text": "siege -c 50 -r 100 http://localhost/",
    "category": "eval"
  },
  {
    "id": "eval_0596",
    "text": "git status",
    "category": "eval"
  },
  {
    "id": "eval_0597",
    "text": "lldb ./app",
    "category": "eval"
  },
  {
    "id": "eval_0598",
    "text": "export docker data",
    "category": "eval"
  },
  {
    "id": "eval_0599",
    "text": "dependabot",
    "category": "eval"
  },
  {
    "id": "long_0000",
    "text": "Write a PostgreSQL query to find all users who have placed more than 5 orders in the last 30 days, where each order total exceeds $100, and who have not been flagged for fraud, grouped by user_id with aggregated statistics including total order count, total spend, average order value, first order date, last order date, and the names of the top 3 most frequently ordered products for each user, sorted by total spend descending, with pagination support using cursor-based pagination on user_id for efficient handling of large result sets. The query should use CTEs for readability, include appropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring.",
    "category": "long"
  },
  {
    "id": "long_0001",
    "text": "Create a Terraform configuration for a highly available three-tier web application on AWS consisting of: a public-facing Application Load Balancer with WAF integration and DDoS protection via AWS Shield Standard; an Auto Scaling Group of EC2 t3.medium instances running in private subnets across three availability zones with a minimum of 3 and maximum of 20 instances; an RDS PostgreSQL 16 Multi-AZ cluster with read replicas in each availability zone, automated backups retained for 30 days, and encryption at rest using AWS KMS; ElastiCache Redis 7 cluster mode enabled with 3 shards and 2 replicas per shard for session storage and caching; an S3 bucket with versioning, cross-region replication to us-west-2, and lifecycle policies to transition objects to Glacier after 90 days; CloudFront distribution fronting the ALB with custom SSL certificate from ACM, security headers policy, and edge caching for static assets; VPC with CIDR 10.0.0.0/16 split into 6 subnets (3 public /24, 3 private /24) with NAT gateways in each AZ; Security Groups following principle of least privilege with separate groups for ALB, web tier, app tier, and database tier.",
    "category": "long"
  },
  {
    "id": "long_0002",
    "text": "Analyze the following microservices architecture and identify potential single points of failure, bottlenecks, and security vulnerabilities: The system consists of an API gateway that routes requests to twelve backend services including user-service, auth-service, payment-service, notification-service, email-service, sms-service, recommendation-service, search-service, catalog-service, inventory-service, order-service, and shipping-service. Each service has its own PostgreSQL database with connection pooling configured via PgBouncer. Services communicate asynchronously via RabbitMQ for non-critical paths and synchronously via gRPC for critical paths like payment processing. The API gateway is implemented in Nginx with Lua scripting for rate limiting. Authentication uses JWT tokens with a 15-minute expiry refreshed automatically by the client. The recommendation service uses a pre-trained collaborative filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses.",
    "category": "long"
  },
  {
    "id": "long_0003",
    "text": "apiVersion: apps/v1 kind: Deployment metadata:   name: mind-nerve-encoder   namespace: production   labels:     app: mind-nerve     component: encoder     version: v0.4.0 spec:   replicas: 3   selector:     matchLabels:       app: mind-nerve       component: encoder   strategy:     type: RollingUpdate     rollingUpdate:       maxSurge: 1       maxUnavailable: 0   template:     metadata:       labels:         app: mind-nerve         component: encoder         version: v0.4.0       annotations:         prometheus.io/scrape: 'true'         prometheus.io/port: '9090'         prometheus.io/path: '/metrics'     spec:       securityContext:         runAsNonRoot: true         runAsUser: 1000         fsGroup: 1000       containers:       - name: encoder         image: ghcr.io/star-ga/mind-nerve:v0.4.0         ports:         - containerPort: 8080         - containerPort: 9090         resources:           requests:             cpu: 500m             memory: 512Mi           limits:             cpu: 2000m             memory: 2Gi         readinessProbe:           httpGet:             path: /health/ready             port: 8080           initialDelaySeconds: 10           periodSeconds: 5         livenessProbe:           httpGet:             path: /health/live             port: 8080           initialDelaySeconds: 30           periodSeconds: 10         env:         - name: MIND_NERVE_BACKEND           value: native         - name: MIND_NERVE_RUNTIME_DIR           value: /runtime         volumeMounts:         - name: runtime           mountPath: /runtime           readOnly: true       volumes:       - name: runtime         configMap:           name: mind-nerve-runtime",
    "category": "long"
  },
  {
    "id": "long_0004",
    "text": "from __future__ import annotations import asyncio import contextlib import dataclasses import hashlib import json import logging import os import time from collections.abc import AsyncGenerator, Generator from pathlib import Path from typing import Any, ClassVar import aiohttp import numpy as np logger = logging.getLogger(__name__) @dataclasses.dataclass(frozen=True, slots=True) class EncoderConfig:     model_hash: str     tokenizer_hash: str     hidden_dim: int = 384     num_heads: int = 12     num_layers: int = 12     max_seq_len: int = 256     window_size: int = 256     stride: int = 192     top_k: int = 5     backend: str = 'native'     runtime_dir: Path = dataclasses.field(         default_factory=lambda: Path.home() / '.local/share/mind-nerve/runtime')     SCHEMA_VERSION: ClassVar[int] = 1     def to_cache_key(self) -> str:         payload = json.dumps(dataclasses.asdict(self), sort_keys=True, default=str)         return hashlib.sha256(payload.encode()).hexdigest() class EncoderError(Exception):     pass class BackendNotAvailableError(EncoderError):     pass class EncoderTimeoutError(EncoderError):     pass @contextlib.asynccontextmanager async def managed_encoder(config: EncoderConfig) -> AsyncGenerator[AsyncEncoder, None]:     encoder = AsyncEncoder(config)     try:         await encoder.initialize()         yield encoder     finally:         await encoder.shutdown() class AsyncEncoder:     def __init__(self, config: EncoderConfig) -> None:         self._config = config         self._session: aiohttp.ClientSession | None = None         self._initialized = False         self._request_count = 0         self._error_count = 0         self._total_latency_ms = 0.0     async def initialize(self) -> None:         connector = aiohttp.TCPConnector(limit=100, limit_per_host=20)         self._session = aiohttp.ClientSession(connector=connector)         self._initialized = True         logger.info('AsyncEncoder initialized: backend=%s', self._config.backend)     async def encode(self, query: str, timeout: float = 5.0) -> np.ndarray:         if not self._initialized:             raise EncoderError('Encoder not initialized')         start = time.perf_counter()         try:             result = await asyncio.wait_for(self._encode_impl(query), timeout=timeout)             self._total_latency_ms += (time.perf_counter() - start) * 1000             self._request_count += 1             return result         except asyncio.TimeoutError as exc:             self._error_count += 1             raise EncoderTimeoutError(f'Encode timed out after {timeout}s') from exc     async def _encode_impl(self, query: str) -> np.ndarray:         raise NotImplementedError     async def shutdown(self) -> None:         if self._session:             await self._session.close()         self._initialized = False     @property     def stats(self) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) } ",
    "category": "long"
  },
  {
    "id": "long_0005",
    "text": "Implement a complete CI/CD pipeline for a Python machine learning project that includes the following stages: code quality checks using ruff, black, isort, mypy, and bandit; unit tests with pytest and coverage reporting; integration tests against a real PostgreSQL database using testcontainers; model training on a held-out validation set with automatic hyperparameter tuning using Optuna; model evaluation comparing the new model against the currently deployed baseline on a standardized benchmark dataset of 10,000 samples; model registration in MLflow with artifact storage on S3; Docker image build with multi-stage build optimized for production size under 500MB; security scanning using trivy and snyk; deployment to Kubernetes staging environment with smoke tests; automated load testing using k6 to validate p99 latency under 200ms at 1000 concurrent users; canary deployment to production with 5% traffic split; monitoring of key metrics for 30 minutes before full rollout; automatic rollback if error rate exceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials.",
    "category": "long"
  },
  {
    "id": "long_0006",
    "text": "package encoder import (     'context'     'crypto/sha256'     'encoding/binary'     'fmt'     'math'     'sync' ) const (     HiddenDim   = 384     NumHeads    = 12     HeadDim     = 32     Q16Scale    = 65536.0     LayerNormEps = 1e-12 ) type Q16 int32 func Float32ToQ16(f float32) Q16 {     scaled := float64(f) * Q16Scale     if scaled > math.MaxInt32 { return Q16(math.MaxInt32) }     if scaled < math.MinInt32 { return Q16(math.MinInt32) }     return Q16(math.Round(scaled)) } func (q Q16) ToFloat32() float32 { return float32(float64(q) / Q16Scale) } type Matrix struct { Data []Q16; Rows, Cols int } func NewMatrix(rows, cols int) *Matrix {     return &Matrix{Data: make([]Q16, rows*cols), Rows: rows, Cols: cols} } func (m *Matrix) At(row, col int) Q16 { return m.Data[row*m.Cols+col] } func (m *Matrix) Set(row, col int, v Q16) { m.Data[row*m.Cols+col] = v } func Matmul(a, b *Matrix) *Matrix {     if a.Cols != b.Rows { panic('dimension mismatch') }     c := NewMatrix(a.Rows, b.Cols)     var wg sync.WaitGroup     for i := 0; i < a.Rows; i++ {         wg.Add(1)         go func(row int) {             defer wg.Done()             for j := 0; j < b.Cols; j++ {                 var acc int64                 for k := 0; k < a.Cols; k++ {                     acc += int64(a.At(row, k)) * int64(b.At(k, j)) }                 c.Set(row, j, Q16(acc>>16)) } }(i) }     wg.Wait()     return c } func LayerNorm(x *Matrix, gamma, beta []Q16) *Matrix {     out := NewMatrix(x.Rows, x.Cols)     for i := 0; i < x.Rows; i++ {         var sum int64         for j := 0; j < x.Cols; j++ { sum += int64(x.At(i, j)) }         mean := Q16(sum / int64(x.Cols))         var varAcc int64         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             varAcc += d * d }         variance := float64(varAcc) / float64(x.Cols) / Q16Scale / Q16Scale         invStd := 1.0 / math.Sqrt(variance + LayerNormEps)         invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out } ",
    "category": "long"
  },
  {
    "id": "long_0007",
    "text": "class DistributedEmbeddingCache: \"\"\"Thread-safe distributed embedding cache backed by Redis with LRU eviction, TTL support, and automatic serialization of numpy float32 arrays to Q16.16 fixed-point representation. The cache key is SHA-256(model_hash + tokenizer_hash + query_text). Cache entries expire after ttl_seconds (default 3600). The cache uses a two-level hierarchy: L1 is an in-process LRU (maxsize=1024) and L2 is the shared Redis cluster. Write-back from L1 to L2 is asynchronous via a background thread. The serialize method converts float32[384] to Q16.16 i32[384] bytes with deterministic rounding for cross-arch reproducibility.\"\"\" def __init__(self, redis_url: str, ttl_seconds: int = 3600,              l1_maxsize: int = 1024) -> None:     self._redis = redis.Redis.from_url(redis_url)     self._ttl = ttl_seconds     self._l1 = functools.lru_cache(maxsize=l1_maxsize)(self._get_l2)     self._write_queue: queue.Queue = queue.Queue(maxsize=4096)     self._writer = threading.Thread(target=self._write_worker, daemon=True)     self._writer.start() def _cache_key(self, model_hash: str, tokenizer_hash: str, query: str) -> str:     payload = f'{model_hash}:{tokenizer_hash}:{query}'.encode('utf-8')     return 'emb:' + hashlib.sha256(payload).hexdigest() def get(self, model_hash: str, tokenizer_hash: str, query: str) -> np.ndarray | None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._redis.get(key)     if raw is None:         return None     return self._deserialize(raw) def put(self, model_hash: str, tokenizer_hash: str, query: str,         embedding: np.ndarray) -> None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._serialize(embedding)     self._redis.setex(key, self._ttl, raw) def _serialize(self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0)",
    "category": "long"
  },
  {
    "id": "long_0008",
    "text": "Architectural Decision Record: Switching from synchronous REST API calls to an event-driven architecture using Apache Kafka for the order processing pipeline. Context: The current synchronous REST-based order processing system suffers from cascading failures when downstream services are slow or unavailable. Peak load of 10,000 orders per minute causes timeouts in the payment service which propagates back to the API gateway causing HTTP 503 errors visible to customers. The average order processing time is 2.3 seconds due to sequential API calls to inventory, payment, notification, and shipping services. Decision: Implement event-driven architecture using Apache Kafka as the message broker. Each service publishes domain events and subscribes to relevant topics. Order creation publishes to order.created topic. Payment service subscribes to order.created and publishes payment.processed or payment.failed. Inventory service subscribes to payment.processed and publishes inventory.reserved or inventory.insufficient. The API immediately returns 202 Accepted with an order ID. Customers poll a read model or receive webhook notifications when order status changes. Consequences: Orders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully.",
    "category": "long"
  },
  {
    "id": "long_0009",
    "text": "Compare and contrast the following approaches to distributed consensus and explain when to use each one in the context of a globally distributed database system: Paxos (Classic Paxos, Multi-Paxos, and Fast Paxos variants), Raft (with and without pre-vote extension), Zab (ZooKeeper Atomic Broadcast), PBFT (Practical Byzantine Fault Tolerance), HotStuff (and its chained variant used in LibraBFT/DiemBFT), Tendermint/CometBFT, and Stellar Consensus Protocol. For each algorithm, discuss: the fault model (crash fault tolerant vs byzantine fault tolerant), the minimum number of nodes required for fault tolerance (f+1 vs 2f+1 vs 3f+1), the leader election mechanism and its impact on availability during leader failures, the message complexity per consensus round, the latency in terms of network round trips, the throughput in transactions per second for typical LAN and WAN deployments, the behavior under network partitions (CAP theorem position), the implementation complexity and available production-grade open-source implementations, and any known correctness bugs or safety violations discovered in production deployments. Then recommend the appropriate algorithm for a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants.",
    "category": "long"
  },
  {
    "id": "long_0010",
    "text": "import { createServer } from 'http'; import { WebSocketServer, WebSocket } from 'ws'; import { EventEmitter } from 'events'; interface ClientMessage { type: 'subscribe' | 'unsubscribe' | 'publish';   channel: string; payload?: unknown; clientId: string; } interface ServerMessage { type: 'data' | 'error' | 'ack';   channel?: string; payload?: unknown; timestamp: number; } class PubSubServer extends EventEmitter {   private channels = new Map<string, Set<WebSocket>>();   private clientChannels = new Map<WebSocket, Set<string>>();   constructor(private readonly port: number) { super(); }   subscribe(ws: WebSocket, channel: string): void {     if (!this.channels.has(channel)) this.channels.set(channel, new Set());     this.channels.get(channel)!.add(ws);     if (!this.clientChannels.has(ws)) this.clientChannels.set(ws, new Set());     this.clientChannels.get(ws)!.add(channel); }   unsubscribe(ws: WebSocket, channel: string): void {     this.channels.get(channel)?.delete(ws);     this.clientChannels.get(ws)?.delete(channel); }   publish(channel: string, payload: unknown): void {     const msg: ServerMessage = { type: 'data', channel, payload,       timestamp: Date.now() };     const data = JSON.stringify(msg);     this.channels.get(channel)?.forEach(client => {       if (client.readyState === WebSocket.OPEN) client.send(data); }); }   cleanup(ws: WebSocket): void {     this.clientChannels.get(ws)?.forEach(ch => this.unsubscribe(ws, ch));     this.clientChannels.delete(ws); }   listen(): void {     const server = createServer();     const wss = new WebSocketServer({ server });     wss.on('connection', ws => {       ws.on('message', raw => {         const msg: ClientMessage = JSON.parse(raw.toString());         switch (msg.type) {           case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } }",
    "category": "long"
  },
  {
    "id": "long_0011",
    "text": "Design and implement a fault-tolerant data pipeline that ingests streaming events from Apache Kafka topics (user.clicked, user.purchased, user.viewed, user.searched) at a rate of 500,000 events per second, applies real-time feature engineering including session reconstruction with a 30-minute session timeout, rolling window aggregations over 1-minute, 5-minute, and 1-hour windows, user-level and item-level statistics, and categorical feature encoding using frequency-adaptive target encoding with Laplace smoothing; writes the enriched feature vectors to a Redis Feature Store with a 24-hour TTL for online serving and to Apache Iceberg tables on S3 for offline training; handles late arriving events up to 5 minutes behind watermark using Apache Flink's event-time processing with allowed lateness; implements exactly-once semantics via Kafka transactions and Flink checkpointing to S3 every 60 seconds; provides a Grafana dashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint.",
    "category": "long"
  },
  {
    "id": "long_0012",
    "text": "Conduct a comprehensive security review of the following authentication implementation and identify all vulnerabilities with CVSS scores: The system uses JWT tokens with HS256 signing using a hardcoded secret key 'super_secret_key_12345' stored directly in the application code. Tokens have a 30-day expiry with no refresh mechanism. The token payload includes the user_id, role, email, and a boolean is_admin flag that is trusted by all downstream services without verification. Password hashing uses MD5 with a 4-character salt. The login endpoint does not implement rate limiting. Session tokens are stored in localStorage instead of httpOnly cookies. The password reset flow sends a 6-digit numeric OTP via email with a 24-hour expiry and no attempt limiting. The API does not validate Content-Type headers on POST requests. SQL queries for user lookup use string concatenation without parameterization. Cross-origin requests are allowed from all origins via Access-Control-Allow-Origin: *. The application runs as root in production containers. Debug endpoints exposing stack traces and environment variables are accessible without authentication in production. Please provide remediation recommendations for each vulnerability ordered by severity.",
    "category": "long"
  },
  {
    "id": "long_0013",
    "text": "#!/usr/bin/env bash set -euo pipefail IFS=$'\\n\\t' # Automated deployment script for mind-nerve v0.4.0 # Performs: build, test, security scan, docker build, push, deploy SCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\" REPO_ROOT=\"$(cd \"${SCRIPT_DIR}/..\" && pwd)\" VERSION=\"${1:-}\" ENVIRONMENT=\"${2:-staging}\" REGISTRY=\"ghcr.io/star-ga\" IMAGE=\"${REGISTRY}/mind-nerve\" if [[ -z \"${VERSION}\" ]]; then     echo 'ERROR: VERSION argument required' >&2     echo 'Usage: deploy.sh <version> [environment]' >&2     exit 1 fi echo \"==> Deploying mind-nerve ${VERSION} to ${ENVIRONMENT}\" cd \"${REPO_ROOT}\" echo '==> Running tests...' python -m pytest tests/ -x -q --tb=short echo '==> Running security scan...' bandit -r python/mind_nerve -ll echo '==> Building Docker image...' docker build --build-arg VERSION=\"${VERSION}\"              --build-arg BUILD_DATE=\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"              --build-arg VCS_REF=\"$(git rev-parse HEAD)\"              -t \"${IMAGE}:${VERSION}\"              -t \"${IMAGE}:latest\"              . echo '==> Scanning image for vulnerabilities...' trivy image --exit-code 1 --severity HIGH,CRITICAL \"${IMAGE}:${VERSION}\" echo '==> Pushing image...' docker push \"${IMAGE}:${VERSION}\" docker push \"${IMAGE}:latest\" echo '==> Deploying to Kubernetes...' kubectl set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\" ",
    "category": "long"
  },
  {
    "id": "long_0014",
    "text": "use std::collections::HashMap; use std::sync::{Arc, RwLock}; use std::time::{Duration, Instant}; #[derive(Debug, Clone)] pub struct CacheEntry<V> { value: V, inserted_at: Instant, ttl: Duration, } impl<V> CacheEntry<V> {     pub fn is_expired(&self) -> bool {         self.inserted_at.elapsed() >= self.ttl } } #[derive(Debug)] pub struct TtlCache<K, V> {     inner: Arc<RwLock<HashMap<K, CacheEntry<V>>>>,     default_ttl: Duration, } impl<K, V> TtlCache<K, V> where K: Eq + std::hash::Hash + Clone, V: Clone, {     pub fn new(default_ttl: Duration) -> Self {         Self { inner: Arc::new(RwLock::new(HashMap::new())), default_ttl } }     pub fn insert(&self, key: K, value: V) {         let entry = CacheEntry { value, inserted_at: Instant::now(),             ttl: self.default_ttl };         self.inner.write().unwrap().insert(key, entry); }     pub fn get(&self, key: &K) -> Option<V> {         let guard = self.inner.read().unwrap();         guard.get(key).filter(|e| !e.is_expired()).map(|e| e.value.clone()) }     pub fn evict_expired(&self) -> usize {         let mut guard = self.inner.write().unwrap();         let before = guard.len();         guard.retain(|_, e| !e.is_expired());         before - guard.len() }     pub fn len(&self) -> usize { self.inner.read().unwrap().len() }     pub fn is_empty(&self) -> bool { self.len() == 0 } }",
    "category": "long"
  },
  {
    "id": "long_0015",
    "text": "openapi: 3.1.0 info:   title: mind-nerve Encoding API   version: 0.4.0   description: |     Q16.16 native BERT encoder for intent classification and tool routing.     Encodes query strings into 384-dimensional L2-normalized embeddings     and returns top-K routing candidates from a precomputed catalog.     The encode endpoint accepts a JSON body with a query string and     optional parameters for top_k, backend selection, and runtime override.     All embeddings are returned as Q16.16 fixed-point integers to ensure     bit-identical results across x86_64 CPU backends. CUDA and ARM backends     are available but require explicit opt-in via the backend parameter.     Rate limiting is enforced at 1000 requests per minute per API key.     The catalog endpoint returns metadata about the currently loaded     routing catalog including version, size, and hash for cache validation. paths:   /v1/encode:     post:       operationId: encodeQuery       summary: Encode a query string and return top-K routing candidates       requestBody:         required: true         content:           application/json:             schema:               type: object               required: [query]               properties:                 query:                   type: string                   minLength: 1                   maxLength: 2048                   description: The query string to encode and route                 top_k:                   type: integer                   minimum: 1                   maximum: 100                   default: 5                 backend:                   type: string                   enum: [native, pytorch, cuda]                   default: native       responses:         '200':           description: Successful encoding and routing           content:             application/json:               schema:                 $ref: '#/components/schemas/RouteResult'         '400':           description: Invalid request (query too long, invalid backend)         '429':           description: Rate limit exceeded         '503':           description: Backend unavailable",
    "category": "long"
  },
  {
    "id": "long_0016",
    "text": "Optimize the following Python function that processes a list of 100,000 user records to compute personalized recommendations: def compute_recommendations(users: list[dict], products: list[dict], interactions: list[dict]) -> dict[str, list[str]]:     results = {}     for user in users:         user_interactions = [i for i in interactions                              if i['user_id'] == user['id']]         interacted_products = {i['product_id'] for i in user_interactions}         similar_users = []         for other_user in users:             if other_user['id'] == user['id']: continue             other_interactions = {i['product_id'] for i in interactions                                   if i['user_id'] == other_user['id']}             if len(interacted_products & other_interactions) >= 3:                 similar_users.append(other_user['id'])         recommended = []         for similar_user_id in similar_users[:10]:             similar_interactions = {i['product_id'] for i in interactions                                     if i['user_id'] == similar_user_id}             for prod_id in similar_interactions - interacted_products:                 if prod_id not in recommended:                     recommended.append(prod_id)         results[user['id']] = recommended[:20]     return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine.",
    "category": "long"
  },
  {
    "id": "long_0017",
    "text": "def process_batch(items: list[dict], batch_size: int = 32, timeout: float = 30.0, retry_count: int = 3, backoff_factor: float = 1.5) -> list[dict]: \"\"\"Process a batch of items with retry logic and exponential backoff. Each item is validated, transformed, and persisted to the database. Failed items are retried up to retry_count times with exponential backoff. Items that fail all retries are logged and returned in the error list. The function is thread-safe and can be called from multiple workers. Parameters: items (list[dict]) — the items to process; batch_size (int) — number of items per database transaction; timeout (float) — per-item timeout in seconds; retry_count (int) — maximum retries per item; backoff_factor (float) — exponential backoff multiplier. Returns: list of successfully processed items.\"\"\" results = [] errors = [] for i in range(0, len(items), batch_size):     chunk = items[i:i + batch_size]     with db.transaction():         for item in chunk:             for attempt in range(retry_count):                 try:                     validated = validate(item)                     transformed = transform(validated)                     persisted = persist(transformed, timeout=timeout)                     results.append(persisted)                     break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors",
    "category": "long"
  },
  {
    "id": "long_0018",
    "text": "Implement a complete CI/CD pipeline for a Python machine learning project that includes the following stages: code quality checks using ruff, black, isort, mypy, and bandit; unit tests with pytest and coverage reporting; integration tests against a real PostgreSQL database using testcontainers; model training on a held-out validation set with automatic hyperparameter tuning using Optuna; model evaluation comparing the new model against the currently deployed baseline on a standardized benchmark dataset of 10,000 samples; model registration in MLflow with artifact storage on S3; Docker image build with multi-stage build optimized for production size under 500MB; security scanning using trivy and snyk; deployment to Kubernetes staging environment with smoke tests; automated load testing using k6 to validate p99 latency under 200ms at 1000 concurrent users; canary deployment to production with 5% traffic split; monitoring of key metrics for 30 minutes before full rollout; automatic rollback if error rate exceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials. Furthermore, xceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials.",
    "category": "long"
  },
  {
    "id": "long_0019",
    "text": "class DistributedEmbeddingCache: \"\"\"Thread-safe distributed embedding cache backed by Redis with LRU eviction, TTL support, and automatic serialization of numpy float32 arrays to Q16.16 fixed-point representation. The cache key is SHA-256(model_hash + tokenizer_hash + query_text). Cache entries expire after ttl_seconds (default 3600). The cache uses a two-level hierarchy: L1 is an in-process LRU (maxsize=1024) and L2 is the shared Redis cluster. Write-back from L1 to L2 is asynchronous via a background thread. The serialize method converts float32[384] to Q16.16 i32[384] bytes with deterministic rounding for cross-arch reproducibility.\"\"\" def __init__(self, redis_url: str, ttl_seconds: int = 3600,              l1_maxsize: int = 1024) -> None:     self._redis = redis.Redis.from_url(redis_url)     self._ttl = ttl_seconds     self._l1 = functools.lru_cache(maxsize=l1_maxsize)(self._get_l2)     self._write_queue: queue.Queue = queue.Queue(maxsize=4096)     self._writer = threading.Thread(target=self._write_worker, daemon=True)     self._writer.start() def _cache_key(self, model_hash: str, tokenizer_hash: str, query: str) -> str:     payload = f'{model_hash}:{tokenizer_hash}:{query}'.encode('utf-8')     return 'emb:' + hashlib.sha256(payload).hexdigest() def get(self, model_hash: str, tokenizer_hash: str, query: str) -> np.ndarray | None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._redis.get(key)     if raw is None:         return None     return self._deserialize(raw) def put(self, model_hash: str, tokenizer_hash: str, query: str,         embedding: np.ndarray) -> None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._serialize(embedding)     self._redis.setex(key, self._ttl, raw) def _serialize(self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0) Furthermore, (self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0)",
    "category": "long"
  },
  {
    "id": "long_0020",
    "text": "use std::collections::HashMap; use std::sync::{Arc, RwLock}; use std::time::{Duration, Instant}; #[derive(Debug, Clone)] pub struct CacheEntry<V> { value: V, inserted_at: Instant, ttl: Duration, } impl<V> CacheEntry<V> {     pub fn is_expired(&self) -> bool {         self.inserted_at.elapsed() >= self.ttl } } #[derive(Debug)] pub struct TtlCache<K, V> {     inner: Arc<RwLock<HashMap<K, CacheEntry<V>>>>,     default_ttl: Duration, } impl<K, V> TtlCache<K, V> where K: Eq + std::hash::Hash + Clone, V: Clone, {     pub fn new(default_ttl: Duration) -> Self {         Self { inner: Arc::new(RwLock::new(HashMap::new())), default_ttl } }     pub fn insert(&self, key: K, value: V) {         let entry = CacheEntry { value, inserted_at: Instant::now(),             ttl: self.default_ttl };         self.inner.write().unwrap().insert(key, entry); }     pub fn get(&self, key: &K) -> Option<V> {         let guard = self.inner.read().unwrap();         guard.get(key).filter(|e| !e.is_expired()).map(|e| e.value.clone()) }     pub fn evict_expired(&self) -> usize {         let mut guard = self.inner.write().unwrap();         let before = guard.len();         guard.retain(|_, e| !e.is_expired());         before - guard.len() }     pub fn len(&self) -> usize { self.inner.read().unwrap().len() }     pub fn is_empty(&self) -> bool { self.len() == 0 } } Furthermore,  -> usize {         let mut guard = self.inner.write().unwrap();         let before = guard.len();         guard.retain(|_, e| !e.is_expired());         before - guard.len() }     pub fn len(&self) -> usize { self.inner.read().unwrap().len() }     pub fn is_empty(&self) -> bool { self.len() == 0 } }",
    "category": "long"
  },
  {
    "id": "long_0021",
    "text": "Design and implement a fault-tolerant data pipeline that ingests streaming events from Apache Kafka topics (user.clicked, user.purchased, user.viewed, user.searched) at a rate of 500,000 events per second, applies real-time feature engineering including session reconstruction with a 30-minute session timeout, rolling window aggregations over 1-minute, 5-minute, and 1-hour windows, user-level and item-level statistics, and categorical feature encoding using frequency-adaptive target encoding with Laplace smoothing; writes the enriched feature vectors to a Redis Feature Store with a 24-hour TTL for online serving and to Apache Iceberg tables on S3 for offline training; handles late arriving events up to 5 minutes behind watermark using Apache Flink's event-time processing with allowed lateness; implements exactly-once semantics via Kafka transactions and Flink checkpointing to S3 every 60 seconds; provides a Grafana dashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint. Furthermore, ashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint.",
    "category": "long"
  },
  {
    "id": "long_0022",
    "text": "Create a Terraform configuration for a highly available three-tier web application on AWS consisting of: a public-facing Application Load Balancer with WAF integration and DDoS protection via AWS Shield Standard; an Auto Scaling Group of EC2 t3.medium instances running in private subnets across three availability zones with a minimum of 3 and maximum of 20 instances; an RDS PostgreSQL 16 Multi-AZ cluster with read replicas in each availability zone, automated backups retained for 30 days, and encryption at rest using AWS KMS; ElastiCache Redis 7 cluster mode enabled with 3 shards and 2 replicas per shard for session storage and caching; an S3 bucket with versioning, cross-region replication to us-west-2, and lifecycle policies to transition objects to Glacier after 90 days; CloudFront distribution fronting the ALB with custom SSL certificate from ACM, security headers policy, and edge caching for static assets; VPC with CIDR 10.0.0.0/16 split into 6 subnets (3 public /24, 3 private /24) with NAT gateways in each AZ; Security Groups following principle of least privilege with separate groups for ALB, web tier, app tier, and database tier. Furthermore, rom ACM, security headers policy, and edge caching for static assets; VPC with CIDR 10.0.0.0/16 split into 6 subnets (3 public /24, 3 private /24) with NAT gateways in each AZ; Security Groups following principle of least privilege with separate groups for ALB, web tier, app tier, and database tier.",
    "category": "long"
  },
  {
    "id": "long_0023",
    "text": "Write a PostgreSQL query to find all users who have placed more than 5 orders in the last 30 days, where each order total exceeds $100, and who have not been flagged for fraud, grouped by user_id with aggregated statistics including total order count, total spend, average order value, first order date, last order date, and the names of the top 3 most frequently ordered products for each user, sorted by total spend descending, with pagination support using cursor-based pagination on user_id for efficient handling of large result sets. The query should use CTEs for readability, include appropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring. Furthermore, ppropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring.",
    "category": "long"
  },
  {
    "id": "long_0024",
    "text": "import { createServer } from 'http'; import { WebSocketServer, WebSocket } from 'ws'; import { EventEmitter } from 'events'; interface ClientMessage { type: 'subscribe' | 'unsubscribe' | 'publish';   channel: string; payload?: unknown; clientId: string; } interface ServerMessage { type: 'data' | 'error' | 'ack';   channel?: string; payload?: unknown; timestamp: number; } class PubSubServer extends EventEmitter {   private channels = new Map<string, Set<WebSocket>>();   private clientChannels = new Map<WebSocket, Set<string>>();   constructor(private readonly port: number) { super(); }   subscribe(ws: WebSocket, channel: string): void {     if (!this.channels.has(channel)) this.channels.set(channel, new Set());     this.channels.get(channel)!.add(ws);     if (!this.clientChannels.has(ws)) this.clientChannels.set(ws, new Set());     this.clientChannels.get(ws)!.add(channel); }   unsubscribe(ws: WebSocket, channel: string): void {     this.channels.get(channel)?.delete(ws);     this.clientChannels.get(ws)?.delete(channel); }   publish(channel: string, payload: unknown): void {     const msg: ServerMessage = { type: 'data', channel, payload,       timestamp: Date.now() };     const data = JSON.stringify(msg);     this.channels.get(channel)?.forEach(client => {       if (client.readyState === WebSocket.OPEN) client.send(data); }); }   cleanup(ws: WebSocket): void {     this.clientChannels.get(ws)?.forEach(ch => this.unsubscribe(ws, ch));     this.clientChannels.delete(ws); }   listen(): void {     const server = createServer();     const wss = new WebSocketServer({ server });     wss.on('connection', ws => {       ws.on('message', raw => {         const msg: ClientMessage = JSON.parse(raw.toString());         switch (msg.type) {           case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } } Furthermore,         case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } }",
    "category": "long"
  },
  {
    "id": "long_0025",
    "text": "#!/usr/bin/env bash set -euo pipefail IFS=$'\\n\\t' # Automated deployment script for mind-nerve v0.4.0 # Performs: build, test, security scan, docker build, push, deploy SCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\" REPO_ROOT=\"$(cd \"${SCRIPT_DIR}/..\" && pwd)\" VERSION=\"${1:-}\" ENVIRONMENT=\"${2:-staging}\" REGISTRY=\"ghcr.io/star-ga\" IMAGE=\"${REGISTRY}/mind-nerve\" if [[ -z \"${VERSION}\" ]]; then     echo 'ERROR: VERSION argument required' >&2     echo 'Usage: deploy.sh <version> [environment]' >&2     exit 1 fi echo \"==> Deploying mind-nerve ${VERSION} to ${ENVIRONMENT}\" cd \"${REPO_ROOT}\" echo '==> Running tests...' python -m pytest tests/ -x -q --tb=short echo '==> Running security scan...' bandit -r python/mind_nerve -ll echo '==> Building Docker image...' docker build --build-arg VERSION=\"${VERSION}\"              --build-arg BUILD_DATE=\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"              --build-arg VCS_REF=\"$(git rev-parse HEAD)\"              -t \"${IMAGE}:${VERSION}\"              -t \"${IMAGE}:latest\"              . echo '==> Scanning image for vulnerabilities...' trivy image --exit-code 1 --severity HIGH,CRITICAL \"${IMAGE}:${VERSION}\" echo '==> Pushing image...' docker push \"${IMAGE}:${VERSION}\" docker push \"${IMAGE}:latest\" echo '==> Deploying to Kubernetes...' kubectl set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\"  Furthermore, l set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\" ",
    "category": "long"
  },
  {
    "id": "long_0026",
    "text": "#!/usr/bin/env bash set -euo pipefail IFS=$'\\n\\t' # Automated deployment script for mind-nerve v0.4.0 # Performs: build, test, security scan, docker build, push, deploy SCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\" REPO_ROOT=\"$(cd \"${SCRIPT_DIR}/..\" && pwd)\" VERSION=\"${1:-}\" ENVIRONMENT=\"${2:-staging}\" REGISTRY=\"ghcr.io/star-ga\" IMAGE=\"${REGISTRY}/mind-nerve\" if [[ -z \"${VERSION}\" ]]; then     echo 'ERROR: VERSION argument required' >&2     echo 'Usage: deploy.sh <version> [environment]' >&2     exit 1 fi echo \"==> Deploying mind-nerve ${VERSION} to ${ENVIRONMENT}\" cd \"${REPO_ROOT}\" echo '==> Running tests...' python -m pytest tests/ -x -q --tb=short echo '==> Running security scan...' bandit -r python/mind_nerve -ll echo '==> Building Docker image...' docker build --build-arg VERSION=\"${VERSION}\"              --build-arg BUILD_DATE=\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"              --build-arg VCS_REF=\"$(git rev-parse HEAD)\"              -t \"${IMAGE}:${VERSION}\"              -t \"${IMAGE}:latest\"              . echo '==> Scanning image for vulnerabilities...' trivy image --exit-code 1 --severity HIGH,CRITICAL \"${IMAGE}:${VERSION}\" echo '==> Pushing image...' docker push \"${IMAGE}:${VERSION}\" docker push \"${IMAGE}:latest\" echo '==> Deploying to Kubernetes...' kubectl set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\"  Furthermore, l set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\" ",
    "category": "long"
  },
  {
    "id": "long_0027",
    "text": "Design and implement a fault-tolerant data pipeline that ingests streaming events from Apache Kafka topics (user.clicked, user.purchased, user.viewed, user.searched) at a rate of 500,000 events per second, applies real-time feature engineering including session reconstruction with a 30-minute session timeout, rolling window aggregations over 1-minute, 5-minute, and 1-hour windows, user-level and item-level statistics, and categorical feature encoding using frequency-adaptive target encoding with Laplace smoothing; writes the enriched feature vectors to a Redis Feature Store with a 24-hour TTL for online serving and to Apache Iceberg tables on S3 for offline training; handles late arriving events up to 5 minutes behind watermark using Apache Flink's event-time processing with allowed lateness; implements exactly-once semantics via Kafka transactions and Flink checkpointing to S3 every 60 seconds; provides a Grafana dashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint. Furthermore, ashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint.",
    "category": "long"
  },
  {
    "id": "long_0028",
    "text": "#!/usr/bin/env bash set -euo pipefail IFS=$'\\n\\t' # Automated deployment script for mind-nerve v0.4.0 # Performs: build, test, security scan, docker build, push, deploy SCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\" REPO_ROOT=\"$(cd \"${SCRIPT_DIR}/..\" && pwd)\" VERSION=\"${1:-}\" ENVIRONMENT=\"${2:-staging}\" REGISTRY=\"ghcr.io/star-ga\" IMAGE=\"${REGISTRY}/mind-nerve\" if [[ -z \"${VERSION}\" ]]; then     echo 'ERROR: VERSION argument required' >&2     echo 'Usage: deploy.sh <version> [environment]' >&2     exit 1 fi echo \"==> Deploying mind-nerve ${VERSION} to ${ENVIRONMENT}\" cd \"${REPO_ROOT}\" echo '==> Running tests...' python -m pytest tests/ -x -q --tb=short echo '==> Running security scan...' bandit -r python/mind_nerve -ll echo '==> Building Docker image...' docker build --build-arg VERSION=\"${VERSION}\"              --build-arg BUILD_DATE=\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"              --build-arg VCS_REF=\"$(git rev-parse HEAD)\"              -t \"${IMAGE}:${VERSION}\"              -t \"${IMAGE}:latest\"              . echo '==> Scanning image for vulnerabilities...' trivy image --exit-code 1 --severity HIGH,CRITICAL \"${IMAGE}:${VERSION}\" echo '==> Pushing image...' docker push \"${IMAGE}:${VERSION}\" docker push \"${IMAGE}:latest\" echo '==> Deploying to Kubernetes...' kubectl set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\"  Furthermore, l set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\" ",
    "category": "long"
  },
  {
    "id": "long_0029",
    "text": "Compare and contrast the following approaches to distributed consensus and explain when to use each one in the context of a globally distributed database system: Paxos (Classic Paxos, Multi-Paxos, and Fast Paxos variants), Raft (with and without pre-vote extension), Zab (ZooKeeper Atomic Broadcast), PBFT (Practical Byzantine Fault Tolerance), HotStuff (and its chained variant used in LibraBFT/DiemBFT), Tendermint/CometBFT, and Stellar Consensus Protocol. For each algorithm, discuss: the fault model (crash fault tolerant vs byzantine fault tolerant), the minimum number of nodes required for fault tolerance (f+1 vs 2f+1 vs 3f+1), the leader election mechanism and its impact on availability during leader failures, the message complexity per consensus round, the latency in terms of network round trips, the throughput in transactions per second for typical LAN and WAN deployments, the behavior under network partitions (CAP theorem position), the implementation complexity and available production-grade open-source implementations, and any known correctness bugs or safety violations discovered in production deployments. Then recommend the appropriate algorithm for a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants. Furthermore, or a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants.",
    "category": "long"
  },
  {
    "id": "long_0030",
    "text": "Design and implement a fault-tolerant data pipeline that ingests streaming events from Apache Kafka topics (user.clicked, user.purchased, user.viewed, user.searched) at a rate of 500,000 events per second, applies real-time feature engineering including session reconstruction with a 30-minute session timeout, rolling window aggregations over 1-minute, 5-minute, and 1-hour windows, user-level and item-level statistics, and categorical feature encoding using frequency-adaptive target encoding with Laplace smoothing; writes the enriched feature vectors to a Redis Feature Store with a 24-hour TTL for online serving and to Apache Iceberg tables on S3 for offline training; handles late arriving events up to 5 minutes behind watermark using Apache Flink's event-time processing with allowed lateness; implements exactly-once semantics via Kafka transactions and Flink checkpointing to S3 every 60 seconds; provides a Grafana dashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint. Furthermore, ashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint.",
    "category": "long"
  },
  {
    "id": "long_0031",
    "text": "Implement a complete CI/CD pipeline for a Python machine learning project that includes the following stages: code quality checks using ruff, black, isort, mypy, and bandit; unit tests with pytest and coverage reporting; integration tests against a real PostgreSQL database using testcontainers; model training on a held-out validation set with automatic hyperparameter tuning using Optuna; model evaluation comparing the new model against the currently deployed baseline on a standardized benchmark dataset of 10,000 samples; model registration in MLflow with artifact storage on S3; Docker image build with multi-stage build optimized for production size under 500MB; security scanning using trivy and snyk; deployment to Kubernetes staging environment with smoke tests; automated load testing using k6 to validate p99 latency under 200ms at 1000 concurrent users; canary deployment to production with 5% traffic split; monitoring of key metrics for 30 minutes before full rollout; automatic rollback if error rate exceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials. Furthermore, xceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials.",
    "category": "long"
  },
  {
    "id": "long_0032",
    "text": "#!/usr/bin/env bash set -euo pipefail IFS=$'\\n\\t' # Automated deployment script for mind-nerve v0.4.0 # Performs: build, test, security scan, docker build, push, deploy SCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\" REPO_ROOT=\"$(cd \"${SCRIPT_DIR}/..\" && pwd)\" VERSION=\"${1:-}\" ENVIRONMENT=\"${2:-staging}\" REGISTRY=\"ghcr.io/star-ga\" IMAGE=\"${REGISTRY}/mind-nerve\" if [[ -z \"${VERSION}\" ]]; then     echo 'ERROR: VERSION argument required' >&2     echo 'Usage: deploy.sh <version> [environment]' >&2     exit 1 fi echo \"==> Deploying mind-nerve ${VERSION} to ${ENVIRONMENT}\" cd \"${REPO_ROOT}\" echo '==> Running tests...' python -m pytest tests/ -x -q --tb=short echo '==> Running security scan...' bandit -r python/mind_nerve -ll echo '==> Building Docker image...' docker build --build-arg VERSION=\"${VERSION}\"              --build-arg BUILD_DATE=\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"              --build-arg VCS_REF=\"$(git rev-parse HEAD)\"              -t \"${IMAGE}:${VERSION}\"              -t \"${IMAGE}:latest\"              . echo '==> Scanning image for vulnerabilities...' trivy image --exit-code 1 --severity HIGH,CRITICAL \"${IMAGE}:${VERSION}\" echo '==> Pushing image...' docker push \"${IMAGE}:${VERSION}\" docker push \"${IMAGE}:latest\" echo '==> Deploying to Kubernetes...' kubectl set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\"  Furthermore, l set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\" ",
    "category": "long"
  },
  {
    "id": "long_0033",
    "text": "#!/usr/bin/env bash set -euo pipefail IFS=$'\\n\\t' # Automated deployment script for mind-nerve v0.4.0 # Performs: build, test, security scan, docker build, push, deploy SCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\" REPO_ROOT=\"$(cd \"${SCRIPT_DIR}/..\" && pwd)\" VERSION=\"${1:-}\" ENVIRONMENT=\"${2:-staging}\" REGISTRY=\"ghcr.io/star-ga\" IMAGE=\"${REGISTRY}/mind-nerve\" if [[ -z \"${VERSION}\" ]]; then     echo 'ERROR: VERSION argument required' >&2     echo 'Usage: deploy.sh <version> [environment]' >&2     exit 1 fi echo \"==> Deploying mind-nerve ${VERSION} to ${ENVIRONMENT}\" cd \"${REPO_ROOT}\" echo '==> Running tests...' python -m pytest tests/ -x -q --tb=short echo '==> Running security scan...' bandit -r python/mind_nerve -ll echo '==> Building Docker image...' docker build --build-arg VERSION=\"${VERSION}\"              --build-arg BUILD_DATE=\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"              --build-arg VCS_REF=\"$(git rev-parse HEAD)\"              -t \"${IMAGE}:${VERSION}\"              -t \"${IMAGE}:latest\"              . echo '==> Scanning image for vulnerabilities...' trivy image --exit-code 1 --severity HIGH,CRITICAL \"${IMAGE}:${VERSION}\" echo '==> Pushing image...' docker push \"${IMAGE}:${VERSION}\" docker push \"${IMAGE}:latest\" echo '==> Deploying to Kubernetes...' kubectl set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\"  Furthermore, l set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\" ",
    "category": "long"
  },
  {
    "id": "long_0034",
    "text": "from __future__ import annotations import asyncio import contextlib import dataclasses import hashlib import json import logging import os import time from collections.abc import AsyncGenerator, Generator from pathlib import Path from typing import Any, ClassVar import aiohttp import numpy as np logger = logging.getLogger(__name__) @dataclasses.dataclass(frozen=True, slots=True) class EncoderConfig:     model_hash: str     tokenizer_hash: str     hidden_dim: int = 384     num_heads: int = 12     num_layers: int = 12     max_seq_len: int = 256     window_size: int = 256     stride: int = 192     top_k: int = 5     backend: str = 'native'     runtime_dir: Path = dataclasses.field(         default_factory=lambda: Path.home() / '.local/share/mind-nerve/runtime')     SCHEMA_VERSION: ClassVar[int] = 1     def to_cache_key(self) -> str:         payload = json.dumps(dataclasses.asdict(self), sort_keys=True, default=str)         return hashlib.sha256(payload.encode()).hexdigest() class EncoderError(Exception):     pass class BackendNotAvailableError(EncoderError):     pass class EncoderTimeoutError(EncoderError):     pass @contextlib.asynccontextmanager async def managed_encoder(config: EncoderConfig) -> AsyncGenerator[AsyncEncoder, None]:     encoder = AsyncEncoder(config)     try:         await encoder.initialize()         yield encoder     finally:         await encoder.shutdown() class AsyncEncoder:     def __init__(self, config: EncoderConfig) -> None:         self._config = config         self._session: aiohttp.ClientSession | None = None         self._initialized = False         self._request_count = 0         self._error_count = 0         self._total_latency_ms = 0.0     async def initialize(self) -> None:         connector = aiohttp.TCPConnector(limit=100, limit_per_host=20)         self._session = aiohttp.ClientSession(connector=connector)         self._initialized = True         logger.info('AsyncEncoder initialized: backend=%s', self._config.backend)     async def encode(self, query: str, timeout: float = 5.0) -> np.ndarray:         if not self._initialized:             raise EncoderError('Encoder not initialized')         start = time.perf_counter()         try:             result = await asyncio.wait_for(self._encode_impl(query), timeout=timeout)             self._total_latency_ms += (time.perf_counter() - start) * 1000             self._request_count += 1             return result         except asyncio.TimeoutError as exc:             self._error_count += 1             raise EncoderTimeoutError(f'Encode timed out after {timeout}s') from exc     async def _encode_impl(self, query: str) -> np.ndarray:         raise NotImplementedError     async def shutdown(self) -> None:         if self._session:             await self._session.close()         self._initialized = False     @property     def stats(self) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) }  Furthermore, elf) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) } ",
    "category": "long"
  },
  {
    "id": "long_0035",
    "text": "from __future__ import annotations import asyncio import contextlib import dataclasses import hashlib import json import logging import os import time from collections.abc import AsyncGenerator, Generator from pathlib import Path from typing import Any, ClassVar import aiohttp import numpy as np logger = logging.getLogger(__name__) @dataclasses.dataclass(frozen=True, slots=True) class EncoderConfig:     model_hash: str     tokenizer_hash: str     hidden_dim: int = 384     num_heads: int = 12     num_layers: int = 12     max_seq_len: int = 256     window_size: int = 256     stride: int = 192     top_k: int = 5     backend: str = 'native'     runtime_dir: Path = dataclasses.field(         default_factory=lambda: Path.home() / '.local/share/mind-nerve/runtime')     SCHEMA_VERSION: ClassVar[int] = 1     def to_cache_key(self) -> str:         payload = json.dumps(dataclasses.asdict(self), sort_keys=True, default=str)         return hashlib.sha256(payload.encode()).hexdigest() class EncoderError(Exception):     pass class BackendNotAvailableError(EncoderError):     pass class EncoderTimeoutError(EncoderError):     pass @contextlib.asynccontextmanager async def managed_encoder(config: EncoderConfig) -> AsyncGenerator[AsyncEncoder, None]:     encoder = AsyncEncoder(config)     try:         await encoder.initialize()         yield encoder     finally:         await encoder.shutdown() class AsyncEncoder:     def __init__(self, config: EncoderConfig) -> None:         self._config = config         self._session: aiohttp.ClientSession | None = None         self._initialized = False         self._request_count = 0         self._error_count = 0         self._total_latency_ms = 0.0     async def initialize(self) -> None:         connector = aiohttp.TCPConnector(limit=100, limit_per_host=20)         self._session = aiohttp.ClientSession(connector=connector)         self._initialized = True         logger.info('AsyncEncoder initialized: backend=%s', self._config.backend)     async def encode(self, query: str, timeout: float = 5.0) -> np.ndarray:         if not self._initialized:             raise EncoderError('Encoder not initialized')         start = time.perf_counter()         try:             result = await asyncio.wait_for(self._encode_impl(query), timeout=timeout)             self._total_latency_ms += (time.perf_counter() - start) * 1000             self._request_count += 1             return result         except asyncio.TimeoutError as exc:             self._error_count += 1             raise EncoderTimeoutError(f'Encode timed out after {timeout}s') from exc     async def _encode_impl(self, query: str) -> np.ndarray:         raise NotImplementedError     async def shutdown(self) -> None:         if self._session:             await self._session.close()         self._initialized = False     @property     def stats(self) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) }  Furthermore, elf) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) } ",
    "category": "long"
  },
  {
    "id": "long_0036",
    "text": "class DistributedEmbeddingCache: \"\"\"Thread-safe distributed embedding cache backed by Redis with LRU eviction, TTL support, and automatic serialization of numpy float32 arrays to Q16.16 fixed-point representation. The cache key is SHA-256(model_hash + tokenizer_hash + query_text). Cache entries expire after ttl_seconds (default 3600). The cache uses a two-level hierarchy: L1 is an in-process LRU (maxsize=1024) and L2 is the shared Redis cluster. Write-back from L1 to L2 is asynchronous via a background thread. The serialize method converts float32[384] to Q16.16 i32[384] bytes with deterministic rounding for cross-arch reproducibility.\"\"\" def __init__(self, redis_url: str, ttl_seconds: int = 3600,              l1_maxsize: int = 1024) -> None:     self._redis = redis.Redis.from_url(redis_url)     self._ttl = ttl_seconds     self._l1 = functools.lru_cache(maxsize=l1_maxsize)(self._get_l2)     self._write_queue: queue.Queue = queue.Queue(maxsize=4096)     self._writer = threading.Thread(target=self._write_worker, daemon=True)     self._writer.start() def _cache_key(self, model_hash: str, tokenizer_hash: str, query: str) -> str:     payload = f'{model_hash}:{tokenizer_hash}:{query}'.encode('utf-8')     return 'emb:' + hashlib.sha256(payload).hexdigest() def get(self, model_hash: str, tokenizer_hash: str, query: str) -> np.ndarray | None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._redis.get(key)     if raw is None:         return None     return self._deserialize(raw) def put(self, model_hash: str, tokenizer_hash: str, query: str,         embedding: np.ndarray) -> None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._serialize(embedding)     self._redis.setex(key, self._ttl, raw) def _serialize(self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0) Furthermore, (self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0)",
    "category": "long"
  },
  {
    "id": "long_0037",
    "text": "Design and implement a fault-tolerant data pipeline that ingests streaming events from Apache Kafka topics (user.clicked, user.purchased, user.viewed, user.searched) at a rate of 500,000 events per second, applies real-time feature engineering including session reconstruction with a 30-minute session timeout, rolling window aggregations over 1-minute, 5-minute, and 1-hour windows, user-level and item-level statistics, and categorical feature encoding using frequency-adaptive target encoding with Laplace smoothing; writes the enriched feature vectors to a Redis Feature Store with a 24-hour TTL for online serving and to Apache Iceberg tables on S3 for offline training; handles late arriving events up to 5 minutes behind watermark using Apache Flink's event-time processing with allowed lateness; implements exactly-once semantics via Kafka transactions and Flink checkpointing to S3 every 60 seconds; provides a Grafana dashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint. Furthermore, ashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint.",
    "category": "long"
  },
  {
    "id": "long_0038",
    "text": "class DistributedEmbeddingCache: \"\"\"Thread-safe distributed embedding cache backed by Redis with LRU eviction, TTL support, and automatic serialization of numpy float32 arrays to Q16.16 fixed-point representation. The cache key is SHA-256(model_hash + tokenizer_hash + query_text). Cache entries expire after ttl_seconds (default 3600). The cache uses a two-level hierarchy: L1 is an in-process LRU (maxsize=1024) and L2 is the shared Redis cluster. Write-back from L1 to L2 is asynchronous via a background thread. The serialize method converts float32[384] to Q16.16 i32[384] bytes with deterministic rounding for cross-arch reproducibility.\"\"\" def __init__(self, redis_url: str, ttl_seconds: int = 3600,              l1_maxsize: int = 1024) -> None:     self._redis = redis.Redis.from_url(redis_url)     self._ttl = ttl_seconds     self._l1 = functools.lru_cache(maxsize=l1_maxsize)(self._get_l2)     self._write_queue: queue.Queue = queue.Queue(maxsize=4096)     self._writer = threading.Thread(target=self._write_worker, daemon=True)     self._writer.start() def _cache_key(self, model_hash: str, tokenizer_hash: str, query: str) -> str:     payload = f'{model_hash}:{tokenizer_hash}:{query}'.encode('utf-8')     return 'emb:' + hashlib.sha256(payload).hexdigest() def get(self, model_hash: str, tokenizer_hash: str, query: str) -> np.ndarray | None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._redis.get(key)     if raw is None:         return None     return self._deserialize(raw) def put(self, model_hash: str, tokenizer_hash: str, query: str,         embedding: np.ndarray) -> None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._serialize(embedding)     self._redis.setex(key, self._ttl, raw) def _serialize(self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0) Furthermore, (self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0)",
    "category": "long"
  },
  {
    "id": "long_0039",
    "text": "Compare and contrast the following approaches to distributed consensus and explain when to use each one in the context of a globally distributed database system: Paxos (Classic Paxos, Multi-Paxos, and Fast Paxos variants), Raft (with and without pre-vote extension), Zab (ZooKeeper Atomic Broadcast), PBFT (Practical Byzantine Fault Tolerance), HotStuff (and its chained variant used in LibraBFT/DiemBFT), Tendermint/CometBFT, and Stellar Consensus Protocol. For each algorithm, discuss: the fault model (crash fault tolerant vs byzantine fault tolerant), the minimum number of nodes required for fault tolerance (f+1 vs 2f+1 vs 3f+1), the leader election mechanism and its impact on availability during leader failures, the message complexity per consensus round, the latency in terms of network round trips, the throughput in transactions per second for typical LAN and WAN deployments, the behavior under network partitions (CAP theorem position), the implementation complexity and available production-grade open-source implementations, and any known correctness bugs or safety violations discovered in production deployments. Then recommend the appropriate algorithm for a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants. Furthermore, or a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants.",
    "category": "long"
  },
  {
    "id": "long_0040",
    "text": "Implement a complete CI/CD pipeline for a Python machine learning project that includes the following stages: code quality checks using ruff, black, isort, mypy, and bandit; unit tests with pytest and coverage reporting; integration tests against a real PostgreSQL database using testcontainers; model training on a held-out validation set with automatic hyperparameter tuning using Optuna; model evaluation comparing the new model against the currently deployed baseline on a standardized benchmark dataset of 10,000 samples; model registration in MLflow with artifact storage on S3; Docker image build with multi-stage build optimized for production size under 500MB; security scanning using trivy and snyk; deployment to Kubernetes staging environment with smoke tests; automated load testing using k6 to validate p99 latency under 200ms at 1000 concurrent users; canary deployment to production with 5% traffic split; monitoring of key metrics for 30 minutes before full rollout; automatic rollback if error rate exceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials. Furthermore, xceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials.",
    "category": "long"
  },
  {
    "id": "long_0041",
    "text": "Compare and contrast the following approaches to distributed consensus and explain when to use each one in the context of a globally distributed database system: Paxos (Classic Paxos, Multi-Paxos, and Fast Paxos variants), Raft (with and without pre-vote extension), Zab (ZooKeeper Atomic Broadcast), PBFT (Practical Byzantine Fault Tolerance), HotStuff (and its chained variant used in LibraBFT/DiemBFT), Tendermint/CometBFT, and Stellar Consensus Protocol. For each algorithm, discuss: the fault model (crash fault tolerant vs byzantine fault tolerant), the minimum number of nodes required for fault tolerance (f+1 vs 2f+1 vs 3f+1), the leader election mechanism and its impact on availability during leader failures, the message complexity per consensus round, the latency in terms of network round trips, the throughput in transactions per second for typical LAN and WAN deployments, the behavior under network partitions (CAP theorem position), the implementation complexity and available production-grade open-source implementations, and any known correctness bugs or safety violations discovered in production deployments. Then recommend the appropriate algorithm for a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants. Furthermore, or a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants.",
    "category": "long"
  },
  {
    "id": "long_0042",
    "text": "Write a PostgreSQL query to find all users who have placed more than 5 orders in the last 30 days, where each order total exceeds $100, and who have not been flagged for fraud, grouped by user_id with aggregated statistics including total order count, total spend, average order value, first order date, last order date, and the names of the top 3 most frequently ordered products for each user, sorted by total spend descending, with pagination support using cursor-based pagination on user_id for efficient handling of large result sets. The query should use CTEs for readability, include appropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring. Furthermore, ppropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring.",
    "category": "long"
  },
  {
    "id": "long_0043",
    "text": "Compare and contrast the following approaches to distributed consensus and explain when to use each one in the context of a globally distributed database system: Paxos (Classic Paxos, Multi-Paxos, and Fast Paxos variants), Raft (with and without pre-vote extension), Zab (ZooKeeper Atomic Broadcast), PBFT (Practical Byzantine Fault Tolerance), HotStuff (and its chained variant used in LibraBFT/DiemBFT), Tendermint/CometBFT, and Stellar Consensus Protocol. For each algorithm, discuss: the fault model (crash fault tolerant vs byzantine fault tolerant), the minimum number of nodes required for fault tolerance (f+1 vs 2f+1 vs 3f+1), the leader election mechanism and its impact on availability during leader failures, the message complexity per consensus round, the latency in terms of network round trips, the throughput in transactions per second for typical LAN and WAN deployments, the behavior under network partitions (CAP theorem position), the implementation complexity and available production-grade open-source implementations, and any known correctness bugs or safety violations discovered in production deployments. Then recommend the appropriate algorithm for a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants. Furthermore, or a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants.",
    "category": "long"
  },
  {
    "id": "long_0044",
    "text": "Compare and contrast the following approaches to distributed consensus and explain when to use each one in the context of a globally distributed database system: Paxos (Classic Paxos, Multi-Paxos, and Fast Paxos variants), Raft (with and without pre-vote extension), Zab (ZooKeeper Atomic Broadcast), PBFT (Practical Byzantine Fault Tolerance), HotStuff (and its chained variant used in LibraBFT/DiemBFT), Tendermint/CometBFT, and Stellar Consensus Protocol. For each algorithm, discuss: the fault model (crash fault tolerant vs byzantine fault tolerant), the minimum number of nodes required for fault tolerance (f+1 vs 2f+1 vs 3f+1), the leader election mechanism and its impact on availability during leader failures, the message complexity per consensus round, the latency in terms of network round trips, the throughput in transactions per second for typical LAN and WAN deployments, the behavior under network partitions (CAP theorem position), the implementation complexity and available production-grade open-source implementations, and any known correctness bugs or safety violations discovered in production deployments. Then recommend the appropriate algorithm for a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants. Furthermore, or a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants.",
    "category": "long"
  },
  {
    "id": "long_0045",
    "text": "Create a Terraform configuration for a highly available three-tier web application on AWS consisting of: a public-facing Application Load Balancer with WAF integration and DDoS protection via AWS Shield Standard; an Auto Scaling Group of EC2 t3.medium instances running in private subnets across three availability zones with a minimum of 3 and maximum of 20 instances; an RDS PostgreSQL 16 Multi-AZ cluster with read replicas in each availability zone, automated backups retained for 30 days, and encryption at rest using AWS KMS; ElastiCache Redis 7 cluster mode enabled with 3 shards and 2 replicas per shard for session storage and caching; an S3 bucket with versioning, cross-region replication to us-west-2, and lifecycle policies to transition objects to Glacier after 90 days; CloudFront distribution fronting the ALB with custom SSL certificate from ACM, security headers policy, and edge caching for static assets; VPC with CIDR 10.0.0.0/16 split into 6 subnets (3 public /24, 3 private /24) with NAT gateways in each AZ; Security Groups following principle of least privilege with separate groups for ALB, web tier, app tier, and database tier. Furthermore, rom ACM, security headers policy, and edge caching for static assets; VPC with CIDR 10.0.0.0/16 split into 6 subnets (3 public /24, 3 private /24) with NAT gateways in each AZ; Security Groups following principle of least privilege with separate groups for ALB, web tier, app tier, and database tier.",
    "category": "long"
  },
  {
    "id": "long_0046",
    "text": "Optimize the following Python function that processes a list of 100,000 user records to compute personalized recommendations: def compute_recommendations(users: list[dict], products: list[dict], interactions: list[dict]) -> dict[str, list[str]]:     results = {}     for user in users:         user_interactions = [i for i in interactions                              if i['user_id'] == user['id']]         interacted_products = {i['product_id'] for i in user_interactions}         similar_users = []         for other_user in users:             if other_user['id'] == user['id']: continue             other_interactions = {i['product_id'] for i in interactions                                   if i['user_id'] == other_user['id']}             if len(interacted_products & other_interactions) >= 3:                 similar_users.append(other_user['id'])         recommended = []         for similar_user_id in similar_users[:10]:             similar_interactions = {i['product_id'] for i in interactions                                     if i['user_id'] == similar_user_id}             for prod_id in similar_interactions - interacted_products:                 if prod_id not in recommended:                     recommended.append(prod_id)         results[user['id']] = recommended[:20]     return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine. Furthermore, return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine.",
    "category": "long"
  },
  {
    "id": "long_0047",
    "text": "Optimize the following Python function that processes a list of 100,000 user records to compute personalized recommendations: def compute_recommendations(users: list[dict], products: list[dict], interactions: list[dict]) -> dict[str, list[str]]:     results = {}     for user in users:         user_interactions = [i for i in interactions                              if i['user_id'] == user['id']]         interacted_products = {i['product_id'] for i in user_interactions}         similar_users = []         for other_user in users:             if other_user['id'] == user['id']: continue             other_interactions = {i['product_id'] for i in interactions                                   if i['user_id'] == other_user['id']}             if len(interacted_products & other_interactions) >= 3:                 similar_users.append(other_user['id'])         recommended = []         for similar_user_id in similar_users[:10]:             similar_interactions = {i['product_id'] for i in interactions                                     if i['user_id'] == similar_user_id}             for prod_id in similar_interactions - interacted_products:                 if prod_id not in recommended:                     recommended.append(prod_id)         results[user['id']] = recommended[:20]     return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine. Furthermore, return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine.",
    "category": "long"
  },
  {
    "id": "long_0048",
    "text": "Implement a complete CI/CD pipeline for a Python machine learning project that includes the following stages: code quality checks using ruff, black, isort, mypy, and bandit; unit tests with pytest and coverage reporting; integration tests against a real PostgreSQL database using testcontainers; model training on a held-out validation set with automatic hyperparameter tuning using Optuna; model evaluation comparing the new model against the currently deployed baseline on a standardized benchmark dataset of 10,000 samples; model registration in MLflow with artifact storage on S3; Docker image build with multi-stage build optimized for production size under 500MB; security scanning using trivy and snyk; deployment to Kubernetes staging environment with smoke tests; automated load testing using k6 to validate p99 latency under 200ms at 1000 concurrent users; canary deployment to production with 5% traffic split; monitoring of key metrics for 30 minutes before full rollout; automatic rollback if error rate exceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials. Furthermore, xceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials.",
    "category": "long"
  },
  {
    "id": "long_0049",
    "text": "package encoder import (     'context'     'crypto/sha256'     'encoding/binary'     'fmt'     'math'     'sync' ) const (     HiddenDim   = 384     NumHeads    = 12     HeadDim     = 32     Q16Scale    = 65536.0     LayerNormEps = 1e-12 ) type Q16 int32 func Float32ToQ16(f float32) Q16 {     scaled := float64(f) * Q16Scale     if scaled > math.MaxInt32 { return Q16(math.MaxInt32) }     if scaled < math.MinInt32 { return Q16(math.MinInt32) }     return Q16(math.Round(scaled)) } func (q Q16) ToFloat32() float32 { return float32(float64(q) / Q16Scale) } type Matrix struct { Data []Q16; Rows, Cols int } func NewMatrix(rows, cols int) *Matrix {     return &Matrix{Data: make([]Q16, rows*cols), Rows: rows, Cols: cols} } func (m *Matrix) At(row, col int) Q16 { return m.Data[row*m.Cols+col] } func (m *Matrix) Set(row, col int, v Q16) { m.Data[row*m.Cols+col] = v } func Matmul(a, b *Matrix) *Matrix {     if a.Cols != b.Rows { panic('dimension mismatch') }     c := NewMatrix(a.Rows, b.Cols)     var wg sync.WaitGroup     for i := 0; i < a.Rows; i++ {         wg.Add(1)         go func(row int) {             defer wg.Done()             for j := 0; j < b.Cols; j++ {                 var acc int64                 for k := 0; k < a.Cols; k++ {                     acc += int64(a.At(row, k)) * int64(b.At(k, j)) }                 c.Set(row, j, Q16(acc>>16)) } }(i) }     wg.Wait()     return c } func LayerNorm(x *Matrix, gamma, beta []Q16) *Matrix {     out := NewMatrix(x.Rows, x.Cols)     for i := 0; i < x.Rows; i++ {         var sum int64         for j := 0; j < x.Cols; j++ { sum += int64(x.At(i, j)) }         mean := Q16(sum / int64(x.Cols))         var varAcc int64         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             varAcc += d * d }         variance := float64(varAcc) / float64(x.Cols) / Q16Scale / Q16Scale         invStd := 1.0 / math.Sqrt(variance + LayerNormEps)         invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out }  Furthermore,     invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out } ",
    "category": "long"
  },
  {
    "id": "long_0050",
    "text": "Design and implement a fault-tolerant data pipeline that ingests streaming events from Apache Kafka topics (user.clicked, user.purchased, user.viewed, user.searched) at a rate of 500,000 events per second, applies real-time feature engineering including session reconstruction with a 30-minute session timeout, rolling window aggregations over 1-minute, 5-minute, and 1-hour windows, user-level and item-level statistics, and categorical feature encoding using frequency-adaptive target encoding with Laplace smoothing; writes the enriched feature vectors to a Redis Feature Store with a 24-hour TTL for online serving and to Apache Iceberg tables on S3 for offline training; handles late arriving events up to 5 minutes behind watermark using Apache Flink's event-time processing with allowed lateness; implements exactly-once semantics via Kafka transactions and Flink checkpointing to S3 every 60 seconds; provides a Grafana dashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint. Furthermore, ashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint.",
    "category": "long"
  },
  {
    "id": "long_0051",
    "text": "apiVersion: apps/v1 kind: Deployment metadata:   name: mind-nerve-encoder   namespace: production   labels:     app: mind-nerve     component: encoder     version: v0.4.0 spec:   replicas: 3   selector:     matchLabels:       app: mind-nerve       component: encoder   strategy:     type: RollingUpdate     rollingUpdate:       maxSurge: 1       maxUnavailable: 0   template:     metadata:       labels:         app: mind-nerve         component: encoder         version: v0.4.0       annotations:         prometheus.io/scrape: 'true'         prometheus.io/port: '9090'         prometheus.io/path: '/metrics'     spec:       securityContext:         runAsNonRoot: true         runAsUser: 1000         fsGroup: 1000       containers:       - name: encoder         image: ghcr.io/star-ga/mind-nerve:v0.4.0         ports:         - containerPort: 8080         - containerPort: 9090         resources:           requests:             cpu: 500m             memory: 512Mi           limits:             cpu: 2000m             memory: 2Gi         readinessProbe:           httpGet:             path: /health/ready             port: 8080           initialDelaySeconds: 10           periodSeconds: 5         livenessProbe:           httpGet:             path: /health/live             port: 8080           initialDelaySeconds: 30           periodSeconds: 10         env:         - name: MIND_NERVE_BACKEND           value: native         - name: MIND_NERVE_RUNTIME_DIR           value: /runtime         volumeMounts:         - name: runtime           mountPath: /runtime           readOnly: true       volumes:       - name: runtime         configMap:           name: mind-nerve-runtime Furthermore,  MIND_NERVE_BACKEND           value: native         - name: MIND_NERVE_RUNTIME_DIR           value: /runtime         volumeMounts:         - name: runtime           mountPath: /runtime           readOnly: true       volumes:       - name: runtime         configMap:           name: mind-nerve-runtime",
    "category": "long"
  },
  {
    "id": "long_0052",
    "text": "from __future__ import annotations import asyncio import contextlib import dataclasses import hashlib import json import logging import os import time from collections.abc import AsyncGenerator, Generator from pathlib import Path from typing import Any, ClassVar import aiohttp import numpy as np logger = logging.getLogger(__name__) @dataclasses.dataclass(frozen=True, slots=True) class EncoderConfig:     model_hash: str     tokenizer_hash: str     hidden_dim: int = 384     num_heads: int = 12     num_layers: int = 12     max_seq_len: int = 256     window_size: int = 256     stride: int = 192     top_k: int = 5     backend: str = 'native'     runtime_dir: Path = dataclasses.field(         default_factory=lambda: Path.home() / '.local/share/mind-nerve/runtime')     SCHEMA_VERSION: ClassVar[int] = 1     def to_cache_key(self) -> str:         payload = json.dumps(dataclasses.asdict(self), sort_keys=True, default=str)         return hashlib.sha256(payload.encode()).hexdigest() class EncoderError(Exception):     pass class BackendNotAvailableError(EncoderError):     pass class EncoderTimeoutError(EncoderError):     pass @contextlib.asynccontextmanager async def managed_encoder(config: EncoderConfig) -> AsyncGenerator[AsyncEncoder, None]:     encoder = AsyncEncoder(config)     try:         await encoder.initialize()         yield encoder     finally:         await encoder.shutdown() class AsyncEncoder:     def __init__(self, config: EncoderConfig) -> None:         self._config = config         self._session: aiohttp.ClientSession | None = None         self._initialized = False         self._request_count = 0         self._error_count = 0         self._total_latency_ms = 0.0     async def initialize(self) -> None:         connector = aiohttp.TCPConnector(limit=100, limit_per_host=20)         self._session = aiohttp.ClientSession(connector=connector)         self._initialized = True         logger.info('AsyncEncoder initialized: backend=%s', self._config.backend)     async def encode(self, query: str, timeout: float = 5.0) -> np.ndarray:         if not self._initialized:             raise EncoderError('Encoder not initialized')         start = time.perf_counter()         try:             result = await asyncio.wait_for(self._encode_impl(query), timeout=timeout)             self._total_latency_ms += (time.perf_counter() - start) * 1000             self._request_count += 1             return result         except asyncio.TimeoutError as exc:             self._error_count += 1             raise EncoderTimeoutError(f'Encode timed out after {timeout}s') from exc     async def _encode_impl(self, query: str) -> np.ndarray:         raise NotImplementedError     async def shutdown(self) -> None:         if self._session:             await self._session.close()         self._initialized = False     @property     def stats(self) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) }  Furthermore, elf) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) } ",
    "category": "long"
  },
  {
    "id": "long_0053",
    "text": "Architectural Decision Record: Switching from synchronous REST API calls to an event-driven architecture using Apache Kafka for the order processing pipeline. Context: The current synchronous REST-based order processing system suffers from cascading failures when downstream services are slow or unavailable. Peak load of 10,000 orders per minute causes timeouts in the payment service which propagates back to the API gateway causing HTTP 503 errors visible to customers. The average order processing time is 2.3 seconds due to sequential API calls to inventory, payment, notification, and shipping services. Decision: Implement event-driven architecture using Apache Kafka as the message broker. Each service publishes domain events and subscribes to relevant topics. Order creation publishes to order.created topic. Payment service subscribes to order.created and publishes payment.processed or payment.failed. Inventory service subscribes to payment.processed and publishes inventory.reserved or inventory.insufficient. The API immediately returns 202 Accepted with an order ID. Customers poll a read model or receive webhook notifications when order status changes. Consequences: Orders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully. Furthermore, ders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully.",
    "category": "long"
  },
  {
    "id": "long_0054",
    "text": "Analyze the following microservices architecture and identify potential single points of failure, bottlenecks, and security vulnerabilities: The system consists of an API gateway that routes requests to twelve backend services including user-service, auth-service, payment-service, notification-service, email-service, sms-service, recommendation-service, search-service, catalog-service, inventory-service, order-service, and shipping-service. Each service has its own PostgreSQL database with connection pooling configured via PgBouncer. Services communicate asynchronously via RabbitMQ for non-critical paths and synchronously via gRPC for critical paths like payment processing. The API gateway is implemented in Nginx with Lua scripting for rate limiting. Authentication uses JWT tokens with a 15-minute expiry refreshed automatically by the client. The recommendation service uses a pre-trained collaborative filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses. Furthermore, ve filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses.",
    "category": "long"
  },
  {
    "id": "long_0055",
    "text": "Optimize the following Python function that processes a list of 100,000 user records to compute personalized recommendations: def compute_recommendations(users: list[dict], products: list[dict], interactions: list[dict]) -> dict[str, list[str]]:     results = {}     for user in users:         user_interactions = [i for i in interactions                              if i['user_id'] == user['id']]         interacted_products = {i['product_id'] for i in user_interactions}         similar_users = []         for other_user in users:             if other_user['id'] == user['id']: continue             other_interactions = {i['product_id'] for i in interactions                                   if i['user_id'] == other_user['id']}             if len(interacted_products & other_interactions) >= 3:                 similar_users.append(other_user['id'])         recommended = []         for similar_user_id in similar_users[:10]:             similar_interactions = {i['product_id'] for i in interactions                                     if i['user_id'] == similar_user_id}             for prod_id in similar_interactions - interacted_products:                 if prod_id not in recommended:                     recommended.append(prod_id)         results[user['id']] = recommended[:20]     return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine. Furthermore, return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine.",
    "category": "long"
  },
  {
    "id": "long_0056",
    "text": "Implement a complete CI/CD pipeline for a Python machine learning project that includes the following stages: code quality checks using ruff, black, isort, mypy, and bandit; unit tests with pytest and coverage reporting; integration tests against a real PostgreSQL database using testcontainers; model training on a held-out validation set with automatic hyperparameter tuning using Optuna; model evaluation comparing the new model against the currently deployed baseline on a standardized benchmark dataset of 10,000 samples; model registration in MLflow with artifact storage on S3; Docker image build with multi-stage build optimized for production size under 500MB; security scanning using trivy and snyk; deployment to Kubernetes staging environment with smoke tests; automated load testing using k6 to validate p99 latency under 200ms at 1000 concurrent users; canary deployment to production with 5% traffic split; monitoring of key metrics for 30 minutes before full rollout; automatic rollback if error rate exceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials. Furthermore, xceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials.",
    "category": "long"
  },
  {
    "id": "long_0057",
    "text": "import { createServer } from 'http'; import { WebSocketServer, WebSocket } from 'ws'; import { EventEmitter } from 'events'; interface ClientMessage { type: 'subscribe' | 'unsubscribe' | 'publish';   channel: string; payload?: unknown; clientId: string; } interface ServerMessage { type: 'data' | 'error' | 'ack';   channel?: string; payload?: unknown; timestamp: number; } class PubSubServer extends EventEmitter {   private channels = new Map<string, Set<WebSocket>>();   private clientChannels = new Map<WebSocket, Set<string>>();   constructor(private readonly port: number) { super(); }   subscribe(ws: WebSocket, channel: string): void {     if (!this.channels.has(channel)) this.channels.set(channel, new Set());     this.channels.get(channel)!.add(ws);     if (!this.clientChannels.has(ws)) this.clientChannels.set(ws, new Set());     this.clientChannels.get(ws)!.add(channel); }   unsubscribe(ws: WebSocket, channel: string): void {     this.channels.get(channel)?.delete(ws);     this.clientChannels.get(ws)?.delete(channel); }   publish(channel: string, payload: unknown): void {     const msg: ServerMessage = { type: 'data', channel, payload,       timestamp: Date.now() };     const data = JSON.stringify(msg);     this.channels.get(channel)?.forEach(client => {       if (client.readyState === WebSocket.OPEN) client.send(data); }); }   cleanup(ws: WebSocket): void {     this.clientChannels.get(ws)?.forEach(ch => this.unsubscribe(ws, ch));     this.clientChannels.delete(ws); }   listen(): void {     const server = createServer();     const wss = new WebSocketServer({ server });     wss.on('connection', ws => {       ws.on('message', raw => {         const msg: ClientMessage = JSON.parse(raw.toString());         switch (msg.type) {           case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } } Furthermore,         case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } }",
    "category": "long"
  },
  {
    "id": "long_0058",
    "text": "Design and implement a fault-tolerant data pipeline that ingests streaming events from Apache Kafka topics (user.clicked, user.purchased, user.viewed, user.searched) at a rate of 500,000 events per second, applies real-time feature engineering including session reconstruction with a 30-minute session timeout, rolling window aggregations over 1-minute, 5-minute, and 1-hour windows, user-level and item-level statistics, and categorical feature encoding using frequency-adaptive target encoding with Laplace smoothing; writes the enriched feature vectors to a Redis Feature Store with a 24-hour TTL for online serving and to Apache Iceberg tables on S3 for offline training; handles late arriving events up to 5 minutes behind watermark using Apache Flink's event-time processing with allowed lateness; implements exactly-once semantics via Kafka transactions and Flink checkpointing to S3 every 60 seconds; provides a Grafana dashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint. Furthermore, ashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint.",
    "category": "long"
  },
  {
    "id": "long_0059",
    "text": "import { createServer } from 'http'; import { WebSocketServer, WebSocket } from 'ws'; import { EventEmitter } from 'events'; interface ClientMessage { type: 'subscribe' | 'unsubscribe' | 'publish';   channel: string; payload?: unknown; clientId: string; } interface ServerMessage { type: 'data' | 'error' | 'ack';   channel?: string; payload?: unknown; timestamp: number; } class PubSubServer extends EventEmitter {   private channels = new Map<string, Set<WebSocket>>();   private clientChannels = new Map<WebSocket, Set<string>>();   constructor(private readonly port: number) { super(); }   subscribe(ws: WebSocket, channel: string): void {     if (!this.channels.has(channel)) this.channels.set(channel, new Set());     this.channels.get(channel)!.add(ws);     if (!this.clientChannels.has(ws)) this.clientChannels.set(ws, new Set());     this.clientChannels.get(ws)!.add(channel); }   unsubscribe(ws: WebSocket, channel: string): void {     this.channels.get(channel)?.delete(ws);     this.clientChannels.get(ws)?.delete(channel); }   publish(channel: string, payload: unknown): void {     const msg: ServerMessage = { type: 'data', channel, payload,       timestamp: Date.now() };     const data = JSON.stringify(msg);     this.channels.get(channel)?.forEach(client => {       if (client.readyState === WebSocket.OPEN) client.send(data); }); }   cleanup(ws: WebSocket): void {     this.clientChannels.get(ws)?.forEach(ch => this.unsubscribe(ws, ch));     this.clientChannels.delete(ws); }   listen(): void {     const server = createServer();     const wss = new WebSocketServer({ server });     wss.on('connection', ws => {       ws.on('message', raw => {         const msg: ClientMessage = JSON.parse(raw.toString());         switch (msg.type) {           case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } } Furthermore,         case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } }",
    "category": "long"
  },
  {
    "id": "long_0060",
    "text": "Optimize the following Python function that processes a list of 100,000 user records to compute personalized recommendations: def compute_recommendations(users: list[dict], products: list[dict], interactions: list[dict]) -> dict[str, list[str]]:     results = {}     for user in users:         user_interactions = [i for i in interactions                              if i['user_id'] == user['id']]         interacted_products = {i['product_id'] for i in user_interactions}         similar_users = []         for other_user in users:             if other_user['id'] == user['id']: continue             other_interactions = {i['product_id'] for i in interactions                                   if i['user_id'] == other_user['id']}             if len(interacted_products & other_interactions) >= 3:                 similar_users.append(other_user['id'])         recommended = []         for similar_user_id in similar_users[:10]:             similar_interactions = {i['product_id'] for i in interactions                                     if i['user_id'] == similar_user_id}             for prod_id in similar_interactions - interacted_products:                 if prod_id not in recommended:                     recommended.append(prod_id)         results[user['id']] = recommended[:20]     return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine. Furthermore, return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine.",
    "category": "long"
  },
  {
    "id": "long_0061",
    "text": "use std::collections::HashMap; use std::sync::{Arc, RwLock}; use std::time::{Duration, Instant}; #[derive(Debug, Clone)] pub struct CacheEntry<V> { value: V, inserted_at: Instant, ttl: Duration, } impl<V> CacheEntry<V> {     pub fn is_expired(&self) -> bool {         self.inserted_at.elapsed() >= self.ttl } } #[derive(Debug)] pub struct TtlCache<K, V> {     inner: Arc<RwLock<HashMap<K, CacheEntry<V>>>>,     default_ttl: Duration, } impl<K, V> TtlCache<K, V> where K: Eq + std::hash::Hash + Clone, V: Clone, {     pub fn new(default_ttl: Duration) -> Self {         Self { inner: Arc::new(RwLock::new(HashMap::new())), default_ttl } }     pub fn insert(&self, key: K, value: V) {         let entry = CacheEntry { value, inserted_at: Instant::now(),             ttl: self.default_ttl };         self.inner.write().unwrap().insert(key, entry); }     pub fn get(&self, key: &K) -> Option<V> {         let guard = self.inner.read().unwrap();         guard.get(key).filter(|e| !e.is_expired()).map(|e| e.value.clone()) }     pub fn evict_expired(&self) -> usize {         let mut guard = self.inner.write().unwrap();         let before = guard.len();         guard.retain(|_, e| !e.is_expired());         before - guard.len() }     pub fn len(&self) -> usize { self.inner.read().unwrap().len() }     pub fn is_empty(&self) -> bool { self.len() == 0 } } Furthermore,  -> usize {         let mut guard = self.inner.write().unwrap();         let before = guard.len();         guard.retain(|_, e| !e.is_expired());         before - guard.len() }     pub fn len(&self) -> usize { self.inner.read().unwrap().len() }     pub fn is_empty(&self) -> bool { self.len() == 0 } }",
    "category": "long"
  },
  {
    "id": "long_0062",
    "text": "from __future__ import annotations import asyncio import contextlib import dataclasses import hashlib import json import logging import os import time from collections.abc import AsyncGenerator, Generator from pathlib import Path from typing import Any, ClassVar import aiohttp import numpy as np logger = logging.getLogger(__name__) @dataclasses.dataclass(frozen=True, slots=True) class EncoderConfig:     model_hash: str     tokenizer_hash: str     hidden_dim: int = 384     num_heads: int = 12     num_layers: int = 12     max_seq_len: int = 256     window_size: int = 256     stride: int = 192     top_k: int = 5     backend: str = 'native'     runtime_dir: Path = dataclasses.field(         default_factory=lambda: Path.home() / '.local/share/mind-nerve/runtime')     SCHEMA_VERSION: ClassVar[int] = 1     def to_cache_key(self) -> str:         payload = json.dumps(dataclasses.asdict(self), sort_keys=True, default=str)         return hashlib.sha256(payload.encode()).hexdigest() class EncoderError(Exception):     pass class BackendNotAvailableError(EncoderError):     pass class EncoderTimeoutError(EncoderError):     pass @contextlib.asynccontextmanager async def managed_encoder(config: EncoderConfig) -> AsyncGenerator[AsyncEncoder, None]:     encoder = AsyncEncoder(config)     try:         await encoder.initialize()         yield encoder     finally:         await encoder.shutdown() class AsyncEncoder:     def __init__(self, config: EncoderConfig) -> None:         self._config = config         self._session: aiohttp.ClientSession | None = None         self._initialized = False         self._request_count = 0         self._error_count = 0         self._total_latency_ms = 0.0     async def initialize(self) -> None:         connector = aiohttp.TCPConnector(limit=100, limit_per_host=20)         self._session = aiohttp.ClientSession(connector=connector)         self._initialized = True         logger.info('AsyncEncoder initialized: backend=%s', self._config.backend)     async def encode(self, query: str, timeout: float = 5.0) -> np.ndarray:         if not self._initialized:             raise EncoderError('Encoder not initialized')         start = time.perf_counter()         try:             result = await asyncio.wait_for(self._encode_impl(query), timeout=timeout)             self._total_latency_ms += (time.perf_counter() - start) * 1000             self._request_count += 1             return result         except asyncio.TimeoutError as exc:             self._error_count += 1             raise EncoderTimeoutError(f'Encode timed out after {timeout}s') from exc     async def _encode_impl(self, query: str) -> np.ndarray:         raise NotImplementedError     async def shutdown(self) -> None:         if self._session:             await self._session.close()         self._initialized = False     @property     def stats(self) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) }  Furthermore, elf) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) } ",
    "category": "long"
  },
  {
    "id": "long_0063",
    "text": "Implement a complete CI/CD pipeline for a Python machine learning project that includes the following stages: code quality checks using ruff, black, isort, mypy, and bandit; unit tests with pytest and coverage reporting; integration tests against a real PostgreSQL database using testcontainers; model training on a held-out validation set with automatic hyperparameter tuning using Optuna; model evaluation comparing the new model against the currently deployed baseline on a standardized benchmark dataset of 10,000 samples; model registration in MLflow with artifact storage on S3; Docker image build with multi-stage build optimized for production size under 500MB; security scanning using trivy and snyk; deployment to Kubernetes staging environment with smoke tests; automated load testing using k6 to validate p99 latency under 200ms at 1000 concurrent users; canary deployment to production with 5% traffic split; monitoring of key metrics for 30 minutes before full rollout; automatic rollback if error rate exceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials. Furthermore, xceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials.",
    "category": "long"
  },
  {
    "id": "long_0064",
    "text": "Write a PostgreSQL query to find all users who have placed more than 5 orders in the last 30 days, where each order total exceeds $100, and who have not been flagged for fraud, grouped by user_id with aggregated statistics including total order count, total spend, average order value, first order date, last order date, and the names of the top 3 most frequently ordered products for each user, sorted by total spend descending, with pagination support using cursor-based pagination on user_id for efficient handling of large result sets. The query should use CTEs for readability, include appropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring. Furthermore, ppropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring.",
    "category": "long"
  },
  {
    "id": "long_0065",
    "text": "Compare and contrast the following approaches to distributed consensus and explain when to use each one in the context of a globally distributed database system: Paxos (Classic Paxos, Multi-Paxos, and Fast Paxos variants), Raft (with and without pre-vote extension), Zab (ZooKeeper Atomic Broadcast), PBFT (Practical Byzantine Fault Tolerance), HotStuff (and its chained variant used in LibraBFT/DiemBFT), Tendermint/CometBFT, and Stellar Consensus Protocol. For each algorithm, discuss: the fault model (crash fault tolerant vs byzantine fault tolerant), the minimum number of nodes required for fault tolerance (f+1 vs 2f+1 vs 3f+1), the leader election mechanism and its impact on availability during leader failures, the message complexity per consensus round, the latency in terms of network round trips, the throughput in transactions per second for typical LAN and WAN deployments, the behavior under network partitions (CAP theorem position), the implementation complexity and available production-grade open-source implementations, and any known correctness bugs or safety violations discovered in production deployments. Then recommend the appropriate algorithm for a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants. Furthermore, or a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants.",
    "category": "long"
  },
  {
    "id": "long_0066",
    "text": "from __future__ import annotations import asyncio import contextlib import dataclasses import hashlib import json import logging import os import time from collections.abc import AsyncGenerator, Generator from pathlib import Path from typing import Any, ClassVar import aiohttp import numpy as np logger = logging.getLogger(__name__) @dataclasses.dataclass(frozen=True, slots=True) class EncoderConfig:     model_hash: str     tokenizer_hash: str     hidden_dim: int = 384     num_heads: int = 12     num_layers: int = 12     max_seq_len: int = 256     window_size: int = 256     stride: int = 192     top_k: int = 5     backend: str = 'native'     runtime_dir: Path = dataclasses.field(         default_factory=lambda: Path.home() / '.local/share/mind-nerve/runtime')     SCHEMA_VERSION: ClassVar[int] = 1     def to_cache_key(self) -> str:         payload = json.dumps(dataclasses.asdict(self), sort_keys=True, default=str)         return hashlib.sha256(payload.encode()).hexdigest() class EncoderError(Exception):     pass class BackendNotAvailableError(EncoderError):     pass class EncoderTimeoutError(EncoderError):     pass @contextlib.asynccontextmanager async def managed_encoder(config: EncoderConfig) -> AsyncGenerator[AsyncEncoder, None]:     encoder = AsyncEncoder(config)     try:         await encoder.initialize()         yield encoder     finally:         await encoder.shutdown() class AsyncEncoder:     def __init__(self, config: EncoderConfig) -> None:         self._config = config         self._session: aiohttp.ClientSession | None = None         self._initialized = False         self._request_count = 0         self._error_count = 0         self._total_latency_ms = 0.0     async def initialize(self) -> None:         connector = aiohttp.TCPConnector(limit=100, limit_per_host=20)         self._session = aiohttp.ClientSession(connector=connector)         self._initialized = True         logger.info('AsyncEncoder initialized: backend=%s', self._config.backend)     async def encode(self, query: str, timeout: float = 5.0) -> np.ndarray:         if not self._initialized:             raise EncoderError('Encoder not initialized')         start = time.perf_counter()         try:             result = await asyncio.wait_for(self._encode_impl(query), timeout=timeout)             self._total_latency_ms += (time.perf_counter() - start) * 1000             self._request_count += 1             return result         except asyncio.TimeoutError as exc:             self._error_count += 1             raise EncoderTimeoutError(f'Encode timed out after {timeout}s') from exc     async def _encode_impl(self, query: str) -> np.ndarray:         raise NotImplementedError     async def shutdown(self) -> None:         if self._session:             await self._session.close()         self._initialized = False     @property     def stats(self) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) }  Furthermore, elf) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) } ",
    "category": "long"
  },
  {
    "id": "long_0067",
    "text": "Compare and contrast the following approaches to distributed consensus and explain when to use each one in the context of a globally distributed database system: Paxos (Classic Paxos, Multi-Paxos, and Fast Paxos variants), Raft (with and without pre-vote extension), Zab (ZooKeeper Atomic Broadcast), PBFT (Practical Byzantine Fault Tolerance), HotStuff (and its chained variant used in LibraBFT/DiemBFT), Tendermint/CometBFT, and Stellar Consensus Protocol. For each algorithm, discuss: the fault model (crash fault tolerant vs byzantine fault tolerant), the minimum number of nodes required for fault tolerance (f+1 vs 2f+1 vs 3f+1), the leader election mechanism and its impact on availability during leader failures, the message complexity per consensus round, the latency in terms of network round trips, the throughput in transactions per second for typical LAN and WAN deployments, the behavior under network partitions (CAP theorem position), the implementation complexity and available production-grade open-source implementations, and any known correctness bugs or safety violations discovered in production deployments. Then recommend the appropriate algorithm for a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants. Furthermore, or a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants.",
    "category": "long"
  },
  {
    "id": "long_0068",
    "text": "Compare and contrast the following approaches to distributed consensus and explain when to use each one in the context of a globally distributed database system: Paxos (Classic Paxos, Multi-Paxos, and Fast Paxos variants), Raft (with and without pre-vote extension), Zab (ZooKeeper Atomic Broadcast), PBFT (Practical Byzantine Fault Tolerance), HotStuff (and its chained variant used in LibraBFT/DiemBFT), Tendermint/CometBFT, and Stellar Consensus Protocol. For each algorithm, discuss: the fault model (crash fault tolerant vs byzantine fault tolerant), the minimum number of nodes required for fault tolerance (f+1 vs 2f+1 vs 3f+1), the leader election mechanism and its impact on availability during leader failures, the message complexity per consensus round, the latency in terms of network round trips, the throughput in transactions per second for typical LAN and WAN deployments, the behavior under network partitions (CAP theorem position), the implementation complexity and available production-grade open-source implementations, and any known correctness bugs or safety violations discovered in production deployments. Then recommend the appropriate algorithm for a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants. Furthermore, or a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants.",
    "category": "long"
  },
  {
    "id": "long_0069",
    "text": "Architectural Decision Record: Switching from synchronous REST API calls to an event-driven architecture using Apache Kafka for the order processing pipeline. Context: The current synchronous REST-based order processing system suffers from cascading failures when downstream services are slow or unavailable. Peak load of 10,000 orders per minute causes timeouts in the payment service which propagates back to the API gateway causing HTTP 503 errors visible to customers. The average order processing time is 2.3 seconds due to sequential API calls to inventory, payment, notification, and shipping services. Decision: Implement event-driven architecture using Apache Kafka as the message broker. Each service publishes domain events and subscribes to relevant topics. Order creation publishes to order.created topic. Payment service subscribes to order.created and publishes payment.processed or payment.failed. Inventory service subscribes to payment.processed and publishes inventory.reserved or inventory.insufficient. The API immediately returns 202 Accepted with an order ID. Customers poll a read model or receive webhook notifications when order status changes. Consequences: Orders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully. Furthermore, ders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully.",
    "category": "long"
  },
  {
    "id": "long_0070",
    "text": "Architectural Decision Record: Switching from synchronous REST API calls to an event-driven architecture using Apache Kafka for the order processing pipeline. Context: The current synchronous REST-based order processing system suffers from cascading failures when downstream services are slow or unavailable. Peak load of 10,000 orders per minute causes timeouts in the payment service which propagates back to the API gateway causing HTTP 503 errors visible to customers. The average order processing time is 2.3 seconds due to sequential API calls to inventory, payment, notification, and shipping services. Decision: Implement event-driven architecture using Apache Kafka as the message broker. Each service publishes domain events and subscribes to relevant topics. Order creation publishes to order.created topic. Payment service subscribes to order.created and publishes payment.processed or payment.failed. Inventory service subscribes to payment.processed and publishes inventory.reserved or inventory.insufficient. The API immediately returns 202 Accepted with an order ID. Customers poll a read model or receive webhook notifications when order status changes. Consequences: Orders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully. Furthermore, ders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully.",
    "category": "long"
  },
  {
    "id": "long_0071",
    "text": "Create a Terraform configuration for a highly available three-tier web application on AWS consisting of: a public-facing Application Load Balancer with WAF integration and DDoS protection via AWS Shield Standard; an Auto Scaling Group of EC2 t3.medium instances running in private subnets across three availability zones with a minimum of 3 and maximum of 20 instances; an RDS PostgreSQL 16 Multi-AZ cluster with read replicas in each availability zone, automated backups retained for 30 days, and encryption at rest using AWS KMS; ElastiCache Redis 7 cluster mode enabled with 3 shards and 2 replicas per shard for session storage and caching; an S3 bucket with versioning, cross-region replication to us-west-2, and lifecycle policies to transition objects to Glacier after 90 days; CloudFront distribution fronting the ALB with custom SSL certificate from ACM, security headers policy, and edge caching for static assets; VPC with CIDR 10.0.0.0/16 split into 6 subnets (3 public /24, 3 private /24) with NAT gateways in each AZ; Security Groups following principle of least privilege with separate groups for ALB, web tier, app tier, and database tier. Furthermore, rom ACM, security headers policy, and edge caching for static assets; VPC with CIDR 10.0.0.0/16 split into 6 subnets (3 public /24, 3 private /24) with NAT gateways in each AZ; Security Groups following principle of least privilege with separate groups for ALB, web tier, app tier, and database tier.",
    "category": "long"
  },
  {
    "id": "long_0072",
    "text": "Architectural Decision Record: Switching from synchronous REST API calls to an event-driven architecture using Apache Kafka for the order processing pipeline. Context: The current synchronous REST-based order processing system suffers from cascading failures when downstream services are slow or unavailable. Peak load of 10,000 orders per minute causes timeouts in the payment service which propagates back to the API gateway causing HTTP 503 errors visible to customers. The average order processing time is 2.3 seconds due to sequential API calls to inventory, payment, notification, and shipping services. Decision: Implement event-driven architecture using Apache Kafka as the message broker. Each service publishes domain events and subscribes to relevant topics. Order creation publishes to order.created topic. Payment service subscribes to order.created and publishes payment.processed or payment.failed. Inventory service subscribes to payment.processed and publishes inventory.reserved or inventory.insufficient. The API immediately returns 202 Accepted with an order ID. Customers poll a read model or receive webhook notifications when order status changes. Consequences: Orders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully. Furthermore, ders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully.",
    "category": "long"
  },
  {
    "id": "long_0073",
    "text": "Compare and contrast the following approaches to distributed consensus and explain when to use each one in the context of a globally distributed database system: Paxos (Classic Paxos, Multi-Paxos, and Fast Paxos variants), Raft (with and without pre-vote extension), Zab (ZooKeeper Atomic Broadcast), PBFT (Practical Byzantine Fault Tolerance), HotStuff (and its chained variant used in LibraBFT/DiemBFT), Tendermint/CometBFT, and Stellar Consensus Protocol. For each algorithm, discuss: the fault model (crash fault tolerant vs byzantine fault tolerant), the minimum number of nodes required for fault tolerance (f+1 vs 2f+1 vs 3f+1), the leader election mechanism and its impact on availability during leader failures, the message complexity per consensus round, the latency in terms of network round trips, the throughput in transactions per second for typical LAN and WAN deployments, the behavior under network partitions (CAP theorem position), the implementation complexity and available production-grade open-source implementations, and any known correctness bugs or safety violations discovered in production deployments. Then recommend the appropriate algorithm for a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants. Furthermore, or a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants.",
    "category": "long"
  },
  {
    "id": "long_0074",
    "text": "use std::collections::HashMap; use std::sync::{Arc, RwLock}; use std::time::{Duration, Instant}; #[derive(Debug, Clone)] pub struct CacheEntry<V> { value: V, inserted_at: Instant, ttl: Duration, } impl<V> CacheEntry<V> {     pub fn is_expired(&self) -> bool {         self.inserted_at.elapsed() >= self.ttl } } #[derive(Debug)] pub struct TtlCache<K, V> {     inner: Arc<RwLock<HashMap<K, CacheEntry<V>>>>,     default_ttl: Duration, } impl<K, V> TtlCache<K, V> where K: Eq + std::hash::Hash + Clone, V: Clone, {     pub fn new(default_ttl: Duration) -> Self {         Self { inner: Arc::new(RwLock::new(HashMap::new())), default_ttl } }     pub fn insert(&self, key: K, value: V) {         let entry = CacheEntry { value, inserted_at: Instant::now(),             ttl: self.default_ttl };         self.inner.write().unwrap().insert(key, entry); }     pub fn get(&self, key: &K) -> Option<V> {         let guard = self.inner.read().unwrap();         guard.get(key).filter(|e| !e.is_expired()).map(|e| e.value.clone()) }     pub fn evict_expired(&self) -> usize {         let mut guard = self.inner.write().unwrap();         let before = guard.len();         guard.retain(|_, e| !e.is_expired());         before - guard.len() }     pub fn len(&self) -> usize { self.inner.read().unwrap().len() }     pub fn is_empty(&self) -> bool { self.len() == 0 } } Furthermore,  -> usize {         let mut guard = self.inner.write().unwrap();         let before = guard.len();         guard.retain(|_, e| !e.is_expired());         before - guard.len() }     pub fn len(&self) -> usize { self.inner.read().unwrap().len() }     pub fn is_empty(&self) -> bool { self.len() == 0 } }",
    "category": "long"
  },
  {
    "id": "long_0075",
    "text": "class DistributedEmbeddingCache: \"\"\"Thread-safe distributed embedding cache backed by Redis with LRU eviction, TTL support, and automatic serialization of numpy float32 arrays to Q16.16 fixed-point representation. The cache key is SHA-256(model_hash + tokenizer_hash + query_text). Cache entries expire after ttl_seconds (default 3600). The cache uses a two-level hierarchy: L1 is an in-process LRU (maxsize=1024) and L2 is the shared Redis cluster. Write-back from L1 to L2 is asynchronous via a background thread. The serialize method converts float32[384] to Q16.16 i32[384] bytes with deterministic rounding for cross-arch reproducibility.\"\"\" def __init__(self, redis_url: str, ttl_seconds: int = 3600,              l1_maxsize: int = 1024) -> None:     self._redis = redis.Redis.from_url(redis_url)     self._ttl = ttl_seconds     self._l1 = functools.lru_cache(maxsize=l1_maxsize)(self._get_l2)     self._write_queue: queue.Queue = queue.Queue(maxsize=4096)     self._writer = threading.Thread(target=self._write_worker, daemon=True)     self._writer.start() def _cache_key(self, model_hash: str, tokenizer_hash: str, query: str) -> str:     payload = f'{model_hash}:{tokenizer_hash}:{query}'.encode('utf-8')     return 'emb:' + hashlib.sha256(payload).hexdigest() def get(self, model_hash: str, tokenizer_hash: str, query: str) -> np.ndarray | None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._redis.get(key)     if raw is None:         return None     return self._deserialize(raw) def put(self, model_hash: str, tokenizer_hash: str, query: str,         embedding: np.ndarray) -> None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._serialize(embedding)     self._redis.setex(key, self._ttl, raw) def _serialize(self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0) Furthermore, (self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0)",
    "category": "long"
  },
  {
    "id": "long_0076",
    "text": "apiVersion: apps/v1 kind: Deployment metadata:   name: mind-nerve-encoder   namespace: production   labels:     app: mind-nerve     component: encoder     version: v0.4.0 spec:   replicas: 3   selector:     matchLabels:       app: mind-nerve       component: encoder   strategy:     type: RollingUpdate     rollingUpdate:       maxSurge: 1       maxUnavailable: 0   template:     metadata:       labels:         app: mind-nerve         component: encoder         version: v0.4.0       annotations:         prometheus.io/scrape: 'true'         prometheus.io/port: '9090'         prometheus.io/path: '/metrics'     spec:       securityContext:         runAsNonRoot: true         runAsUser: 1000         fsGroup: 1000       containers:       - name: encoder         image: ghcr.io/star-ga/mind-nerve:v0.4.0         ports:         - containerPort: 8080         - containerPort: 9090         resources:           requests:             cpu: 500m             memory: 512Mi           limits:             cpu: 2000m             memory: 2Gi         readinessProbe:           httpGet:             path: /health/ready             port: 8080           initialDelaySeconds: 10           periodSeconds: 5         livenessProbe:           httpGet:             path: /health/live             port: 8080           initialDelaySeconds: 30           periodSeconds: 10         env:         - name: MIND_NERVE_BACKEND           value: native         - name: MIND_NERVE_RUNTIME_DIR           value: /runtime         volumeMounts:         - name: runtime           mountPath: /runtime           readOnly: true       volumes:       - name: runtime         configMap:           name: mind-nerve-runtime Furthermore,  MIND_NERVE_BACKEND           value: native         - name: MIND_NERVE_RUNTIME_DIR           value: /runtime         volumeMounts:         - name: runtime           mountPath: /runtime           readOnly: true       volumes:       - name: runtime         configMap:           name: mind-nerve-runtime",
    "category": "long"
  },
  {
    "id": "long_0077",
    "text": "Conduct a comprehensive security review of the following authentication implementation and identify all vulnerabilities with CVSS scores: The system uses JWT tokens with HS256 signing using a hardcoded secret key 'super_secret_key_12345' stored directly in the application code. Tokens have a 30-day expiry with no refresh mechanism. The token payload includes the user_id, role, email, and a boolean is_admin flag that is trusted by all downstream services without verification. Password hashing uses MD5 with a 4-character salt. The login endpoint does not implement rate limiting. Session tokens are stored in localStorage instead of httpOnly cookies. The password reset flow sends a 6-digit numeric OTP via email with a 24-hour expiry and no attempt limiting. The API does not validate Content-Type headers on POST requests. SQL queries for user lookup use string concatenation without parameterization. Cross-origin requests are allowed from all origins via Access-Control-Allow-Origin: *. The application runs as root in production containers. Debug endpoints exposing stack traces and environment variables are accessible without authentication in production. Please provide remediation recommendations for each vulnerability ordered by severity. Furthermore, igins via Access-Control-Allow-Origin: *. The application runs as root in production containers. Debug endpoints exposing stack traces and environment variables are accessible without authentication in production. Please provide remediation recommendations for each vulnerability ordered by severity.",
    "category": "long"
  },
  {
    "id": "long_0078",
    "text": "Optimize the following Python function that processes a list of 100,000 user records to compute personalized recommendations: def compute_recommendations(users: list[dict], products: list[dict], interactions: list[dict]) -> dict[str, list[str]]:     results = {}     for user in users:         user_interactions = [i for i in interactions                              if i['user_id'] == user['id']]         interacted_products = {i['product_id'] for i in user_interactions}         similar_users = []         for other_user in users:             if other_user['id'] == user['id']: continue             other_interactions = {i['product_id'] for i in interactions                                   if i['user_id'] == other_user['id']}             if len(interacted_products & other_interactions) >= 3:                 similar_users.append(other_user['id'])         recommended = []         for similar_user_id in similar_users[:10]:             similar_interactions = {i['product_id'] for i in interactions                                     if i['user_id'] == similar_user_id}             for prod_id in similar_interactions - interacted_products:                 if prod_id not in recommended:                     recommended.append(prod_id)         results[user['id']] = recommended[:20]     return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine. Furthermore, return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine.",
    "category": "long"
  },
  {
    "id": "long_0079",
    "text": "Optimize the following Python function that processes a list of 100,000 user records to compute personalized recommendations: def compute_recommendations(users: list[dict], products: list[dict], interactions: list[dict]) -> dict[str, list[str]]:     results = {}     for user in users:         user_interactions = [i for i in interactions                              if i['user_id'] == user['id']]         interacted_products = {i['product_id'] for i in user_interactions}         similar_users = []         for other_user in users:             if other_user['id'] == user['id']: continue             other_interactions = {i['product_id'] for i in interactions                                   if i['user_id'] == other_user['id']}             if len(interacted_products & other_interactions) >= 3:                 similar_users.append(other_user['id'])         recommended = []         for similar_user_id in similar_users[:10]:             similar_interactions = {i['product_id'] for i in interactions                                     if i['user_id'] == similar_user_id}             for prod_id in similar_interactions - interacted_products:                 if prod_id not in recommended:                     recommended.append(prod_id)         results[user['id']] = recommended[:20]     return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine. Furthermore, return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine.",
    "category": "long"
  },
  {
    "id": "long_0080",
    "text": "openapi: 3.1.0 info:   title: mind-nerve Encoding API   version: 0.4.0   description: |     Q16.16 native BERT encoder for intent classification and tool routing.     Encodes query strings into 384-dimensional L2-normalized embeddings     and returns top-K routing candidates from a precomputed catalog.     The encode endpoint accepts a JSON body with a query string and     optional parameters for top_k, backend selection, and runtime override.     All embeddings are returned as Q16.16 fixed-point integers to ensure     bit-identical results across x86_64 CPU backends. CUDA and ARM backends     are available but require explicit opt-in via the backend parameter.     Rate limiting is enforced at 1000 requests per minute per API key.     The catalog endpoint returns metadata about the currently loaded     routing catalog including version, size, and hash for cache validation. paths:   /v1/encode:     post:       operationId: encodeQuery       summary: Encode a query string and return top-K routing candidates       requestBody:         required: true         content:           application/json:             schema:               type: object               required: [query]               properties:                 query:                   type: string                   minLength: 1                   maxLength: 2048                   description: The query string to encode and route                 top_k:                   type: integer                   minimum: 1                   maximum: 100                   default: 5                 backend:                   type: string                   enum: [native, pytorch, cuda]                   default: native       responses:         '200':           description: Successful encoding and routing           content:             application/json:               schema:                 $ref: '#/components/schemas/RouteResult'         '400':           description: Invalid request (query too long, invalid backend)         '429':           description: Rate limit exceeded         '503':           description: Backend unavailable Furthermore, application/json:               schema:                 $ref: '#/components/schemas/RouteResult'         '400':           description: Invalid request (query too long, invalid backend)         '429':           description: Rate limit exceeded         '503':           description: Backend unavailable",
    "category": "long"
  },
  {
    "id": "long_0081",
    "text": "Design and implement a fault-tolerant data pipeline that ingests streaming events from Apache Kafka topics (user.clicked, user.purchased, user.viewed, user.searched) at a rate of 500,000 events per second, applies real-time feature engineering including session reconstruction with a 30-minute session timeout, rolling window aggregations over 1-minute, 5-minute, and 1-hour windows, user-level and item-level statistics, and categorical feature encoding using frequency-adaptive target encoding with Laplace smoothing; writes the enriched feature vectors to a Redis Feature Store with a 24-hour TTL for online serving and to Apache Iceberg tables on S3 for offline training; handles late arriving events up to 5 minutes behind watermark using Apache Flink's event-time processing with allowed lateness; implements exactly-once semantics via Kafka transactions and Flink checkpointing to S3 every 60 seconds; provides a Grafana dashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint. Furthermore, ashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint.",
    "category": "long"
  },
  {
    "id": "long_0082",
    "text": "Optimize the following Python function that processes a list of 100,000 user records to compute personalized recommendations: def compute_recommendations(users: list[dict], products: list[dict], interactions: list[dict]) -> dict[str, list[str]]:     results = {}     for user in users:         user_interactions = [i for i in interactions                              if i['user_id'] == user['id']]         interacted_products = {i['product_id'] for i in user_interactions}         similar_users = []         for other_user in users:             if other_user['id'] == user['id']: continue             other_interactions = {i['product_id'] for i in interactions                                   if i['user_id'] == other_user['id']}             if len(interacted_products & other_interactions) >= 3:                 similar_users.append(other_user['id'])         recommended = []         for similar_user_id in similar_users[:10]:             similar_interactions = {i['product_id'] for i in interactions                                     if i['user_id'] == similar_user_id}             for prod_id in similar_interactions - interacted_products:                 if prod_id not in recommended:                     recommended.append(prod_id)         results[user['id']] = recommended[:20]     return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine. Furthermore, return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine.",
    "category": "long"
  },
  {
    "id": "long_0083",
    "text": "Compare and contrast the following approaches to distributed consensus and explain when to use each one in the context of a globally distributed database system: Paxos (Classic Paxos, Multi-Paxos, and Fast Paxos variants), Raft (with and without pre-vote extension), Zab (ZooKeeper Atomic Broadcast), PBFT (Practical Byzantine Fault Tolerance), HotStuff (and its chained variant used in LibraBFT/DiemBFT), Tendermint/CometBFT, and Stellar Consensus Protocol. For each algorithm, discuss: the fault model (crash fault tolerant vs byzantine fault tolerant), the minimum number of nodes required for fault tolerance (f+1 vs 2f+1 vs 3f+1), the leader election mechanism and its impact on availability during leader failures, the message complexity per consensus round, the latency in terms of network round trips, the throughput in transactions per second for typical LAN and WAN deployments, the behavior under network partitions (CAP theorem position), the implementation complexity and available production-grade open-source implementations, and any known correctness bugs or safety violations discovered in production deployments. Then recommend the appropriate algorithm for a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants. Furthermore, or a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants.",
    "category": "long"
  },
  {
    "id": "long_0084",
    "text": "Analyze the following microservices architecture and identify potential single points of failure, bottlenecks, and security vulnerabilities: The system consists of an API gateway that routes requests to twelve backend services including user-service, auth-service, payment-service, notification-service, email-service, sms-service, recommendation-service, search-service, catalog-service, inventory-service, order-service, and shipping-service. Each service has its own PostgreSQL database with connection pooling configured via PgBouncer. Services communicate asynchronously via RabbitMQ for non-critical paths and synchronously via gRPC for critical paths like payment processing. The API gateway is implemented in Nginx with Lua scripting for rate limiting. Authentication uses JWT tokens with a 15-minute expiry refreshed automatically by the client. The recommendation service uses a pre-trained collaborative filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses. Furthermore, ve filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses.",
    "category": "long"
  },
  {
    "id": "long_0085",
    "text": "Design and implement a fault-tolerant data pipeline that ingests streaming events from Apache Kafka topics (user.clicked, user.purchased, user.viewed, user.searched) at a rate of 500,000 events per second, applies real-time feature engineering including session reconstruction with a 30-minute session timeout, rolling window aggregations over 1-minute, 5-minute, and 1-hour windows, user-level and item-level statistics, and categorical feature encoding using frequency-adaptive target encoding with Laplace smoothing; writes the enriched feature vectors to a Redis Feature Store with a 24-hour TTL for online serving and to Apache Iceberg tables on S3 for offline training; handles late arriving events up to 5 minutes behind watermark using Apache Flink's event-time processing with allowed lateness; implements exactly-once semantics via Kafka transactions and Flink checkpointing to S3 every 60 seconds; provides a Grafana dashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint. Furthermore, ashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint.",
    "category": "long"
  },
  {
    "id": "long_0086",
    "text": "Implement a complete CI/CD pipeline for a Python machine learning project that includes the following stages: code quality checks using ruff, black, isort, mypy, and bandit; unit tests with pytest and coverage reporting; integration tests against a real PostgreSQL database using testcontainers; model training on a held-out validation set with automatic hyperparameter tuning using Optuna; model evaluation comparing the new model against the currently deployed baseline on a standardized benchmark dataset of 10,000 samples; model registration in MLflow with artifact storage on S3; Docker image build with multi-stage build optimized for production size under 500MB; security scanning using trivy and snyk; deployment to Kubernetes staging environment with smoke tests; automated load testing using k6 to validate p99 latency under 200ms at 1000 concurrent users; canary deployment to production with 5% traffic split; monitoring of key metrics for 30 minutes before full rollout; automatic rollback if error rate exceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials. Furthermore, xceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials.",
    "category": "long"
  },
  {
    "id": "long_0087",
    "text": "Write a PostgreSQL query to find all users who have placed more than 5 orders in the last 30 days, where each order total exceeds $100, and who have not been flagged for fraud, grouped by user_id with aggregated statistics including total order count, total spend, average order value, first order date, last order date, and the names of the top 3 most frequently ordered products for each user, sorted by total spend descending, with pagination support using cursor-based pagination on user_id for efficient handling of large result sets. The query should use CTEs for readability, include appropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring. Furthermore, ppropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring.",
    "category": "long"
  },
  {
    "id": "long_0088",
    "text": "package encoder import (     'context'     'crypto/sha256'     'encoding/binary'     'fmt'     'math'     'sync' ) const (     HiddenDim   = 384     NumHeads    = 12     HeadDim     = 32     Q16Scale    = 65536.0     LayerNormEps = 1e-12 ) type Q16 int32 func Float32ToQ16(f float32) Q16 {     scaled := float64(f) * Q16Scale     if scaled > math.MaxInt32 { return Q16(math.MaxInt32) }     if scaled < math.MinInt32 { return Q16(math.MinInt32) }     return Q16(math.Round(scaled)) } func (q Q16) ToFloat32() float32 { return float32(float64(q) / Q16Scale) } type Matrix struct { Data []Q16; Rows, Cols int } func NewMatrix(rows, cols int) *Matrix {     return &Matrix{Data: make([]Q16, rows*cols), Rows: rows, Cols: cols} } func (m *Matrix) At(row, col int) Q16 { return m.Data[row*m.Cols+col] } func (m *Matrix) Set(row, col int, v Q16) { m.Data[row*m.Cols+col] = v } func Matmul(a, b *Matrix) *Matrix {     if a.Cols != b.Rows { panic('dimension mismatch') }     c := NewMatrix(a.Rows, b.Cols)     var wg sync.WaitGroup     for i := 0; i < a.Rows; i++ {         wg.Add(1)         go func(row int) {             defer wg.Done()             for j := 0; j < b.Cols; j++ {                 var acc int64                 for k := 0; k < a.Cols; k++ {                     acc += int64(a.At(row, k)) * int64(b.At(k, j)) }                 c.Set(row, j, Q16(acc>>16)) } }(i) }     wg.Wait()     return c } func LayerNorm(x *Matrix, gamma, beta []Q16) *Matrix {     out := NewMatrix(x.Rows, x.Cols)     for i := 0; i < x.Rows; i++ {         var sum int64         for j := 0; j < x.Cols; j++ { sum += int64(x.At(i, j)) }         mean := Q16(sum / int64(x.Cols))         var varAcc int64         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             varAcc += d * d }         variance := float64(varAcc) / float64(x.Cols) / Q16Scale / Q16Scale         invStd := 1.0 / math.Sqrt(variance + LayerNormEps)         invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out }  Furthermore,     invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out } ",
    "category": "long"
  },
  {
    "id": "long_0089",
    "text": "Write a PostgreSQL query to find all users who have placed more than 5 orders in the last 30 days, where each order total exceeds $100, and who have not been flagged for fraud, grouped by user_id with aggregated statistics including total order count, total spend, average order value, first order date, last order date, and the names of the top 3 most frequently ordered products for each user, sorted by total spend descending, with pagination support using cursor-based pagination on user_id for efficient handling of large result sets. The query should use CTEs for readability, include appropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring. Furthermore, ppropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring.",
    "category": "long"
  },
  {
    "id": "long_0090",
    "text": "Create a Terraform configuration for a highly available three-tier web application on AWS consisting of: a public-facing Application Load Balancer with WAF integration and DDoS protection via AWS Shield Standard; an Auto Scaling Group of EC2 t3.medium instances running in private subnets across three availability zones with a minimum of 3 and maximum of 20 instances; an RDS PostgreSQL 16 Multi-AZ cluster with read replicas in each availability zone, automated backups retained for 30 days, and encryption at rest using AWS KMS; ElastiCache Redis 7 cluster mode enabled with 3 shards and 2 replicas per shard for session storage and caching; an S3 bucket with versioning, cross-region replication to us-west-2, and lifecycle policies to transition objects to Glacier after 90 days; CloudFront distribution fronting the ALB with custom SSL certificate from ACM, security headers policy, and edge caching for static assets; VPC with CIDR 10.0.0.0/16 split into 6 subnets (3 public /24, 3 private /24) with NAT gateways in each AZ; Security Groups following principle of least privilege with separate groups for ALB, web tier, app tier, and database tier. Furthermore, rom ACM, security headers policy, and edge caching for static assets; VPC with CIDR 10.0.0.0/16 split into 6 subnets (3 public /24, 3 private /24) with NAT gateways in each AZ; Security Groups following principle of least privilege with separate groups for ALB, web tier, app tier, and database tier.",
    "category": "long"
  },
  {
    "id": "long_0091",
    "text": "class DistributedEmbeddingCache: \"\"\"Thread-safe distributed embedding cache backed by Redis with LRU eviction, TTL support, and automatic serialization of numpy float32 arrays to Q16.16 fixed-point representation. The cache key is SHA-256(model_hash + tokenizer_hash + query_text). Cache entries expire after ttl_seconds (default 3600). The cache uses a two-level hierarchy: L1 is an in-process LRU (maxsize=1024) and L2 is the shared Redis cluster. Write-back from L1 to L2 is asynchronous via a background thread. The serialize method converts float32[384] to Q16.16 i32[384] bytes with deterministic rounding for cross-arch reproducibility.\"\"\" def __init__(self, redis_url: str, ttl_seconds: int = 3600,              l1_maxsize: int = 1024) -> None:     self._redis = redis.Redis.from_url(redis_url)     self._ttl = ttl_seconds     self._l1 = functools.lru_cache(maxsize=l1_maxsize)(self._get_l2)     self._write_queue: queue.Queue = queue.Queue(maxsize=4096)     self._writer = threading.Thread(target=self._write_worker, daemon=True)     self._writer.start() def _cache_key(self, model_hash: str, tokenizer_hash: str, query: str) -> str:     payload = f'{model_hash}:{tokenizer_hash}:{query}'.encode('utf-8')     return 'emb:' + hashlib.sha256(payload).hexdigest() def get(self, model_hash: str, tokenizer_hash: str, query: str) -> np.ndarray | None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._redis.get(key)     if raw is None:         return None     return self._deserialize(raw) def put(self, model_hash: str, tokenizer_hash: str, query: str,         embedding: np.ndarray) -> None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._serialize(embedding)     self._redis.setex(key, self._ttl, raw) def _serialize(self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0) Furthermore, (self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0)",
    "category": "long"
  },
  {
    "id": "long_0092",
    "text": "Optimize the following Python function that processes a list of 100,000 user records to compute personalized recommendations: def compute_recommendations(users: list[dict], products: list[dict], interactions: list[dict]) -> dict[str, list[str]]:     results = {}     for user in users:         user_interactions = [i for i in interactions                              if i['user_id'] == user['id']]         interacted_products = {i['product_id'] for i in user_interactions}         similar_users = []         for other_user in users:             if other_user['id'] == user['id']: continue             other_interactions = {i['product_id'] for i in interactions                                   if i['user_id'] == other_user['id']}             if len(interacted_products & other_interactions) >= 3:                 similar_users.append(other_user['id'])         recommended = []         for similar_user_id in similar_users[:10]:             similar_interactions = {i['product_id'] for i in interactions                                     if i['user_id'] == similar_user_id}             for prod_id in similar_interactions - interacted_products:                 if prod_id not in recommended:                     recommended.append(prod_id)         results[user['id']] = recommended[:20]     return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine. Furthermore, return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine.",
    "category": "long"
  },
  {
    "id": "long_0093",
    "text": "#!/usr/bin/env bash set -euo pipefail IFS=$'\\n\\t' # Automated deployment script for mind-nerve v0.4.0 # Performs: build, test, security scan, docker build, push, deploy SCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\" REPO_ROOT=\"$(cd \"${SCRIPT_DIR}/..\" && pwd)\" VERSION=\"${1:-}\" ENVIRONMENT=\"${2:-staging}\" REGISTRY=\"ghcr.io/star-ga\" IMAGE=\"${REGISTRY}/mind-nerve\" if [[ -z \"${VERSION}\" ]]; then     echo 'ERROR: VERSION argument required' >&2     echo 'Usage: deploy.sh <version> [environment]' >&2     exit 1 fi echo \"==> Deploying mind-nerve ${VERSION} to ${ENVIRONMENT}\" cd \"${REPO_ROOT}\" echo '==> Running tests...' python -m pytest tests/ -x -q --tb=short echo '==> Running security scan...' bandit -r python/mind_nerve -ll echo '==> Building Docker image...' docker build --build-arg VERSION=\"${VERSION}\"              --build-arg BUILD_DATE=\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"              --build-arg VCS_REF=\"$(git rev-parse HEAD)\"              -t \"${IMAGE}:${VERSION}\"              -t \"${IMAGE}:latest\"              . echo '==> Scanning image for vulnerabilities...' trivy image --exit-code 1 --severity HIGH,CRITICAL \"${IMAGE}:${VERSION}\" echo '==> Pushing image...' docker push \"${IMAGE}:${VERSION}\" docker push \"${IMAGE}:latest\" echo '==> Deploying to Kubernetes...' kubectl set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\"  Furthermore, l set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\" ",
    "category": "long"
  },
  {
    "id": "long_0094",
    "text": "from __future__ import annotations import asyncio import contextlib import dataclasses import hashlib import json import logging import os import time from collections.abc import AsyncGenerator, Generator from pathlib import Path from typing import Any, ClassVar import aiohttp import numpy as np logger = logging.getLogger(__name__) @dataclasses.dataclass(frozen=True, slots=True) class EncoderConfig:     model_hash: str     tokenizer_hash: str     hidden_dim: int = 384     num_heads: int = 12     num_layers: int = 12     max_seq_len: int = 256     window_size: int = 256     stride: int = 192     top_k: int = 5     backend: str = 'native'     runtime_dir: Path = dataclasses.field(         default_factory=lambda: Path.home() / '.local/share/mind-nerve/runtime')     SCHEMA_VERSION: ClassVar[int] = 1     def to_cache_key(self) -> str:         payload = json.dumps(dataclasses.asdict(self), sort_keys=True, default=str)         return hashlib.sha256(payload.encode()).hexdigest() class EncoderError(Exception):     pass class BackendNotAvailableError(EncoderError):     pass class EncoderTimeoutError(EncoderError):     pass @contextlib.asynccontextmanager async def managed_encoder(config: EncoderConfig) -> AsyncGenerator[AsyncEncoder, None]:     encoder = AsyncEncoder(config)     try:         await encoder.initialize()         yield encoder     finally:         await encoder.shutdown() class AsyncEncoder:     def __init__(self, config: EncoderConfig) -> None:         self._config = config         self._session: aiohttp.ClientSession | None = None         self._initialized = False         self._request_count = 0         self._error_count = 0         self._total_latency_ms = 0.0     async def initialize(self) -> None:         connector = aiohttp.TCPConnector(limit=100, limit_per_host=20)         self._session = aiohttp.ClientSession(connector=connector)         self._initialized = True         logger.info('AsyncEncoder initialized: backend=%s', self._config.backend)     async def encode(self, query: str, timeout: float = 5.0) -> np.ndarray:         if not self._initialized:             raise EncoderError('Encoder not initialized')         start = time.perf_counter()         try:             result = await asyncio.wait_for(self._encode_impl(query), timeout=timeout)             self._total_latency_ms += (time.perf_counter() - start) * 1000             self._request_count += 1             return result         except asyncio.TimeoutError as exc:             self._error_count += 1             raise EncoderTimeoutError(f'Encode timed out after {timeout}s') from exc     async def _encode_impl(self, query: str) -> np.ndarray:         raise NotImplementedError     async def shutdown(self) -> None:         if self._session:             await self._session.close()         self._initialized = False     @property     def stats(self) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) }  Furthermore, elf) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) } ",
    "category": "long"
  },
  {
    "id": "long_0095",
    "text": "#!/usr/bin/env bash set -euo pipefail IFS=$'\\n\\t' # Automated deployment script for mind-nerve v0.4.0 # Performs: build, test, security scan, docker build, push, deploy SCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\" REPO_ROOT=\"$(cd \"${SCRIPT_DIR}/..\" && pwd)\" VERSION=\"${1:-}\" ENVIRONMENT=\"${2:-staging}\" REGISTRY=\"ghcr.io/star-ga\" IMAGE=\"${REGISTRY}/mind-nerve\" if [[ -z \"${VERSION}\" ]]; then     echo 'ERROR: VERSION argument required' >&2     echo 'Usage: deploy.sh <version> [environment]' >&2     exit 1 fi echo \"==> Deploying mind-nerve ${VERSION} to ${ENVIRONMENT}\" cd \"${REPO_ROOT}\" echo '==> Running tests...' python -m pytest tests/ -x -q --tb=short echo '==> Running security scan...' bandit -r python/mind_nerve -ll echo '==> Building Docker image...' docker build --build-arg VERSION=\"${VERSION}\"              --build-arg BUILD_DATE=\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"              --build-arg VCS_REF=\"$(git rev-parse HEAD)\"              -t \"${IMAGE}:${VERSION}\"              -t \"${IMAGE}:latest\"              . echo '==> Scanning image for vulnerabilities...' trivy image --exit-code 1 --severity HIGH,CRITICAL \"${IMAGE}:${VERSION}\" echo '==> Pushing image...' docker push \"${IMAGE}:${VERSION}\" docker push \"${IMAGE}:latest\" echo '==> Deploying to Kubernetes...' kubectl set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\"  Furthermore, l set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\" ",
    "category": "long"
  },
  {
    "id": "long_0096",
    "text": "Compare and contrast the following approaches to distributed consensus and explain when to use each one in the context of a globally distributed database system: Paxos (Classic Paxos, Multi-Paxos, and Fast Paxos variants), Raft (with and without pre-vote extension), Zab (ZooKeeper Atomic Broadcast), PBFT (Practical Byzantine Fault Tolerance), HotStuff (and its chained variant used in LibraBFT/DiemBFT), Tendermint/CometBFT, and Stellar Consensus Protocol. For each algorithm, discuss: the fault model (crash fault tolerant vs byzantine fault tolerant), the minimum number of nodes required for fault tolerance (f+1 vs 2f+1 vs 3f+1), the leader election mechanism and its impact on availability during leader failures, the message complexity per consensus round, the latency in terms of network round trips, the throughput in transactions per second for typical LAN and WAN deployments, the behavior under network partitions (CAP theorem position), the implementation complexity and available production-grade open-source implementations, and any known correctness bugs or safety violations discovered in production deployments. Then recommend the appropriate algorithm for a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants. Furthermore, or a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants.",
    "category": "long"
  },
  {
    "id": "long_0097",
    "text": "package encoder import (     'context'     'crypto/sha256'     'encoding/binary'     'fmt'     'math'     'sync' ) const (     HiddenDim   = 384     NumHeads    = 12     HeadDim     = 32     Q16Scale    = 65536.0     LayerNormEps = 1e-12 ) type Q16 int32 func Float32ToQ16(f float32) Q16 {     scaled := float64(f) * Q16Scale     if scaled > math.MaxInt32 { return Q16(math.MaxInt32) }     if scaled < math.MinInt32 { return Q16(math.MinInt32) }     return Q16(math.Round(scaled)) } func (q Q16) ToFloat32() float32 { return float32(float64(q) / Q16Scale) } type Matrix struct { Data []Q16; Rows, Cols int } func NewMatrix(rows, cols int) *Matrix {     return &Matrix{Data: make([]Q16, rows*cols), Rows: rows, Cols: cols} } func (m *Matrix) At(row, col int) Q16 { return m.Data[row*m.Cols+col] } func (m *Matrix) Set(row, col int, v Q16) { m.Data[row*m.Cols+col] = v } func Matmul(a, b *Matrix) *Matrix {     if a.Cols != b.Rows { panic('dimension mismatch') }     c := NewMatrix(a.Rows, b.Cols)     var wg sync.WaitGroup     for i := 0; i < a.Rows; i++ {         wg.Add(1)         go func(row int) {             defer wg.Done()             for j := 0; j < b.Cols; j++ {                 var acc int64                 for k := 0; k < a.Cols; k++ {                     acc += int64(a.At(row, k)) * int64(b.At(k, j)) }                 c.Set(row, j, Q16(acc>>16)) } }(i) }     wg.Wait()     return c } func LayerNorm(x *Matrix, gamma, beta []Q16) *Matrix {     out := NewMatrix(x.Rows, x.Cols)     for i := 0; i < x.Rows; i++ {         var sum int64         for j := 0; j < x.Cols; j++ { sum += int64(x.At(i, j)) }         mean := Q16(sum / int64(x.Cols))         var varAcc int64         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             varAcc += d * d }         variance := float64(varAcc) / float64(x.Cols) / Q16Scale / Q16Scale         invStd := 1.0 / math.Sqrt(variance + LayerNormEps)         invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out }  Furthermore,     invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out } ",
    "category": "long"
  },
  {
    "id": "long_0098",
    "text": "Analyze the following microservices architecture and identify potential single points of failure, bottlenecks, and security vulnerabilities: The system consists of an API gateway that routes requests to twelve backend services including user-service, auth-service, payment-service, notification-service, email-service, sms-service, recommendation-service, search-service, catalog-service, inventory-service, order-service, and shipping-service. Each service has its own PostgreSQL database with connection pooling configured via PgBouncer. Services communicate asynchronously via RabbitMQ for non-critical paths and synchronously via gRPC for critical paths like payment processing. The API gateway is implemented in Nginx with Lua scripting for rate limiting. Authentication uses JWT tokens with a 15-minute expiry refreshed automatically by the client. The recommendation service uses a pre-trained collaborative filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses. Furthermore, ve filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses.",
    "category": "long"
  },
  {
    "id": "long_0099",
    "text": "use std::collections::HashMap; use std::sync::{Arc, RwLock}; use std::time::{Duration, Instant}; #[derive(Debug, Clone)] pub struct CacheEntry<V> { value: V, inserted_at: Instant, ttl: Duration, } impl<V> CacheEntry<V> {     pub fn is_expired(&self) -> bool {         self.inserted_at.elapsed() >= self.ttl } } #[derive(Debug)] pub struct TtlCache<K, V> {     inner: Arc<RwLock<HashMap<K, CacheEntry<V>>>>,     default_ttl: Duration, } impl<K, V> TtlCache<K, V> where K: Eq + std::hash::Hash + Clone, V: Clone, {     pub fn new(default_ttl: Duration) -> Self {         Self { inner: Arc::new(RwLock::new(HashMap::new())), default_ttl } }     pub fn insert(&self, key: K, value: V) {         let entry = CacheEntry { value, inserted_at: Instant::now(),             ttl: self.default_ttl };         self.inner.write().unwrap().insert(key, entry); }     pub fn get(&self, key: &K) -> Option<V> {         let guard = self.inner.read().unwrap();         guard.get(key).filter(|e| !e.is_expired()).map(|e| e.value.clone()) }     pub fn evict_expired(&self) -> usize {         let mut guard = self.inner.write().unwrap();         let before = guard.len();         guard.retain(|_, e| !e.is_expired());         before - guard.len() }     pub fn len(&self) -> usize { self.inner.read().unwrap().len() }     pub fn is_empty(&self) -> bool { self.len() == 0 } } Furthermore,  -> usize {         let mut guard = self.inner.write().unwrap();         let before = guard.len();         guard.retain(|_, e| !e.is_expired());         before - guard.len() }     pub fn len(&self) -> usize { self.inner.read().unwrap().len() }     pub fn is_empty(&self) -> bool { self.len() == 0 } }",
    "category": "long"
  },
  {
    "id": "long_0100",
    "text": "Analyze the following microservices architecture and identify potential single points of failure, bottlenecks, and security vulnerabilities: The system consists of an API gateway that routes requests to twelve backend services including user-service, auth-service, payment-service, notification-service, email-service, sms-service, recommendation-service, search-service, catalog-service, inventory-service, order-service, and shipping-service. Each service has its own PostgreSQL database with connection pooling configured via PgBouncer. Services communicate asynchronously via RabbitMQ for non-critical paths and synchronously via gRPC for critical paths like payment processing. The API gateway is implemented in Nginx with Lua scripting for rate limiting. Authentication uses JWT tokens with a 15-minute expiry refreshed automatically by the client. The recommendation service uses a pre-trained collaborative filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses. Furthermore, ve filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses.",
    "category": "long"
  },
  {
    "id": "long_0101",
    "text": "import { createServer } from 'http'; import { WebSocketServer, WebSocket } from 'ws'; import { EventEmitter } from 'events'; interface ClientMessage { type: 'subscribe' | 'unsubscribe' | 'publish';   channel: string; payload?: unknown; clientId: string; } interface ServerMessage { type: 'data' | 'error' | 'ack';   channel?: string; payload?: unknown; timestamp: number; } class PubSubServer extends EventEmitter {   private channels = new Map<string, Set<WebSocket>>();   private clientChannels = new Map<WebSocket, Set<string>>();   constructor(private readonly port: number) { super(); }   subscribe(ws: WebSocket, channel: string): void {     if (!this.channels.has(channel)) this.channels.set(channel, new Set());     this.channels.get(channel)!.add(ws);     if (!this.clientChannels.has(ws)) this.clientChannels.set(ws, new Set());     this.clientChannels.get(ws)!.add(channel); }   unsubscribe(ws: WebSocket, channel: string): void {     this.channels.get(channel)?.delete(ws);     this.clientChannels.get(ws)?.delete(channel); }   publish(channel: string, payload: unknown): void {     const msg: ServerMessage = { type: 'data', channel, payload,       timestamp: Date.now() };     const data = JSON.stringify(msg);     this.channels.get(channel)?.forEach(client => {       if (client.readyState === WebSocket.OPEN) client.send(data); }); }   cleanup(ws: WebSocket): void {     this.clientChannels.get(ws)?.forEach(ch => this.unsubscribe(ws, ch));     this.clientChannels.delete(ws); }   listen(): void {     const server = createServer();     const wss = new WebSocketServer({ server });     wss.on('connection', ws => {       ws.on('message', raw => {         const msg: ClientMessage = JSON.parse(raw.toString());         switch (msg.type) {           case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } } Furthermore,         case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } }",
    "category": "long"
  },
  {
    "id": "long_0102",
    "text": "package encoder import (     'context'     'crypto/sha256'     'encoding/binary'     'fmt'     'math'     'sync' ) const (     HiddenDim   = 384     NumHeads    = 12     HeadDim     = 32     Q16Scale    = 65536.0     LayerNormEps = 1e-12 ) type Q16 int32 func Float32ToQ16(f float32) Q16 {     scaled := float64(f) * Q16Scale     if scaled > math.MaxInt32 { return Q16(math.MaxInt32) }     if scaled < math.MinInt32 { return Q16(math.MinInt32) }     return Q16(math.Round(scaled)) } func (q Q16) ToFloat32() float32 { return float32(float64(q) / Q16Scale) } type Matrix struct { Data []Q16; Rows, Cols int } func NewMatrix(rows, cols int) *Matrix {     return &Matrix{Data: make([]Q16, rows*cols), Rows: rows, Cols: cols} } func (m *Matrix) At(row, col int) Q16 { return m.Data[row*m.Cols+col] } func (m *Matrix) Set(row, col int, v Q16) { m.Data[row*m.Cols+col] = v } func Matmul(a, b *Matrix) *Matrix {     if a.Cols != b.Rows { panic('dimension mismatch') }     c := NewMatrix(a.Rows, b.Cols)     var wg sync.WaitGroup     for i := 0; i < a.Rows; i++ {         wg.Add(1)         go func(row int) {             defer wg.Done()             for j := 0; j < b.Cols; j++ {                 var acc int64                 for k := 0; k < a.Cols; k++ {                     acc += int64(a.At(row, k)) * int64(b.At(k, j)) }                 c.Set(row, j, Q16(acc>>16)) } }(i) }     wg.Wait()     return c } func LayerNorm(x *Matrix, gamma, beta []Q16) *Matrix {     out := NewMatrix(x.Rows, x.Cols)     for i := 0; i < x.Rows; i++ {         var sum int64         for j := 0; j < x.Cols; j++ { sum += int64(x.At(i, j)) }         mean := Q16(sum / int64(x.Cols))         var varAcc int64         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             varAcc += d * d }         variance := float64(varAcc) / float64(x.Cols) / Q16Scale / Q16Scale         invStd := 1.0 / math.Sqrt(variance + LayerNormEps)         invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out }  Furthermore,     invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out } ",
    "category": "long"
  },
  {
    "id": "long_0103",
    "text": "#!/usr/bin/env bash set -euo pipefail IFS=$'\\n\\t' # Automated deployment script for mind-nerve v0.4.0 # Performs: build, test, security scan, docker build, push, deploy SCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\" REPO_ROOT=\"$(cd \"${SCRIPT_DIR}/..\" && pwd)\" VERSION=\"${1:-}\" ENVIRONMENT=\"${2:-staging}\" REGISTRY=\"ghcr.io/star-ga\" IMAGE=\"${REGISTRY}/mind-nerve\" if [[ -z \"${VERSION}\" ]]; then     echo 'ERROR: VERSION argument required' >&2     echo 'Usage: deploy.sh <version> [environment]' >&2     exit 1 fi echo \"==> Deploying mind-nerve ${VERSION} to ${ENVIRONMENT}\" cd \"${REPO_ROOT}\" echo '==> Running tests...' python -m pytest tests/ -x -q --tb=short echo '==> Running security scan...' bandit -r python/mind_nerve -ll echo '==> Building Docker image...' docker build --build-arg VERSION=\"${VERSION}\"              --build-arg BUILD_DATE=\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"              --build-arg VCS_REF=\"$(git rev-parse HEAD)\"              -t \"${IMAGE}:${VERSION}\"              -t \"${IMAGE}:latest\"              . echo '==> Scanning image for vulnerabilities...' trivy image --exit-code 1 --severity HIGH,CRITICAL \"${IMAGE}:${VERSION}\" echo '==> Pushing image...' docker push \"${IMAGE}:${VERSION}\" docker push \"${IMAGE}:latest\" echo '==> Deploying to Kubernetes...' kubectl set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\"  Furthermore, l set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\" ",
    "category": "long"
  },
  {
    "id": "long_0104",
    "text": "Implement a complete CI/CD pipeline for a Python machine learning project that includes the following stages: code quality checks using ruff, black, isort, mypy, and bandit; unit tests with pytest and coverage reporting; integration tests against a real PostgreSQL database using testcontainers; model training on a held-out validation set with automatic hyperparameter tuning using Optuna; model evaluation comparing the new model against the currently deployed baseline on a standardized benchmark dataset of 10,000 samples; model registration in MLflow with artifact storage on S3; Docker image build with multi-stage build optimized for production size under 500MB; security scanning using trivy and snyk; deployment to Kubernetes staging environment with smoke tests; automated load testing using k6 to validate p99 latency under 200ms at 1000 concurrent users; canary deployment to production with 5% traffic split; monitoring of key metrics for 30 minutes before full rollout; automatic rollback if error rate exceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials. Furthermore, xceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials.",
    "category": "long"
  },
  {
    "id": "long_0105",
    "text": "openapi: 3.1.0 info:   title: mind-nerve Encoding API   version: 0.4.0   description: |     Q16.16 native BERT encoder for intent classification and tool routing.     Encodes query strings into 384-dimensional L2-normalized embeddings     and returns top-K routing candidates from a precomputed catalog.     The encode endpoint accepts a JSON body with a query string and     optional parameters for top_k, backend selection, and runtime override.     All embeddings are returned as Q16.16 fixed-point integers to ensure     bit-identical results across x86_64 CPU backends. CUDA and ARM backends     are available but require explicit opt-in via the backend parameter.     Rate limiting is enforced at 1000 requests per minute per API key.     The catalog endpoint returns metadata about the currently loaded     routing catalog including version, size, and hash for cache validation. paths:   /v1/encode:     post:       operationId: encodeQuery       summary: Encode a query string and return top-K routing candidates       requestBody:         required: true         content:           application/json:             schema:               type: object               required: [query]               properties:                 query:                   type: string                   minLength: 1                   maxLength: 2048                   description: The query string to encode and route                 top_k:                   type: integer                   minimum: 1                   maximum: 100                   default: 5                 backend:                   type: string                   enum: [native, pytorch, cuda]                   default: native       responses:         '200':           description: Successful encoding and routing           content:             application/json:               schema:                 $ref: '#/components/schemas/RouteResult'         '400':           description: Invalid request (query too long, invalid backend)         '429':           description: Rate limit exceeded         '503':           description: Backend unavailable Furthermore, application/json:               schema:                 $ref: '#/components/schemas/RouteResult'         '400':           description: Invalid request (query too long, invalid backend)         '429':           description: Rate limit exceeded         '503':           description: Backend unavailable",
    "category": "long"
  },
  {
    "id": "long_0106",
    "text": "apiVersion: apps/v1 kind: Deployment metadata:   name: mind-nerve-encoder   namespace: production   labels:     app: mind-nerve     component: encoder     version: v0.4.0 spec:   replicas: 3   selector:     matchLabels:       app: mind-nerve       component: encoder   strategy:     type: RollingUpdate     rollingUpdate:       maxSurge: 1       maxUnavailable: 0   template:     metadata:       labels:         app: mind-nerve         component: encoder         version: v0.4.0       annotations:         prometheus.io/scrape: 'true'         prometheus.io/port: '9090'         prometheus.io/path: '/metrics'     spec:       securityContext:         runAsNonRoot: true         runAsUser: 1000         fsGroup: 1000       containers:       - name: encoder         image: ghcr.io/star-ga/mind-nerve:v0.4.0         ports:         - containerPort: 8080         - containerPort: 9090         resources:           requests:             cpu: 500m             memory: 512Mi           limits:             cpu: 2000m             memory: 2Gi         readinessProbe:           httpGet:             path: /health/ready             port: 8080           initialDelaySeconds: 10           periodSeconds: 5         livenessProbe:           httpGet:             path: /health/live             port: 8080           initialDelaySeconds: 30           periodSeconds: 10         env:         - name: MIND_NERVE_BACKEND           value: native         - name: MIND_NERVE_RUNTIME_DIR           value: /runtime         volumeMounts:         - name: runtime           mountPath: /runtime           readOnly: true       volumes:       - name: runtime         configMap:           name: mind-nerve-runtime Furthermore,  MIND_NERVE_BACKEND           value: native         - name: MIND_NERVE_RUNTIME_DIR           value: /runtime         volumeMounts:         - name: runtime           mountPath: /runtime           readOnly: true       volumes:       - name: runtime         configMap:           name: mind-nerve-runtime",
    "category": "long"
  },
  {
    "id": "long_0107",
    "text": "openapi: 3.1.0 info:   title: mind-nerve Encoding API   version: 0.4.0   description: |     Q16.16 native BERT encoder for intent classification and tool routing.     Encodes query strings into 384-dimensional L2-normalized embeddings     and returns top-K routing candidates from a precomputed catalog.     The encode endpoint accepts a JSON body with a query string and     optional parameters for top_k, backend selection, and runtime override.     All embeddings are returned as Q16.16 fixed-point integers to ensure     bit-identical results across x86_64 CPU backends. CUDA and ARM backends     are available but require explicit opt-in via the backend parameter.     Rate limiting is enforced at 1000 requests per minute per API key.     The catalog endpoint returns metadata about the currently loaded     routing catalog including version, size, and hash for cache validation. paths:   /v1/encode:     post:       operationId: encodeQuery       summary: Encode a query string and return top-K routing candidates       requestBody:         required: true         content:           application/json:             schema:               type: object               required: [query]               properties:                 query:                   type: string                   minLength: 1                   maxLength: 2048                   description: The query string to encode and route                 top_k:                   type: integer                   minimum: 1                   maximum: 100                   default: 5                 backend:                   type: string                   enum: [native, pytorch, cuda]                   default: native       responses:         '200':           description: Successful encoding and routing           content:             application/json:               schema:                 $ref: '#/components/schemas/RouteResult'         '400':           description: Invalid request (query too long, invalid backend)         '429':           description: Rate limit exceeded         '503':           description: Backend unavailable Furthermore, application/json:               schema:                 $ref: '#/components/schemas/RouteResult'         '400':           description: Invalid request (query too long, invalid backend)         '429':           description: Rate limit exceeded         '503':           description: Backend unavailable",
    "category": "long"
  },
  {
    "id": "long_0108",
    "text": "class DistributedEmbeddingCache: \"\"\"Thread-safe distributed embedding cache backed by Redis with LRU eviction, TTL support, and automatic serialization of numpy float32 arrays to Q16.16 fixed-point representation. The cache key is SHA-256(model_hash + tokenizer_hash + query_text). Cache entries expire after ttl_seconds (default 3600). The cache uses a two-level hierarchy: L1 is an in-process LRU (maxsize=1024) and L2 is the shared Redis cluster. Write-back from L1 to L2 is asynchronous via a background thread. The serialize method converts float32[384] to Q16.16 i32[384] bytes with deterministic rounding for cross-arch reproducibility.\"\"\" def __init__(self, redis_url: str, ttl_seconds: int = 3600,              l1_maxsize: int = 1024) -> None:     self._redis = redis.Redis.from_url(redis_url)     self._ttl = ttl_seconds     self._l1 = functools.lru_cache(maxsize=l1_maxsize)(self._get_l2)     self._write_queue: queue.Queue = queue.Queue(maxsize=4096)     self._writer = threading.Thread(target=self._write_worker, daemon=True)     self._writer.start() def _cache_key(self, model_hash: str, tokenizer_hash: str, query: str) -> str:     payload = f'{model_hash}:{tokenizer_hash}:{query}'.encode('utf-8')     return 'emb:' + hashlib.sha256(payload).hexdigest() def get(self, model_hash: str, tokenizer_hash: str, query: str) -> np.ndarray | None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._redis.get(key)     if raw is None:         return None     return self._deserialize(raw) def put(self, model_hash: str, tokenizer_hash: str, query: str,         embedding: np.ndarray) -> None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._serialize(embedding)     self._redis.setex(key, self._ttl, raw) def _serialize(self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0) Furthermore, (self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0)",
    "category": "long"
  },
  {
    "id": "long_0109",
    "text": "Design and implement a fault-tolerant data pipeline that ingests streaming events from Apache Kafka topics (user.clicked, user.purchased, user.viewed, user.searched) at a rate of 500,000 events per second, applies real-time feature engineering including session reconstruction with a 30-minute session timeout, rolling window aggregations over 1-minute, 5-minute, and 1-hour windows, user-level and item-level statistics, and categorical feature encoding using frequency-adaptive target encoding with Laplace smoothing; writes the enriched feature vectors to a Redis Feature Store with a 24-hour TTL for online serving and to Apache Iceberg tables on S3 for offline training; handles late arriving events up to 5 minutes behind watermark using Apache Flink's event-time processing with allowed lateness; implements exactly-once semantics via Kafka transactions and Flink checkpointing to S3 every 60 seconds; provides a Grafana dashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint. Furthermore, ashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint.",
    "category": "long"
  },
  {
    "id": "long_0110",
    "text": "Design and implement a fault-tolerant data pipeline that ingests streaming events from Apache Kafka topics (user.clicked, user.purchased, user.viewed, user.searched) at a rate of 500,000 events per second, applies real-time feature engineering including session reconstruction with a 30-minute session timeout, rolling window aggregations over 1-minute, 5-minute, and 1-hour windows, user-level and item-level statistics, and categorical feature encoding using frequency-adaptive target encoding with Laplace smoothing; writes the enriched feature vectors to a Redis Feature Store with a 24-hour TTL for online serving and to Apache Iceberg tables on S3 for offline training; handles late arriving events up to 5 minutes behind watermark using Apache Flink's event-time processing with allowed lateness; implements exactly-once semantics via Kafka transactions and Flink checkpointing to S3 every 60 seconds; provides a Grafana dashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint. Furthermore, ashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint.",
    "category": "long"
  },
  {
    "id": "long_0111",
    "text": "package encoder import (     'context'     'crypto/sha256'     'encoding/binary'     'fmt'     'math'     'sync' ) const (     HiddenDim   = 384     NumHeads    = 12     HeadDim     = 32     Q16Scale    = 65536.0     LayerNormEps = 1e-12 ) type Q16 int32 func Float32ToQ16(f float32) Q16 {     scaled := float64(f) * Q16Scale     if scaled > math.MaxInt32 { return Q16(math.MaxInt32) }     if scaled < math.MinInt32 { return Q16(math.MinInt32) }     return Q16(math.Round(scaled)) } func (q Q16) ToFloat32() float32 { return float32(float64(q) / Q16Scale) } type Matrix struct { Data []Q16; Rows, Cols int } func NewMatrix(rows, cols int) *Matrix {     return &Matrix{Data: make([]Q16, rows*cols), Rows: rows, Cols: cols} } func (m *Matrix) At(row, col int) Q16 { return m.Data[row*m.Cols+col] } func (m *Matrix) Set(row, col int, v Q16) { m.Data[row*m.Cols+col] = v } func Matmul(a, b *Matrix) *Matrix {     if a.Cols != b.Rows { panic('dimension mismatch') }     c := NewMatrix(a.Rows, b.Cols)     var wg sync.WaitGroup     for i := 0; i < a.Rows; i++ {         wg.Add(1)         go func(row int) {             defer wg.Done()             for j := 0; j < b.Cols; j++ {                 var acc int64                 for k := 0; k < a.Cols; k++ {                     acc += int64(a.At(row, k)) * int64(b.At(k, j)) }                 c.Set(row, j, Q16(acc>>16)) } }(i) }     wg.Wait()     return c } func LayerNorm(x *Matrix, gamma, beta []Q16) *Matrix {     out := NewMatrix(x.Rows, x.Cols)     for i := 0; i < x.Rows; i++ {         var sum int64         for j := 0; j < x.Cols; j++ { sum += int64(x.At(i, j)) }         mean := Q16(sum / int64(x.Cols))         var varAcc int64         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             varAcc += d * d }         variance := float64(varAcc) / float64(x.Cols) / Q16Scale / Q16Scale         invStd := 1.0 / math.Sqrt(variance + LayerNormEps)         invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out }  Furthermore,     invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out } ",
    "category": "long"
  },
  {
    "id": "long_0112",
    "text": "Implement a complete CI/CD pipeline for a Python machine learning project that includes the following stages: code quality checks using ruff, black, isort, mypy, and bandit; unit tests with pytest and coverage reporting; integration tests against a real PostgreSQL database using testcontainers; model training on a held-out validation set with automatic hyperparameter tuning using Optuna; model evaluation comparing the new model against the currently deployed baseline on a standardized benchmark dataset of 10,000 samples; model registration in MLflow with artifact storage on S3; Docker image build with multi-stage build optimized for production size under 500MB; security scanning using trivy and snyk; deployment to Kubernetes staging environment with smoke tests; automated load testing using k6 to validate p99 latency under 200ms at 1000 concurrent users; canary deployment to production with 5% traffic split; monitoring of key metrics for 30 minutes before full rollout; automatic rollback if error rate exceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials. Furthermore, xceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials.",
    "category": "long"
  },
  {
    "id": "long_0113",
    "text": "import { createServer } from 'http'; import { WebSocketServer, WebSocket } from 'ws'; import { EventEmitter } from 'events'; interface ClientMessage { type: 'subscribe' | 'unsubscribe' | 'publish';   channel: string; payload?: unknown; clientId: string; } interface ServerMessage { type: 'data' | 'error' | 'ack';   channel?: string; payload?: unknown; timestamp: number; } class PubSubServer extends EventEmitter {   private channels = new Map<string, Set<WebSocket>>();   private clientChannels = new Map<WebSocket, Set<string>>();   constructor(private readonly port: number) { super(); }   subscribe(ws: WebSocket, channel: string): void {     if (!this.channels.has(channel)) this.channels.set(channel, new Set());     this.channels.get(channel)!.add(ws);     if (!this.clientChannels.has(ws)) this.clientChannels.set(ws, new Set());     this.clientChannels.get(ws)!.add(channel); }   unsubscribe(ws: WebSocket, channel: string): void {     this.channels.get(channel)?.delete(ws);     this.clientChannels.get(ws)?.delete(channel); }   publish(channel: string, payload: unknown): void {     const msg: ServerMessage = { type: 'data', channel, payload,       timestamp: Date.now() };     const data = JSON.stringify(msg);     this.channels.get(channel)?.forEach(client => {       if (client.readyState === WebSocket.OPEN) client.send(data); }); }   cleanup(ws: WebSocket): void {     this.clientChannels.get(ws)?.forEach(ch => this.unsubscribe(ws, ch));     this.clientChannels.delete(ws); }   listen(): void {     const server = createServer();     const wss = new WebSocketServer({ server });     wss.on('connection', ws => {       ws.on('message', raw => {         const msg: ClientMessage = JSON.parse(raw.toString());         switch (msg.type) {           case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } } Furthermore,         case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } }",
    "category": "long"
  },
  {
    "id": "long_0114",
    "text": "Architectural Decision Record: Switching from synchronous REST API calls to an event-driven architecture using Apache Kafka for the order processing pipeline. Context: The current synchronous REST-based order processing system suffers from cascading failures when downstream services are slow or unavailable. Peak load of 10,000 orders per minute causes timeouts in the payment service which propagates back to the API gateway causing HTTP 503 errors visible to customers. The average order processing time is 2.3 seconds due to sequential API calls to inventory, payment, notification, and shipping services. Decision: Implement event-driven architecture using Apache Kafka as the message broker. Each service publishes domain events and subscribes to relevant topics. Order creation publishes to order.created topic. Payment service subscribes to order.created and publishes payment.processed or payment.failed. Inventory service subscribes to payment.processed and publishes inventory.reserved or inventory.insufficient. The API immediately returns 202 Accepted with an order ID. Customers poll a read model or receive webhook notifications when order status changes. Consequences: Orders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully. Furthermore, ders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully.",
    "category": "long"
  },
  {
    "id": "long_0115",
    "text": "Write a PostgreSQL query to find all users who have placed more than 5 orders in the last 30 days, where each order total exceeds $100, and who have not been flagged for fraud, grouped by user_id with aggregated statistics including total order count, total spend, average order value, first order date, last order date, and the names of the top 3 most frequently ordered products for each user, sorted by total spend descending, with pagination support using cursor-based pagination on user_id for efficient handling of large result sets. The query should use CTEs for readability, include appropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring. Furthermore, ppropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring.",
    "category": "long"
  },
  {
    "id": "long_0116",
    "text": "class DistributedEmbeddingCache: \"\"\"Thread-safe distributed embedding cache backed by Redis with LRU eviction, TTL support, and automatic serialization of numpy float32 arrays to Q16.16 fixed-point representation. The cache key is SHA-256(model_hash + tokenizer_hash + query_text). Cache entries expire after ttl_seconds (default 3600). The cache uses a two-level hierarchy: L1 is an in-process LRU (maxsize=1024) and L2 is the shared Redis cluster. Write-back from L1 to L2 is asynchronous via a background thread. The serialize method converts float32[384] to Q16.16 i32[384] bytes with deterministic rounding for cross-arch reproducibility.\"\"\" def __init__(self, redis_url: str, ttl_seconds: int = 3600,              l1_maxsize: int = 1024) -> None:     self._redis = redis.Redis.from_url(redis_url)     self._ttl = ttl_seconds     self._l1 = functools.lru_cache(maxsize=l1_maxsize)(self._get_l2)     self._write_queue: queue.Queue = queue.Queue(maxsize=4096)     self._writer = threading.Thread(target=self._write_worker, daemon=True)     self._writer.start() def _cache_key(self, model_hash: str, tokenizer_hash: str, query: str) -> str:     payload = f'{model_hash}:{tokenizer_hash}:{query}'.encode('utf-8')     return 'emb:' + hashlib.sha256(payload).hexdigest() def get(self, model_hash: str, tokenizer_hash: str, query: str) -> np.ndarray | None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._redis.get(key)     if raw is None:         return None     return self._deserialize(raw) def put(self, model_hash: str, tokenizer_hash: str, query: str,         embedding: np.ndarray) -> None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._serialize(embedding)     self._redis.setex(key, self._ttl, raw) def _serialize(self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0) Furthermore, (self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0)",
    "category": "long"
  },
  {
    "id": "long_0117",
    "text": "apiVersion: apps/v1 kind: Deployment metadata:   name: mind-nerve-encoder   namespace: production   labels:     app: mind-nerve     component: encoder     version: v0.4.0 spec:   replicas: 3   selector:     matchLabels:       app: mind-nerve       component: encoder   strategy:     type: RollingUpdate     rollingUpdate:       maxSurge: 1       maxUnavailable: 0   template:     metadata:       labels:         app: mind-nerve         component: encoder         version: v0.4.0       annotations:         prometheus.io/scrape: 'true'         prometheus.io/port: '9090'         prometheus.io/path: '/metrics'     spec:       securityContext:         runAsNonRoot: true         runAsUser: 1000         fsGroup: 1000       containers:       - name: encoder         image: ghcr.io/star-ga/mind-nerve:v0.4.0         ports:         - containerPort: 8080         - containerPort: 9090         resources:           requests:             cpu: 500m             memory: 512Mi           limits:             cpu: 2000m             memory: 2Gi         readinessProbe:           httpGet:             path: /health/ready             port: 8080           initialDelaySeconds: 10           periodSeconds: 5         livenessProbe:           httpGet:             path: /health/live             port: 8080           initialDelaySeconds: 30           periodSeconds: 10         env:         - name: MIND_NERVE_BACKEND           value: native         - name: MIND_NERVE_RUNTIME_DIR           value: /runtime         volumeMounts:         - name: runtime           mountPath: /runtime           readOnly: true       volumes:       - name: runtime         configMap:           name: mind-nerve-runtime Furthermore,  MIND_NERVE_BACKEND           value: native         - name: MIND_NERVE_RUNTIME_DIR           value: /runtime         volumeMounts:         - name: runtime           mountPath: /runtime           readOnly: true       volumes:       - name: runtime         configMap:           name: mind-nerve-runtime",
    "category": "long"
  },
  {
    "id": "long_0118",
    "text": "apiVersion: apps/v1 kind: Deployment metadata:   name: mind-nerve-encoder   namespace: production   labels:     app: mind-nerve     component: encoder     version: v0.4.0 spec:   replicas: 3   selector:     matchLabels:       app: mind-nerve       component: encoder   strategy:     type: RollingUpdate     rollingUpdate:       maxSurge: 1       maxUnavailable: 0   template:     metadata:       labels:         app: mind-nerve         component: encoder         version: v0.4.0       annotations:         prometheus.io/scrape: 'true'         prometheus.io/port: '9090'         prometheus.io/path: '/metrics'     spec:       securityContext:         runAsNonRoot: true         runAsUser: 1000         fsGroup: 1000       containers:       - name: encoder         image: ghcr.io/star-ga/mind-nerve:v0.4.0         ports:         - containerPort: 8080         - containerPort: 9090         resources:           requests:             cpu: 500m             memory: 512Mi           limits:             cpu: 2000m             memory: 2Gi         readinessProbe:           httpGet:             path: /health/ready             port: 8080           initialDelaySeconds: 10           periodSeconds: 5         livenessProbe:           httpGet:             path: /health/live             port: 8080           initialDelaySeconds: 30           periodSeconds: 10         env:         - name: MIND_NERVE_BACKEND           value: native         - name: MIND_NERVE_RUNTIME_DIR           value: /runtime         volumeMounts:         - name: runtime           mountPath: /runtime           readOnly: true       volumes:       - name: runtime         configMap:           name: mind-nerve-runtime Furthermore,  MIND_NERVE_BACKEND           value: native         - name: MIND_NERVE_RUNTIME_DIR           value: /runtime         volumeMounts:         - name: runtime           mountPath: /runtime           readOnly: true       volumes:       - name: runtime         configMap:           name: mind-nerve-runtime",
    "category": "long"
  },
  {
    "id": "long_0119",
    "text": "def process_batch(items: list[dict], batch_size: int = 32, timeout: float = 30.0, retry_count: int = 3, backoff_factor: float = 1.5) -> list[dict]: \"\"\"Process a batch of items with retry logic and exponential backoff. Each item is validated, transformed, and persisted to the database. Failed items are retried up to retry_count times with exponential backoff. Items that fail all retries are logged and returned in the error list. The function is thread-safe and can be called from multiple workers. Parameters: items (list[dict]) — the items to process; batch_size (int) — number of items per database transaction; timeout (float) — per-item timeout in seconds; retry_count (int) — maximum retries per item; backoff_factor (float) — exponential backoff multiplier. Returns: list of successfully processed items.\"\"\" results = [] errors = [] for i in range(0, len(items), batch_size):     chunk = items[i:i + batch_size]     with db.transaction():         for item in chunk:             for attempt in range(retry_count):                 try:                     validated = validate(item)                     transformed = transform(validated)                     persisted = persist(transformed, timeout=timeout)                     results.append(persisted)                     break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors Furthermore,                   break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors",
    "category": "long"
  },
  {
    "id": "long_0120",
    "text": "def process_batch(items: list[dict], batch_size: int = 32, timeout: float = 30.0, retry_count: int = 3, backoff_factor: float = 1.5) -> list[dict]: \"\"\"Process a batch of items with retry logic and exponential backoff. Each item is validated, transformed, and persisted to the database. Failed items are retried up to retry_count times with exponential backoff. Items that fail all retries are logged and returned in the error list. The function is thread-safe and can be called from multiple workers. Parameters: items (list[dict]) — the items to process; batch_size (int) — number of items per database transaction; timeout (float) — per-item timeout in seconds; retry_count (int) — maximum retries per item; backoff_factor (float) — exponential backoff multiplier. Returns: list of successfully processed items.\"\"\" results = [] errors = [] for i in range(0, len(items), batch_size):     chunk = items[i:i + batch_size]     with db.transaction():         for item in chunk:             for attempt in range(retry_count):                 try:                     validated = validate(item)                     transformed = transform(validated)                     persisted = persist(transformed, timeout=timeout)                     results.append(persisted)                     break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors Furthermore,                   break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors",
    "category": "long"
  },
  {
    "id": "long_0121",
    "text": "from __future__ import annotations import asyncio import contextlib import dataclasses import hashlib import json import logging import os import time from collections.abc import AsyncGenerator, Generator from pathlib import Path from typing import Any, ClassVar import aiohttp import numpy as np logger = logging.getLogger(__name__) @dataclasses.dataclass(frozen=True, slots=True) class EncoderConfig:     model_hash: str     tokenizer_hash: str     hidden_dim: int = 384     num_heads: int = 12     num_layers: int = 12     max_seq_len: int = 256     window_size: int = 256     stride: int = 192     top_k: int = 5     backend: str = 'native'     runtime_dir: Path = dataclasses.field(         default_factory=lambda: Path.home() / '.local/share/mind-nerve/runtime')     SCHEMA_VERSION: ClassVar[int] = 1     def to_cache_key(self) -> str:         payload = json.dumps(dataclasses.asdict(self), sort_keys=True, default=str)         return hashlib.sha256(payload.encode()).hexdigest() class EncoderError(Exception):     pass class BackendNotAvailableError(EncoderError):     pass class EncoderTimeoutError(EncoderError):     pass @contextlib.asynccontextmanager async def managed_encoder(config: EncoderConfig) -> AsyncGenerator[AsyncEncoder, None]:     encoder = AsyncEncoder(config)     try:         await encoder.initialize()         yield encoder     finally:         await encoder.shutdown() class AsyncEncoder:     def __init__(self, config: EncoderConfig) -> None:         self._config = config         self._session: aiohttp.ClientSession | None = None         self._initialized = False         self._request_count = 0         self._error_count = 0         self._total_latency_ms = 0.0     async def initialize(self) -> None:         connector = aiohttp.TCPConnector(limit=100, limit_per_host=20)         self._session = aiohttp.ClientSession(connector=connector)         self._initialized = True         logger.info('AsyncEncoder initialized: backend=%s', self._config.backend)     async def encode(self, query: str, timeout: float = 5.0) -> np.ndarray:         if not self._initialized:             raise EncoderError('Encoder not initialized')         start = time.perf_counter()         try:             result = await asyncio.wait_for(self._encode_impl(query), timeout=timeout)             self._total_latency_ms += (time.perf_counter() - start) * 1000             self._request_count += 1             return result         except asyncio.TimeoutError as exc:             self._error_count += 1             raise EncoderTimeoutError(f'Encode timed out after {timeout}s') from exc     async def _encode_impl(self, query: str) -> np.ndarray:         raise NotImplementedError     async def shutdown(self) -> None:         if self._session:             await self._session.close()         self._initialized = False     @property     def stats(self) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) }  Furthermore, elf) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) } ",
    "category": "long"
  },
  {
    "id": "long_0122",
    "text": "def process_batch(items: list[dict], batch_size: int = 32, timeout: float = 30.0, retry_count: int = 3, backoff_factor: float = 1.5) -> list[dict]: \"\"\"Process a batch of items with retry logic and exponential backoff. Each item is validated, transformed, and persisted to the database. Failed items are retried up to retry_count times with exponential backoff. Items that fail all retries are logged and returned in the error list. The function is thread-safe and can be called from multiple workers. Parameters: items (list[dict]) — the items to process; batch_size (int) — number of items per database transaction; timeout (float) — per-item timeout in seconds; retry_count (int) — maximum retries per item; backoff_factor (float) — exponential backoff multiplier. Returns: list of successfully processed items.\"\"\" results = [] errors = [] for i in range(0, len(items), batch_size):     chunk = items[i:i + batch_size]     with db.transaction():         for item in chunk:             for attempt in range(retry_count):                 try:                     validated = validate(item)                     transformed = transform(validated)                     persisted = persist(transformed, timeout=timeout)                     results.append(persisted)                     break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors Furthermore,                   break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors",
    "category": "long"
  },
  {
    "id": "long_0123",
    "text": "Create a Terraform configuration for a highly available three-tier web application on AWS consisting of: a public-facing Application Load Balancer with WAF integration and DDoS protection via AWS Shield Standard; an Auto Scaling Group of EC2 t3.medium instances running in private subnets across three availability zones with a minimum of 3 and maximum of 20 instances; an RDS PostgreSQL 16 Multi-AZ cluster with read replicas in each availability zone, automated backups retained for 30 days, and encryption at rest using AWS KMS; ElastiCache Redis 7 cluster mode enabled with 3 shards and 2 replicas per shard for session storage and caching; an S3 bucket with versioning, cross-region replication to us-west-2, and lifecycle policies to transition objects to Glacier after 90 days; CloudFront distribution fronting the ALB with custom SSL certificate from ACM, security headers policy, and edge caching for static assets; VPC with CIDR 10.0.0.0/16 split into 6 subnets (3 public /24, 3 private /24) with NAT gateways in each AZ; Security Groups following principle of least privilege with separate groups for ALB, web tier, app tier, and database tier. Furthermore, rom ACM, security headers policy, and edge caching for static assets; VPC with CIDR 10.0.0.0/16 split into 6 subnets (3 public /24, 3 private /24) with NAT gateways in each AZ; Security Groups following principle of least privilege with separate groups for ALB, web tier, app tier, and database tier.",
    "category": "long"
  },
  {
    "id": "long_0124",
    "text": "#!/usr/bin/env bash set -euo pipefail IFS=$'\\n\\t' # Automated deployment script for mind-nerve v0.4.0 # Performs: build, test, security scan, docker build, push, deploy SCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\" REPO_ROOT=\"$(cd \"${SCRIPT_DIR}/..\" && pwd)\" VERSION=\"${1:-}\" ENVIRONMENT=\"${2:-staging}\" REGISTRY=\"ghcr.io/star-ga\" IMAGE=\"${REGISTRY}/mind-nerve\" if [[ -z \"${VERSION}\" ]]; then     echo 'ERROR: VERSION argument required' >&2     echo 'Usage: deploy.sh <version> [environment]' >&2     exit 1 fi echo \"==> Deploying mind-nerve ${VERSION} to ${ENVIRONMENT}\" cd \"${REPO_ROOT}\" echo '==> Running tests...' python -m pytest tests/ -x -q --tb=short echo '==> Running security scan...' bandit -r python/mind_nerve -ll echo '==> Building Docker image...' docker build --build-arg VERSION=\"${VERSION}\"              --build-arg BUILD_DATE=\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"              --build-arg VCS_REF=\"$(git rev-parse HEAD)\"              -t \"${IMAGE}:${VERSION}\"              -t \"${IMAGE}:latest\"              . echo '==> Scanning image for vulnerabilities...' trivy image --exit-code 1 --severity HIGH,CRITICAL \"${IMAGE}:${VERSION}\" echo '==> Pushing image...' docker push \"${IMAGE}:${VERSION}\" docker push \"${IMAGE}:latest\" echo '==> Deploying to Kubernetes...' kubectl set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\"  Furthermore, l set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\" ",
    "category": "long"
  },
  {
    "id": "long_0125",
    "text": "def process_batch(items: list[dict], batch_size: int = 32, timeout: float = 30.0, retry_count: int = 3, backoff_factor: float = 1.5) -> list[dict]: \"\"\"Process a batch of items with retry logic and exponential backoff. Each item is validated, transformed, and persisted to the database. Failed items are retried up to retry_count times with exponential backoff. Items that fail all retries are logged and returned in the error list. The function is thread-safe and can be called from multiple workers. Parameters: items (list[dict]) — the items to process; batch_size (int) — number of items per database transaction; timeout (float) — per-item timeout in seconds; retry_count (int) — maximum retries per item; backoff_factor (float) — exponential backoff multiplier. Returns: list of successfully processed items.\"\"\" results = [] errors = [] for i in range(0, len(items), batch_size):     chunk = items[i:i + batch_size]     with db.transaction():         for item in chunk:             for attempt in range(retry_count):                 try:                     validated = validate(item)                     transformed = transform(validated)                     persisted = persist(transformed, timeout=timeout)                     results.append(persisted)                     break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors Furthermore,                   break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors",
    "category": "long"
  },
  {
    "id": "long_0126",
    "text": "#!/usr/bin/env bash set -euo pipefail IFS=$'\\n\\t' # Automated deployment script for mind-nerve v0.4.0 # Performs: build, test, security scan, docker build, push, deploy SCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\" REPO_ROOT=\"$(cd \"${SCRIPT_DIR}/..\" && pwd)\" VERSION=\"${1:-}\" ENVIRONMENT=\"${2:-staging}\" REGISTRY=\"ghcr.io/star-ga\" IMAGE=\"${REGISTRY}/mind-nerve\" if [[ -z \"${VERSION}\" ]]; then     echo 'ERROR: VERSION argument required' >&2     echo 'Usage: deploy.sh <version> [environment]' >&2     exit 1 fi echo \"==> Deploying mind-nerve ${VERSION} to ${ENVIRONMENT}\" cd \"${REPO_ROOT}\" echo '==> Running tests...' python -m pytest tests/ -x -q --tb=short echo '==> Running security scan...' bandit -r python/mind_nerve -ll echo '==> Building Docker image...' docker build --build-arg VERSION=\"${VERSION}\"              --build-arg BUILD_DATE=\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"              --build-arg VCS_REF=\"$(git rev-parse HEAD)\"              -t \"${IMAGE}:${VERSION}\"              -t \"${IMAGE}:latest\"              . echo '==> Scanning image for vulnerabilities...' trivy image --exit-code 1 --severity HIGH,CRITICAL \"${IMAGE}:${VERSION}\" echo '==> Pushing image...' docker push \"${IMAGE}:${VERSION}\" docker push \"${IMAGE}:latest\" echo '==> Deploying to Kubernetes...' kubectl set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\"  Furthermore, l set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\" ",
    "category": "long"
  },
  {
    "id": "long_0127",
    "text": "Implement a complete CI/CD pipeline for a Python machine learning project that includes the following stages: code quality checks using ruff, black, isort, mypy, and bandit; unit tests with pytest and coverage reporting; integration tests against a real PostgreSQL database using testcontainers; model training on a held-out validation set with automatic hyperparameter tuning using Optuna; model evaluation comparing the new model against the currently deployed baseline on a standardized benchmark dataset of 10,000 samples; model registration in MLflow with artifact storage on S3; Docker image build with multi-stage build optimized for production size under 500MB; security scanning using trivy and snyk; deployment to Kubernetes staging environment with smoke tests; automated load testing using k6 to validate p99 latency under 200ms at 1000 concurrent users; canary deployment to production with 5% traffic split; monitoring of key metrics for 30 minutes before full rollout; automatic rollback if error rate exceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials. Furthermore, xceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials.",
    "category": "long"
  },
  {
    "id": "long_0128",
    "text": "from __future__ import annotations import asyncio import contextlib import dataclasses import hashlib import json import logging import os import time from collections.abc import AsyncGenerator, Generator from pathlib import Path from typing import Any, ClassVar import aiohttp import numpy as np logger = logging.getLogger(__name__) @dataclasses.dataclass(frozen=True, slots=True) class EncoderConfig:     model_hash: str     tokenizer_hash: str     hidden_dim: int = 384     num_heads: int = 12     num_layers: int = 12     max_seq_len: int = 256     window_size: int = 256     stride: int = 192     top_k: int = 5     backend: str = 'native'     runtime_dir: Path = dataclasses.field(         default_factory=lambda: Path.home() / '.local/share/mind-nerve/runtime')     SCHEMA_VERSION: ClassVar[int] = 1     def to_cache_key(self) -> str:         payload = json.dumps(dataclasses.asdict(self), sort_keys=True, default=str)         return hashlib.sha256(payload.encode()).hexdigest() class EncoderError(Exception):     pass class BackendNotAvailableError(EncoderError):     pass class EncoderTimeoutError(EncoderError):     pass @contextlib.asynccontextmanager async def managed_encoder(config: EncoderConfig) -> AsyncGenerator[AsyncEncoder, None]:     encoder = AsyncEncoder(config)     try:         await encoder.initialize()         yield encoder     finally:         await encoder.shutdown() class AsyncEncoder:     def __init__(self, config: EncoderConfig) -> None:         self._config = config         self._session: aiohttp.ClientSession | None = None         self._initialized = False         self._request_count = 0         self._error_count = 0         self._total_latency_ms = 0.0     async def initialize(self) -> None:         connector = aiohttp.TCPConnector(limit=100, limit_per_host=20)         self._session = aiohttp.ClientSession(connector=connector)         self._initialized = True         logger.info('AsyncEncoder initialized: backend=%s', self._config.backend)     async def encode(self, query: str, timeout: float = 5.0) -> np.ndarray:         if not self._initialized:             raise EncoderError('Encoder not initialized')         start = time.perf_counter()         try:             result = await asyncio.wait_for(self._encode_impl(query), timeout=timeout)             self._total_latency_ms += (time.perf_counter() - start) * 1000             self._request_count += 1             return result         except asyncio.TimeoutError as exc:             self._error_count += 1             raise EncoderTimeoutError(f'Encode timed out after {timeout}s') from exc     async def _encode_impl(self, query: str) -> np.ndarray:         raise NotImplementedError     async def shutdown(self) -> None:         if self._session:             await self._session.close()         self._initialized = False     @property     def stats(self) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) }  Furthermore, elf) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) } ",
    "category": "long"
  },
  {
    "id": "long_0129",
    "text": "package encoder import (     'context'     'crypto/sha256'     'encoding/binary'     'fmt'     'math'     'sync' ) const (     HiddenDim   = 384     NumHeads    = 12     HeadDim     = 32     Q16Scale    = 65536.0     LayerNormEps = 1e-12 ) type Q16 int32 func Float32ToQ16(f float32) Q16 {     scaled := float64(f) * Q16Scale     if scaled > math.MaxInt32 { return Q16(math.MaxInt32) }     if scaled < math.MinInt32 { return Q16(math.MinInt32) }     return Q16(math.Round(scaled)) } func (q Q16) ToFloat32() float32 { return float32(float64(q) / Q16Scale) } type Matrix struct { Data []Q16; Rows, Cols int } func NewMatrix(rows, cols int) *Matrix {     return &Matrix{Data: make([]Q16, rows*cols), Rows: rows, Cols: cols} } func (m *Matrix) At(row, col int) Q16 { return m.Data[row*m.Cols+col] } func (m *Matrix) Set(row, col int, v Q16) { m.Data[row*m.Cols+col] = v } func Matmul(a, b *Matrix) *Matrix {     if a.Cols != b.Rows { panic('dimension mismatch') }     c := NewMatrix(a.Rows, b.Cols)     var wg sync.WaitGroup     for i := 0; i < a.Rows; i++ {         wg.Add(1)         go func(row int) {             defer wg.Done()             for j := 0; j < b.Cols; j++ {                 var acc int64                 for k := 0; k < a.Cols; k++ {                     acc += int64(a.At(row, k)) * int64(b.At(k, j)) }                 c.Set(row, j, Q16(acc>>16)) } }(i) }     wg.Wait()     return c } func LayerNorm(x *Matrix, gamma, beta []Q16) *Matrix {     out := NewMatrix(x.Rows, x.Cols)     for i := 0; i < x.Rows; i++ {         var sum int64         for j := 0; j < x.Cols; j++ { sum += int64(x.At(i, j)) }         mean := Q16(sum / int64(x.Cols))         var varAcc int64         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             varAcc += d * d }         variance := float64(varAcc) / float64(x.Cols) / Q16Scale / Q16Scale         invStd := 1.0 / math.Sqrt(variance + LayerNormEps)         invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out }  Furthermore,     invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out } ",
    "category": "long"
  },
  {
    "id": "long_0130",
    "text": "Conduct a comprehensive security review of the following authentication implementation and identify all vulnerabilities with CVSS scores: The system uses JWT tokens with HS256 signing using a hardcoded secret key 'super_secret_key_12345' stored directly in the application code. Tokens have a 30-day expiry with no refresh mechanism. The token payload includes the user_id, role, email, and a boolean is_admin flag that is trusted by all downstream services without verification. Password hashing uses MD5 with a 4-character salt. The login endpoint does not implement rate limiting. Session tokens are stored in localStorage instead of httpOnly cookies. The password reset flow sends a 6-digit numeric OTP via email with a 24-hour expiry and no attempt limiting. The API does not validate Content-Type headers on POST requests. SQL queries for user lookup use string concatenation without parameterization. Cross-origin requests are allowed from all origins via Access-Control-Allow-Origin: *. The application runs as root in production containers. Debug endpoints exposing stack traces and environment variables are accessible without authentication in production. Please provide remediation recommendations for each vulnerability ordered by severity. Furthermore, igins via Access-Control-Allow-Origin: *. The application runs as root in production containers. Debug endpoints exposing stack traces and environment variables are accessible without authentication in production. Please provide remediation recommendations for each vulnerability ordered by severity.",
    "category": "long"
  },
  {
    "id": "long_0131",
    "text": "apiVersion: apps/v1 kind: Deployment metadata:   name: mind-nerve-encoder   namespace: production   labels:     app: mind-nerve     component: encoder     version: v0.4.0 spec:   replicas: 3   selector:     matchLabels:       app: mind-nerve       component: encoder   strategy:     type: RollingUpdate     rollingUpdate:       maxSurge: 1       maxUnavailable: 0   template:     metadata:       labels:         app: mind-nerve         component: encoder         version: v0.4.0       annotations:         prometheus.io/scrape: 'true'         prometheus.io/port: '9090'         prometheus.io/path: '/metrics'     spec:       securityContext:         runAsNonRoot: true         runAsUser: 1000         fsGroup: 1000       containers:       - name: encoder         image: ghcr.io/star-ga/mind-nerve:v0.4.0         ports:         - containerPort: 8080         - containerPort: 9090         resources:           requests:             cpu: 500m             memory: 512Mi           limits:             cpu: 2000m             memory: 2Gi         readinessProbe:           httpGet:             path: /health/ready             port: 8080           initialDelaySeconds: 10           periodSeconds: 5         livenessProbe:           httpGet:             path: /health/live             port: 8080           initialDelaySeconds: 30           periodSeconds: 10         env:         - name: MIND_NERVE_BACKEND           value: native         - name: MIND_NERVE_RUNTIME_DIR           value: /runtime         volumeMounts:         - name: runtime           mountPath: /runtime           readOnly: true       volumes:       - name: runtime         configMap:           name: mind-nerve-runtime Furthermore,  MIND_NERVE_BACKEND           value: native         - name: MIND_NERVE_RUNTIME_DIR           value: /runtime         volumeMounts:         - name: runtime           mountPath: /runtime           readOnly: true       volumes:       - name: runtime         configMap:           name: mind-nerve-runtime",
    "category": "long"
  },
  {
    "id": "long_0132",
    "text": "Design and implement a fault-tolerant data pipeline that ingests streaming events from Apache Kafka topics (user.clicked, user.purchased, user.viewed, user.searched) at a rate of 500,000 events per second, applies real-time feature engineering including session reconstruction with a 30-minute session timeout, rolling window aggregations over 1-minute, 5-minute, and 1-hour windows, user-level and item-level statistics, and categorical feature encoding using frequency-adaptive target encoding with Laplace smoothing; writes the enriched feature vectors to a Redis Feature Store with a 24-hour TTL for online serving and to Apache Iceberg tables on S3 for offline training; handles late arriving events up to 5 minutes behind watermark using Apache Flink's event-time processing with allowed lateness; implements exactly-once semantics via Kafka transactions and Flink checkpointing to S3 every 60 seconds; provides a Grafana dashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint. Furthermore, ashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint.",
    "category": "long"
  },
  {
    "id": "long_0133",
    "text": "Implement a complete CI/CD pipeline for a Python machine learning project that includes the following stages: code quality checks using ruff, black, isort, mypy, and bandit; unit tests with pytest and coverage reporting; integration tests against a real PostgreSQL database using testcontainers; model training on a held-out validation set with automatic hyperparameter tuning using Optuna; model evaluation comparing the new model against the currently deployed baseline on a standardized benchmark dataset of 10,000 samples; model registration in MLflow with artifact storage on S3; Docker image build with multi-stage build optimized for production size under 500MB; security scanning using trivy and snyk; deployment to Kubernetes staging environment with smoke tests; automated load testing using k6 to validate p99 latency under 200ms at 1000 concurrent users; canary deployment to production with 5% traffic split; monitoring of key metrics for 30 minutes before full rollout; automatic rollback if error rate exceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials. Furthermore, xceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials.",
    "category": "long"
  },
  {
    "id": "long_0134",
    "text": "Compare and contrast the following approaches to distributed consensus and explain when to use each one in the context of a globally distributed database system: Paxos (Classic Paxos, Multi-Paxos, and Fast Paxos variants), Raft (with and without pre-vote extension), Zab (ZooKeeper Atomic Broadcast), PBFT (Practical Byzantine Fault Tolerance), HotStuff (and its chained variant used in LibraBFT/DiemBFT), Tendermint/CometBFT, and Stellar Consensus Protocol. For each algorithm, discuss: the fault model (crash fault tolerant vs byzantine fault tolerant), the minimum number of nodes required for fault tolerance (f+1 vs 2f+1 vs 3f+1), the leader election mechanism and its impact on availability during leader failures, the message complexity per consensus round, the latency in terms of network round trips, the throughput in transactions per second for typical LAN and WAN deployments, the behavior under network partitions (CAP theorem position), the implementation complexity and available production-grade open-source implementations, and any known correctness bugs or safety violations discovered in production deployments. Then recommend the appropriate algorithm for a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants. Furthermore, or a financial trading system requiring sub-millisecond latency with strong consistency guarantees, a globally distributed key-value store spanning 5 geographic regions with eventual consistency acceptable, and an open blockchain network requiring Byzantine fault tolerance with unknown participants.",
    "category": "long"
  },
  {
    "id": "long_0135",
    "text": "Implement a complete CI/CD pipeline for a Python machine learning project that includes the following stages: code quality checks using ruff, black, isort, mypy, and bandit; unit tests with pytest and coverage reporting; integration tests against a real PostgreSQL database using testcontainers; model training on a held-out validation set with automatic hyperparameter tuning using Optuna; model evaluation comparing the new model against the currently deployed baseline on a standardized benchmark dataset of 10,000 samples; model registration in MLflow with artifact storage on S3; Docker image build with multi-stage build optimized for production size under 500MB; security scanning using trivy and snyk; deployment to Kubernetes staging environment with smoke tests; automated load testing using k6 to validate p99 latency under 200ms at 1000 concurrent users; canary deployment to production with 5% traffic split; monitoring of key metrics for 30 minutes before full rollout; automatic rollback if error rate exceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials. Furthermore, xceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials.",
    "category": "long"
  },
  {
    "id": "long_0136",
    "text": "def process_batch(items: list[dict], batch_size: int = 32, timeout: float = 30.0, retry_count: int = 3, backoff_factor: float = 1.5) -> list[dict]: \"\"\"Process a batch of items with retry logic and exponential backoff. Each item is validated, transformed, and persisted to the database. Failed items are retried up to retry_count times with exponential backoff. Items that fail all retries are logged and returned in the error list. The function is thread-safe and can be called from multiple workers. Parameters: items (list[dict]) — the items to process; batch_size (int) — number of items per database transaction; timeout (float) — per-item timeout in seconds; retry_count (int) — maximum retries per item; backoff_factor (float) — exponential backoff multiplier. Returns: list of successfully processed items.\"\"\" results = [] errors = [] for i in range(0, len(items), batch_size):     chunk = items[i:i + batch_size]     with db.transaction():         for item in chunk:             for attempt in range(retry_count):                 try:                     validated = validate(item)                     transformed = transform(validated)                     persisted = persist(transformed, timeout=timeout)                     results.append(persisted)                     break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors Furthermore,                   break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors",
    "category": "long"
  },
  {
    "id": "long_0137",
    "text": "import { createServer } from 'http'; import { WebSocketServer, WebSocket } from 'ws'; import { EventEmitter } from 'events'; interface ClientMessage { type: 'subscribe' | 'unsubscribe' | 'publish';   channel: string; payload?: unknown; clientId: string; } interface ServerMessage { type: 'data' | 'error' | 'ack';   channel?: string; payload?: unknown; timestamp: number; } class PubSubServer extends EventEmitter {   private channels = new Map<string, Set<WebSocket>>();   private clientChannels = new Map<WebSocket, Set<string>>();   constructor(private readonly port: number) { super(); }   subscribe(ws: WebSocket, channel: string): void {     if (!this.channels.has(channel)) this.channels.set(channel, new Set());     this.channels.get(channel)!.add(ws);     if (!this.clientChannels.has(ws)) this.clientChannels.set(ws, new Set());     this.clientChannels.get(ws)!.add(channel); }   unsubscribe(ws: WebSocket, channel: string): void {     this.channels.get(channel)?.delete(ws);     this.clientChannels.get(ws)?.delete(channel); }   publish(channel: string, payload: unknown): void {     const msg: ServerMessage = { type: 'data', channel, payload,       timestamp: Date.now() };     const data = JSON.stringify(msg);     this.channels.get(channel)?.forEach(client => {       if (client.readyState === WebSocket.OPEN) client.send(data); }); }   cleanup(ws: WebSocket): void {     this.clientChannels.get(ws)?.forEach(ch => this.unsubscribe(ws, ch));     this.clientChannels.delete(ws); }   listen(): void {     const server = createServer();     const wss = new WebSocketServer({ server });     wss.on('connection', ws => {       ws.on('message', raw => {         const msg: ClientMessage = JSON.parse(raw.toString());         switch (msg.type) {           case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } } Furthermore,         case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } }",
    "category": "long"
  },
  {
    "id": "long_0138",
    "text": "Conduct a comprehensive security review of the following authentication implementation and identify all vulnerabilities with CVSS scores: The system uses JWT tokens with HS256 signing using a hardcoded secret key 'super_secret_key_12345' stored directly in the application code. Tokens have a 30-day expiry with no refresh mechanism. The token payload includes the user_id, role, email, and a boolean is_admin flag that is trusted by all downstream services without verification. Password hashing uses MD5 with a 4-character salt. The login endpoint does not implement rate limiting. Session tokens are stored in localStorage instead of httpOnly cookies. The password reset flow sends a 6-digit numeric OTP via email with a 24-hour expiry and no attempt limiting. The API does not validate Content-Type headers on POST requests. SQL queries for user lookup use string concatenation without parameterization. Cross-origin requests are allowed from all origins via Access-Control-Allow-Origin: *. The application runs as root in production containers. Debug endpoints exposing stack traces and environment variables are accessible without authentication in production. Please provide remediation recommendations for each vulnerability ordered by severity. Furthermore, igins via Access-Control-Allow-Origin: *. The application runs as root in production containers. Debug endpoints exposing stack traces and environment variables are accessible without authentication in production. Please provide remediation recommendations for each vulnerability ordered by severity.",
    "category": "long"
  },
  {
    "id": "long_0139",
    "text": "Architectural Decision Record: Switching from synchronous REST API calls to an event-driven architecture using Apache Kafka for the order processing pipeline. Context: The current synchronous REST-based order processing system suffers from cascading failures when downstream services are slow or unavailable. Peak load of 10,000 orders per minute causes timeouts in the payment service which propagates back to the API gateway causing HTTP 503 errors visible to customers. The average order processing time is 2.3 seconds due to sequential API calls to inventory, payment, notification, and shipping services. Decision: Implement event-driven architecture using Apache Kafka as the message broker. Each service publishes domain events and subscribes to relevant topics. Order creation publishes to order.created topic. Payment service subscribes to order.created and publishes payment.processed or payment.failed. Inventory service subscribes to payment.processed and publishes inventory.reserved or inventory.insufficient. The API immediately returns 202 Accepted with an order ID. Customers poll a read model or receive webhook notifications when order status changes. Consequences: Orders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully. Furthermore, ders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully.",
    "category": "long"
  },
  {
    "id": "long_0140",
    "text": "package encoder import (     'context'     'crypto/sha256'     'encoding/binary'     'fmt'     'math'     'sync' ) const (     HiddenDim   = 384     NumHeads    = 12     HeadDim     = 32     Q16Scale    = 65536.0     LayerNormEps = 1e-12 ) type Q16 int32 func Float32ToQ16(f float32) Q16 {     scaled := float64(f) * Q16Scale     if scaled > math.MaxInt32 { return Q16(math.MaxInt32) }     if scaled < math.MinInt32 { return Q16(math.MinInt32) }     return Q16(math.Round(scaled)) } func (q Q16) ToFloat32() float32 { return float32(float64(q) / Q16Scale) } type Matrix struct { Data []Q16; Rows, Cols int } func NewMatrix(rows, cols int) *Matrix {     return &Matrix{Data: make([]Q16, rows*cols), Rows: rows, Cols: cols} } func (m *Matrix) At(row, col int) Q16 { return m.Data[row*m.Cols+col] } func (m *Matrix) Set(row, col int, v Q16) { m.Data[row*m.Cols+col] = v } func Matmul(a, b *Matrix) *Matrix {     if a.Cols != b.Rows { panic('dimension mismatch') }     c := NewMatrix(a.Rows, b.Cols)     var wg sync.WaitGroup     for i := 0; i < a.Rows; i++ {         wg.Add(1)         go func(row int) {             defer wg.Done()             for j := 0; j < b.Cols; j++ {                 var acc int64                 for k := 0; k < a.Cols; k++ {                     acc += int64(a.At(row, k)) * int64(b.At(k, j)) }                 c.Set(row, j, Q16(acc>>16)) } }(i) }     wg.Wait()     return c } func LayerNorm(x *Matrix, gamma, beta []Q16) *Matrix {     out := NewMatrix(x.Rows, x.Cols)     for i := 0; i < x.Rows; i++ {         var sum int64         for j := 0; j < x.Cols; j++ { sum += int64(x.At(i, j)) }         mean := Q16(sum / int64(x.Cols))         var varAcc int64         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             varAcc += d * d }         variance := float64(varAcc) / float64(x.Cols) / Q16Scale / Q16Scale         invStd := 1.0 / math.Sqrt(variance + LayerNormEps)         invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out }  Furthermore,     invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out } ",
    "category": "long"
  },
  {
    "id": "long_0141",
    "text": "class DistributedEmbeddingCache: \"\"\"Thread-safe distributed embedding cache backed by Redis with LRU eviction, TTL support, and automatic serialization of numpy float32 arrays to Q16.16 fixed-point representation. The cache key is SHA-256(model_hash + tokenizer_hash + query_text). Cache entries expire after ttl_seconds (default 3600). The cache uses a two-level hierarchy: L1 is an in-process LRU (maxsize=1024) and L2 is the shared Redis cluster. Write-back from L1 to L2 is asynchronous via a background thread. The serialize method converts float32[384] to Q16.16 i32[384] bytes with deterministic rounding for cross-arch reproducibility.\"\"\" def __init__(self, redis_url: str, ttl_seconds: int = 3600,              l1_maxsize: int = 1024) -> None:     self._redis = redis.Redis.from_url(redis_url)     self._ttl = ttl_seconds     self._l1 = functools.lru_cache(maxsize=l1_maxsize)(self._get_l2)     self._write_queue: queue.Queue = queue.Queue(maxsize=4096)     self._writer = threading.Thread(target=self._write_worker, daemon=True)     self._writer.start() def _cache_key(self, model_hash: str, tokenizer_hash: str, query: str) -> str:     payload = f'{model_hash}:{tokenizer_hash}:{query}'.encode('utf-8')     return 'emb:' + hashlib.sha256(payload).hexdigest() def get(self, model_hash: str, tokenizer_hash: str, query: str) -> np.ndarray | None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._redis.get(key)     if raw is None:         return None     return self._deserialize(raw) def put(self, model_hash: str, tokenizer_hash: str, query: str,         embedding: np.ndarray) -> None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._serialize(embedding)     self._redis.setex(key, self._ttl, raw) def _serialize(self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0) Furthermore, (self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0)",
    "category": "long"
  },
  {
    "id": "long_0142",
    "text": "Conduct a comprehensive security review of the following authentication implementation and identify all vulnerabilities with CVSS scores: The system uses JWT tokens with HS256 signing using a hardcoded secret key 'super_secret_key_12345' stored directly in the application code. Tokens have a 30-day expiry with no refresh mechanism. The token payload includes the user_id, role, email, and a boolean is_admin flag that is trusted by all downstream services without verification. Password hashing uses MD5 with a 4-character salt. The login endpoint does not implement rate limiting. Session tokens are stored in localStorage instead of httpOnly cookies. The password reset flow sends a 6-digit numeric OTP via email with a 24-hour expiry and no attempt limiting. The API does not validate Content-Type headers on POST requests. SQL queries for user lookup use string concatenation without parameterization. Cross-origin requests are allowed from all origins via Access-Control-Allow-Origin: *. The application runs as root in production containers. Debug endpoints exposing stack traces and environment variables are accessible without authentication in production. Please provide remediation recommendations for each vulnerability ordered by severity. Furthermore, igins via Access-Control-Allow-Origin: *. The application runs as root in production containers. Debug endpoints exposing stack traces and environment variables are accessible without authentication in production. Please provide remediation recommendations for each vulnerability ordered by severity.",
    "category": "long"
  },
  {
    "id": "long_0143",
    "text": "Write a PostgreSQL query to find all users who have placed more than 5 orders in the last 30 days, where each order total exceeds $100, and who have not been flagged for fraud, grouped by user_id with aggregated statistics including total order count, total spend, average order value, first order date, last order date, and the names of the top 3 most frequently ordered products for each user, sorted by total spend descending, with pagination support using cursor-based pagination on user_id for efficient handling of large result sets. The query should use CTEs for readability, include appropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring. Furthermore, ppropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring.",
    "category": "long"
  },
  {
    "id": "long_0144",
    "text": "Architectural Decision Record: Switching from synchronous REST API calls to an event-driven architecture using Apache Kafka for the order processing pipeline. Context: The current synchronous REST-based order processing system suffers from cascading failures when downstream services are slow or unavailable. Peak load of 10,000 orders per minute causes timeouts in the payment service which propagates back to the API gateway causing HTTP 503 errors visible to customers. The average order processing time is 2.3 seconds due to sequential API calls to inventory, payment, notification, and shipping services. Decision: Implement event-driven architecture using Apache Kafka as the message broker. Each service publishes domain events and subscribes to relevant topics. Order creation publishes to order.created topic. Payment service subscribes to order.created and publishes payment.processed or payment.failed. Inventory service subscribes to payment.processed and publishes inventory.reserved or inventory.insufficient. The API immediately returns 202 Accepted with an order ID. Customers poll a read model or receive webhook notifications when order status changes. Consequences: Orders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully. Furthermore, ders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully.",
    "category": "long"
  },
  {
    "id": "long_0145",
    "text": "class DistributedEmbeddingCache: \"\"\"Thread-safe distributed embedding cache backed by Redis with LRU eviction, TTL support, and automatic serialization of numpy float32 arrays to Q16.16 fixed-point representation. The cache key is SHA-256(model_hash + tokenizer_hash + query_text). Cache entries expire after ttl_seconds (default 3600). The cache uses a two-level hierarchy: L1 is an in-process LRU (maxsize=1024) and L2 is the shared Redis cluster. Write-back from L1 to L2 is asynchronous via a background thread. The serialize method converts float32[384] to Q16.16 i32[384] bytes with deterministic rounding for cross-arch reproducibility.\"\"\" def __init__(self, redis_url: str, ttl_seconds: int = 3600,              l1_maxsize: int = 1024) -> None:     self._redis = redis.Redis.from_url(redis_url)     self._ttl = ttl_seconds     self._l1 = functools.lru_cache(maxsize=l1_maxsize)(self._get_l2)     self._write_queue: queue.Queue = queue.Queue(maxsize=4096)     self._writer = threading.Thread(target=self._write_worker, daemon=True)     self._writer.start() def _cache_key(self, model_hash: str, tokenizer_hash: str, query: str) -> str:     payload = f'{model_hash}:{tokenizer_hash}:{query}'.encode('utf-8')     return 'emb:' + hashlib.sha256(payload).hexdigest() def get(self, model_hash: str, tokenizer_hash: str, query: str) -> np.ndarray | None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._redis.get(key)     if raw is None:         return None     return self._deserialize(raw) def put(self, model_hash: str, tokenizer_hash: str, query: str,         embedding: np.ndarray) -> None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._serialize(embedding)     self._redis.setex(key, self._ttl, raw) def _serialize(self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0) Furthermore, (self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0)",
    "category": "long"
  },
  {
    "id": "long_0146",
    "text": "from __future__ import annotations import asyncio import contextlib import dataclasses import hashlib import json import logging import os import time from collections.abc import AsyncGenerator, Generator from pathlib import Path from typing import Any, ClassVar import aiohttp import numpy as np logger = logging.getLogger(__name__) @dataclasses.dataclass(frozen=True, slots=True) class EncoderConfig:     model_hash: str     tokenizer_hash: str     hidden_dim: int = 384     num_heads: int = 12     num_layers: int = 12     max_seq_len: int = 256     window_size: int = 256     stride: int = 192     top_k: int = 5     backend: str = 'native'     runtime_dir: Path = dataclasses.field(         default_factory=lambda: Path.home() / '.local/share/mind-nerve/runtime')     SCHEMA_VERSION: ClassVar[int] = 1     def to_cache_key(self) -> str:         payload = json.dumps(dataclasses.asdict(self), sort_keys=True, default=str)         return hashlib.sha256(payload.encode()).hexdigest() class EncoderError(Exception):     pass class BackendNotAvailableError(EncoderError):     pass class EncoderTimeoutError(EncoderError):     pass @contextlib.asynccontextmanager async def managed_encoder(config: EncoderConfig) -> AsyncGenerator[AsyncEncoder, None]:     encoder = AsyncEncoder(config)     try:         await encoder.initialize()         yield encoder     finally:         await encoder.shutdown() class AsyncEncoder:     def __init__(self, config: EncoderConfig) -> None:         self._config = config         self._session: aiohttp.ClientSession | None = None         self._initialized = False         self._request_count = 0         self._error_count = 0         self._total_latency_ms = 0.0     async def initialize(self) -> None:         connector = aiohttp.TCPConnector(limit=100, limit_per_host=20)         self._session = aiohttp.ClientSession(connector=connector)         self._initialized = True         logger.info('AsyncEncoder initialized: backend=%s', self._config.backend)     async def encode(self, query: str, timeout: float = 5.0) -> np.ndarray:         if not self._initialized:             raise EncoderError('Encoder not initialized')         start = time.perf_counter()         try:             result = await asyncio.wait_for(self._encode_impl(query), timeout=timeout)             self._total_latency_ms += (time.perf_counter() - start) * 1000             self._request_count += 1             return result         except asyncio.TimeoutError as exc:             self._error_count += 1             raise EncoderTimeoutError(f'Encode timed out after {timeout}s') from exc     async def _encode_impl(self, query: str) -> np.ndarray:         raise NotImplementedError     async def shutdown(self) -> None:         if self._session:             await self._session.close()         self._initialized = False     @property     def stats(self) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) }  Furthermore, elf) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) } ",
    "category": "long"
  },
  {
    "id": "long_0147",
    "text": "package encoder import (     'context'     'crypto/sha256'     'encoding/binary'     'fmt'     'math'     'sync' ) const (     HiddenDim   = 384     NumHeads    = 12     HeadDim     = 32     Q16Scale    = 65536.0     LayerNormEps = 1e-12 ) type Q16 int32 func Float32ToQ16(f float32) Q16 {     scaled := float64(f) * Q16Scale     if scaled > math.MaxInt32 { return Q16(math.MaxInt32) }     if scaled < math.MinInt32 { return Q16(math.MinInt32) }     return Q16(math.Round(scaled)) } func (q Q16) ToFloat32() float32 { return float32(float64(q) / Q16Scale) } type Matrix struct { Data []Q16; Rows, Cols int } func NewMatrix(rows, cols int) *Matrix {     return &Matrix{Data: make([]Q16, rows*cols), Rows: rows, Cols: cols} } func (m *Matrix) At(row, col int) Q16 { return m.Data[row*m.Cols+col] } func (m *Matrix) Set(row, col int, v Q16) { m.Data[row*m.Cols+col] = v } func Matmul(a, b *Matrix) *Matrix {     if a.Cols != b.Rows { panic('dimension mismatch') }     c := NewMatrix(a.Rows, b.Cols)     var wg sync.WaitGroup     for i := 0; i < a.Rows; i++ {         wg.Add(1)         go func(row int) {             defer wg.Done()             for j := 0; j < b.Cols; j++ {                 var acc int64                 for k := 0; k < a.Cols; k++ {                     acc += int64(a.At(row, k)) * int64(b.At(k, j)) }                 c.Set(row, j, Q16(acc>>16)) } }(i) }     wg.Wait()     return c } func LayerNorm(x *Matrix, gamma, beta []Q16) *Matrix {     out := NewMatrix(x.Rows, x.Cols)     for i := 0; i < x.Rows; i++ {         var sum int64         for j := 0; j < x.Cols; j++ { sum += int64(x.At(i, j)) }         mean := Q16(sum / int64(x.Cols))         var varAcc int64         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             varAcc += d * d }         variance := float64(varAcc) / float64(x.Cols) / Q16Scale / Q16Scale         invStd := 1.0 / math.Sqrt(variance + LayerNormEps)         invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out }  Furthermore,     invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out } ",
    "category": "long"
  },
  {
    "id": "long_0148",
    "text": "import { createServer } from 'http'; import { WebSocketServer, WebSocket } from 'ws'; import { EventEmitter } from 'events'; interface ClientMessage { type: 'subscribe' | 'unsubscribe' | 'publish';   channel: string; payload?: unknown; clientId: string; } interface ServerMessage { type: 'data' | 'error' | 'ack';   channel?: string; payload?: unknown; timestamp: number; } class PubSubServer extends EventEmitter {   private channels = new Map<string, Set<WebSocket>>();   private clientChannels = new Map<WebSocket, Set<string>>();   constructor(private readonly port: number) { super(); }   subscribe(ws: WebSocket, channel: string): void {     if (!this.channels.has(channel)) this.channels.set(channel, new Set());     this.channels.get(channel)!.add(ws);     if (!this.clientChannels.has(ws)) this.clientChannels.set(ws, new Set());     this.clientChannels.get(ws)!.add(channel); }   unsubscribe(ws: WebSocket, channel: string): void {     this.channels.get(channel)?.delete(ws);     this.clientChannels.get(ws)?.delete(channel); }   publish(channel: string, payload: unknown): void {     const msg: ServerMessage = { type: 'data', channel, payload,       timestamp: Date.now() };     const data = JSON.stringify(msg);     this.channels.get(channel)?.forEach(client => {       if (client.readyState === WebSocket.OPEN) client.send(data); }); }   cleanup(ws: WebSocket): void {     this.clientChannels.get(ws)?.forEach(ch => this.unsubscribe(ws, ch));     this.clientChannels.delete(ws); }   listen(): void {     const server = createServer();     const wss = new WebSocketServer({ server });     wss.on('connection', ws => {       ws.on('message', raw => {         const msg: ClientMessage = JSON.parse(raw.toString());         switch (msg.type) {           case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } } Furthermore,         case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } }",
    "category": "long"
  },
  {
    "id": "long_0149",
    "text": "def process_batch(items: list[dict], batch_size: int = 32, timeout: float = 30.0, retry_count: int = 3, backoff_factor: float = 1.5) -> list[dict]: \"\"\"Process a batch of items with retry logic and exponential backoff. Each item is validated, transformed, and persisted to the database. Failed items are retried up to retry_count times with exponential backoff. Items that fail all retries are logged and returned in the error list. The function is thread-safe and can be called from multiple workers. Parameters: items (list[dict]) — the items to process; batch_size (int) — number of items per database transaction; timeout (float) — per-item timeout in seconds; retry_count (int) — maximum retries per item; backoff_factor (float) — exponential backoff multiplier. Returns: list of successfully processed items.\"\"\" results = [] errors = [] for i in range(0, len(items), batch_size):     chunk = items[i:i + batch_size]     with db.transaction():         for item in chunk:             for attempt in range(retry_count):                 try:                     validated = validate(item)                     transformed = transform(validated)                     persisted = persist(transformed, timeout=timeout)                     results.append(persisted)                     break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors Furthermore,                   break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors",
    "category": "long"
  },
  {
    "id": "long_0150",
    "text": "Create a Terraform configuration for a highly available three-tier web application on AWS consisting of: a public-facing Application Load Balancer with WAF integration and DDoS protection via AWS Shield Standard; an Auto Scaling Group of EC2 t3.medium instances running in private subnets across three availability zones with a minimum of 3 and maximum of 20 instances; an RDS PostgreSQL 16 Multi-AZ cluster with read replicas in each availability zone, automated backups retained for 30 days, and encryption at rest using AWS KMS; ElastiCache Redis 7 cluster mode enabled with 3 shards and 2 replicas per shard for session storage and caching; an S3 bucket with versioning, cross-region replication to us-west-2, and lifecycle policies to transition objects to Glacier after 90 days; CloudFront distribution fronting the ALB with custom SSL certificate from ACM, security headers policy, and edge caching for static assets; VPC with CIDR 10.0.0.0/16 split into 6 subnets (3 public /24, 3 private /24) with NAT gateways in each AZ; Security Groups following principle of least privilege with separate groups for ALB, web tier, app tier, and database tier. Furthermore, rom ACM, security headers policy, and edge caching for static assets; VPC with CIDR 10.0.0.0/16 split into 6 subnets (3 public /24, 3 private /24) with NAT gateways in each AZ; Security Groups following principle of least privilege with separate groups for ALB, web tier, app tier, and database tier.",
    "category": "long"
  },
  {
    "id": "long_0151",
    "text": "def process_batch(items: list[dict], batch_size: int = 32, timeout: float = 30.0, retry_count: int = 3, backoff_factor: float = 1.5) -> list[dict]: \"\"\"Process a batch of items with retry logic and exponential backoff. Each item is validated, transformed, and persisted to the database. Failed items are retried up to retry_count times with exponential backoff. Items that fail all retries are logged and returned in the error list. The function is thread-safe and can be called from multiple workers. Parameters: items (list[dict]) — the items to process; batch_size (int) — number of items per database transaction; timeout (float) — per-item timeout in seconds; retry_count (int) — maximum retries per item; backoff_factor (float) — exponential backoff multiplier. Returns: list of successfully processed items.\"\"\" results = [] errors = [] for i in range(0, len(items), batch_size):     chunk = items[i:i + batch_size]     with db.transaction():         for item in chunk:             for attempt in range(retry_count):                 try:                     validated = validate(item)                     transformed = transform(validated)                     persisted = persist(transformed, timeout=timeout)                     results.append(persisted)                     break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors Furthermore,                   break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors",
    "category": "long"
  },
  {
    "id": "long_0152",
    "text": "openapi: 3.1.0 info:   title: mind-nerve Encoding API   version: 0.4.0   description: |     Q16.16 native BERT encoder for intent classification and tool routing.     Encodes query strings into 384-dimensional L2-normalized embeddings     and returns top-K routing candidates from a precomputed catalog.     The encode endpoint accepts a JSON body with a query string and     optional parameters for top_k, backend selection, and runtime override.     All embeddings are returned as Q16.16 fixed-point integers to ensure     bit-identical results across x86_64 CPU backends. CUDA and ARM backends     are available but require explicit opt-in via the backend parameter.     Rate limiting is enforced at 1000 requests per minute per API key.     The catalog endpoint returns metadata about the currently loaded     routing catalog including version, size, and hash for cache validation. paths:   /v1/encode:     post:       operationId: encodeQuery       summary: Encode a query string and return top-K routing candidates       requestBody:         required: true         content:           application/json:             schema:               type: object               required: [query]               properties:                 query:                   type: string                   minLength: 1                   maxLength: 2048                   description: The query string to encode and route                 top_k:                   type: integer                   minimum: 1                   maximum: 100                   default: 5                 backend:                   type: string                   enum: [native, pytorch, cuda]                   default: native       responses:         '200':           description: Successful encoding and routing           content:             application/json:               schema:                 $ref: '#/components/schemas/RouteResult'         '400':           description: Invalid request (query too long, invalid backend)         '429':           description: Rate limit exceeded         '503':           description: Backend unavailable Furthermore, application/json:               schema:                 $ref: '#/components/schemas/RouteResult'         '400':           description: Invalid request (query too long, invalid backend)         '429':           description: Rate limit exceeded         '503':           description: Backend unavailable",
    "category": "long"
  },
  {
    "id": "long_0153",
    "text": "import { createServer } from 'http'; import { WebSocketServer, WebSocket } from 'ws'; import { EventEmitter } from 'events'; interface ClientMessage { type: 'subscribe' | 'unsubscribe' | 'publish';   channel: string; payload?: unknown; clientId: string; } interface ServerMessage { type: 'data' | 'error' | 'ack';   channel?: string; payload?: unknown; timestamp: number; } class PubSubServer extends EventEmitter {   private channels = new Map<string, Set<WebSocket>>();   private clientChannels = new Map<WebSocket, Set<string>>();   constructor(private readonly port: number) { super(); }   subscribe(ws: WebSocket, channel: string): void {     if (!this.channels.has(channel)) this.channels.set(channel, new Set());     this.channels.get(channel)!.add(ws);     if (!this.clientChannels.has(ws)) this.clientChannels.set(ws, new Set());     this.clientChannels.get(ws)!.add(channel); }   unsubscribe(ws: WebSocket, channel: string): void {     this.channels.get(channel)?.delete(ws);     this.clientChannels.get(ws)?.delete(channel); }   publish(channel: string, payload: unknown): void {     const msg: ServerMessage = { type: 'data', channel, payload,       timestamp: Date.now() };     const data = JSON.stringify(msg);     this.channels.get(channel)?.forEach(client => {       if (client.readyState === WebSocket.OPEN) client.send(data); }); }   cleanup(ws: WebSocket): void {     this.clientChannels.get(ws)?.forEach(ch => this.unsubscribe(ws, ch));     this.clientChannels.delete(ws); }   listen(): void {     const server = createServer();     const wss = new WebSocketServer({ server });     wss.on('connection', ws => {       ws.on('message', raw => {         const msg: ClientMessage = JSON.parse(raw.toString());         switch (msg.type) {           case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } } Furthermore,         case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } }",
    "category": "long"
  },
  {
    "id": "long_0154",
    "text": "Write a PostgreSQL query to find all users who have placed more than 5 orders in the last 30 days, where each order total exceeds $100, and who have not been flagged for fraud, grouped by user_id with aggregated statistics including total order count, total spend, average order value, first order date, last order date, and the names of the top 3 most frequently ordered products for each user, sorted by total spend descending, with pagination support using cursor-based pagination on user_id for efficient handling of large result sets. The query should use CTEs for readability, include appropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring. Furthermore, ppropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring.",
    "category": "long"
  },
  {
    "id": "long_0155",
    "text": "openapi: 3.1.0 info:   title: mind-nerve Encoding API   version: 0.4.0   description: |     Q16.16 native BERT encoder for intent classification and tool routing.     Encodes query strings into 384-dimensional L2-normalized embeddings     and returns top-K routing candidates from a precomputed catalog.     The encode endpoint accepts a JSON body with a query string and     optional parameters for top_k, backend selection, and runtime override.     All embeddings are returned as Q16.16 fixed-point integers to ensure     bit-identical results across x86_64 CPU backends. CUDA and ARM backends     are available but require explicit opt-in via the backend parameter.     Rate limiting is enforced at 1000 requests per minute per API key.     The catalog endpoint returns metadata about the currently loaded     routing catalog including version, size, and hash for cache validation. paths:   /v1/encode:     post:       operationId: encodeQuery       summary: Encode a query string and return top-K routing candidates       requestBody:         required: true         content:           application/json:             schema:               type: object               required: [query]               properties:                 query:                   type: string                   minLength: 1                   maxLength: 2048                   description: The query string to encode and route                 top_k:                   type: integer                   minimum: 1                   maximum: 100                   default: 5                 backend:                   type: string                   enum: [native, pytorch, cuda]                   default: native       responses:         '200':           description: Successful encoding and routing           content:             application/json:               schema:                 $ref: '#/components/schemas/RouteResult'         '400':           description: Invalid request (query too long, invalid backend)         '429':           description: Rate limit exceeded         '503':           description: Backend unavailable Furthermore, application/json:               schema:                 $ref: '#/components/schemas/RouteResult'         '400':           description: Invalid request (query too long, invalid backend)         '429':           description: Rate limit exceeded         '503':           description: Backend unavailable",
    "category": "long"
  },
  {
    "id": "long_0156",
    "text": "from __future__ import annotations import asyncio import contextlib import dataclasses import hashlib import json import logging import os import time from collections.abc import AsyncGenerator, Generator from pathlib import Path from typing import Any, ClassVar import aiohttp import numpy as np logger = logging.getLogger(__name__) @dataclasses.dataclass(frozen=True, slots=True) class EncoderConfig:     model_hash: str     tokenizer_hash: str     hidden_dim: int = 384     num_heads: int = 12     num_layers: int = 12     max_seq_len: int = 256     window_size: int = 256     stride: int = 192     top_k: int = 5     backend: str = 'native'     runtime_dir: Path = dataclasses.field(         default_factory=lambda: Path.home() / '.local/share/mind-nerve/runtime')     SCHEMA_VERSION: ClassVar[int] = 1     def to_cache_key(self) -> str:         payload = json.dumps(dataclasses.asdict(self), sort_keys=True, default=str)         return hashlib.sha256(payload.encode()).hexdigest() class EncoderError(Exception):     pass class BackendNotAvailableError(EncoderError):     pass class EncoderTimeoutError(EncoderError):     pass @contextlib.asynccontextmanager async def managed_encoder(config: EncoderConfig) -> AsyncGenerator[AsyncEncoder, None]:     encoder = AsyncEncoder(config)     try:         await encoder.initialize()         yield encoder     finally:         await encoder.shutdown() class AsyncEncoder:     def __init__(self, config: EncoderConfig) -> None:         self._config = config         self._session: aiohttp.ClientSession | None = None         self._initialized = False         self._request_count = 0         self._error_count = 0         self._total_latency_ms = 0.0     async def initialize(self) -> None:         connector = aiohttp.TCPConnector(limit=100, limit_per_host=20)         self._session = aiohttp.ClientSession(connector=connector)         self._initialized = True         logger.info('AsyncEncoder initialized: backend=%s', self._config.backend)     async def encode(self, query: str, timeout: float = 5.0) -> np.ndarray:         if not self._initialized:             raise EncoderError('Encoder not initialized')         start = time.perf_counter()         try:             result = await asyncio.wait_for(self._encode_impl(query), timeout=timeout)             self._total_latency_ms += (time.perf_counter() - start) * 1000             self._request_count += 1             return result         except asyncio.TimeoutError as exc:             self._error_count += 1             raise EncoderTimeoutError(f'Encode timed out after {timeout}s') from exc     async def _encode_impl(self, query: str) -> np.ndarray:         raise NotImplementedError     async def shutdown(self) -> None:         if self._session:             await self._session.close()         self._initialized = False     @property     def stats(self) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) }  Furthermore, elf) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) } ",
    "category": "long"
  },
  {
    "id": "long_0157",
    "text": "Implement a complete CI/CD pipeline for a Python machine learning project that includes the following stages: code quality checks using ruff, black, isort, mypy, and bandit; unit tests with pytest and coverage reporting; integration tests against a real PostgreSQL database using testcontainers; model training on a held-out validation set with automatic hyperparameter tuning using Optuna; model evaluation comparing the new model against the currently deployed baseline on a standardized benchmark dataset of 10,000 samples; model registration in MLflow with artifact storage on S3; Docker image build with multi-stage build optimized for production size under 500MB; security scanning using trivy and snyk; deployment to Kubernetes staging environment with smoke tests; automated load testing using k6 to validate p99 latency under 200ms at 1000 concurrent users; canary deployment to production with 5% traffic split; monitoring of key metrics for 30 minutes before full rollout; automatic rollback if error rate exceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials. Furthermore, xceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials.",
    "category": "long"
  },
  {
    "id": "long_0158",
    "text": "Implement a complete CI/CD pipeline for a Python machine learning project that includes the following stages: code quality checks using ruff, black, isort, mypy, and bandit; unit tests with pytest and coverage reporting; integration tests against a real PostgreSQL database using testcontainers; model training on a held-out validation set with automatic hyperparameter tuning using Optuna; model evaluation comparing the new model against the currently deployed baseline on a standardized benchmark dataset of 10,000 samples; model registration in MLflow with artifact storage on S3; Docker image build with multi-stage build optimized for production size under 500MB; security scanning using trivy and snyk; deployment to Kubernetes staging environment with smoke tests; automated load testing using k6 to validate p99 latency under 200ms at 1000 concurrent users; canary deployment to production with 5% traffic split; monitoring of key metrics for 30 minutes before full rollout; automatic rollback if error rate exceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials. Furthermore, xceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials.",
    "category": "long"
  },
  {
    "id": "long_0159",
    "text": "Architectural Decision Record: Switching from synchronous REST API calls to an event-driven architecture using Apache Kafka for the order processing pipeline. Context: The current synchronous REST-based order processing system suffers from cascading failures when downstream services are slow or unavailable. Peak load of 10,000 orders per minute causes timeouts in the payment service which propagates back to the API gateway causing HTTP 503 errors visible to customers. The average order processing time is 2.3 seconds due to sequential API calls to inventory, payment, notification, and shipping services. Decision: Implement event-driven architecture using Apache Kafka as the message broker. Each service publishes domain events and subscribes to relevant topics. Order creation publishes to order.created topic. Payment service subscribes to order.created and publishes payment.processed or payment.failed. Inventory service subscribes to payment.processed and publishes inventory.reserved or inventory.insufficient. The API immediately returns 202 Accepted with an order ID. Customers poll a read model or receive webhook notifications when order status changes. Consequences: Orders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully. Furthermore, ders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully.",
    "category": "long"
  },
  {
    "id": "long_0160",
    "text": "class DistributedEmbeddingCache: \"\"\"Thread-safe distributed embedding cache backed by Redis with LRU eviction, TTL support, and automatic serialization of numpy float32 arrays to Q16.16 fixed-point representation. The cache key is SHA-256(model_hash + tokenizer_hash + query_text). Cache entries expire after ttl_seconds (default 3600). The cache uses a two-level hierarchy: L1 is an in-process LRU (maxsize=1024) and L2 is the shared Redis cluster. Write-back from L1 to L2 is asynchronous via a background thread. The serialize method converts float32[384] to Q16.16 i32[384] bytes with deterministic rounding for cross-arch reproducibility.\"\"\" def __init__(self, redis_url: str, ttl_seconds: int = 3600,              l1_maxsize: int = 1024) -> None:     self._redis = redis.Redis.from_url(redis_url)     self._ttl = ttl_seconds     self._l1 = functools.lru_cache(maxsize=l1_maxsize)(self._get_l2)     self._write_queue: queue.Queue = queue.Queue(maxsize=4096)     self._writer = threading.Thread(target=self._write_worker, daemon=True)     self._writer.start() def _cache_key(self, model_hash: str, tokenizer_hash: str, query: str) -> str:     payload = f'{model_hash}:{tokenizer_hash}:{query}'.encode('utf-8')     return 'emb:' + hashlib.sha256(payload).hexdigest() def get(self, model_hash: str, tokenizer_hash: str, query: str) -> np.ndarray | None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._redis.get(key)     if raw is None:         return None     return self._deserialize(raw) def put(self, model_hash: str, tokenizer_hash: str, query: str,         embedding: np.ndarray) -> None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._serialize(embedding)     self._redis.setex(key, self._ttl, raw) def _serialize(self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0) Furthermore, (self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0)",
    "category": "long"
  },
  {
    "id": "long_0161",
    "text": "Implement a complete CI/CD pipeline for a Python machine learning project that includes the following stages: code quality checks using ruff, black, isort, mypy, and bandit; unit tests with pytest and coverage reporting; integration tests against a real PostgreSQL database using testcontainers; model training on a held-out validation set with automatic hyperparameter tuning using Optuna; model evaluation comparing the new model against the currently deployed baseline on a standardized benchmark dataset of 10,000 samples; model registration in MLflow with artifact storage on S3; Docker image build with multi-stage build optimized for production size under 500MB; security scanning using trivy and snyk; deployment to Kubernetes staging environment with smoke tests; automated load testing using k6 to validate p99 latency under 200ms at 1000 concurrent users; canary deployment to production with 5% traffic split; monitoring of key metrics for 30 minutes before full rollout; automatic rollback if error rate exceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials. Furthermore, xceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials.",
    "category": "long"
  },
  {
    "id": "long_0162",
    "text": "Design and implement a fault-tolerant data pipeline that ingests streaming events from Apache Kafka topics (user.clicked, user.purchased, user.viewed, user.searched) at a rate of 500,000 events per second, applies real-time feature engineering including session reconstruction with a 30-minute session timeout, rolling window aggregations over 1-minute, 5-minute, and 1-hour windows, user-level and item-level statistics, and categorical feature encoding using frequency-adaptive target encoding with Laplace smoothing; writes the enriched feature vectors to a Redis Feature Store with a 24-hour TTL for online serving and to Apache Iceberg tables on S3 for offline training; handles late arriving events up to 5 minutes behind watermark using Apache Flink's event-time processing with allowed lateness; implements exactly-once semantics via Kafka transactions and Flink checkpointing to S3 every 60 seconds; provides a Grafana dashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint. Furthermore, ashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint.",
    "category": "long"
  },
  {
    "id": "long_0163",
    "text": "Analyze the following microservices architecture and identify potential single points of failure, bottlenecks, and security vulnerabilities: The system consists of an API gateway that routes requests to twelve backend services including user-service, auth-service, payment-service, notification-service, email-service, sms-service, recommendation-service, search-service, catalog-service, inventory-service, order-service, and shipping-service. Each service has its own PostgreSQL database with connection pooling configured via PgBouncer. Services communicate asynchronously via RabbitMQ for non-critical paths and synchronously via gRPC for critical paths like payment processing. The API gateway is implemented in Nginx with Lua scripting for rate limiting. Authentication uses JWT tokens with a 15-minute expiry refreshed automatically by the client. The recommendation service uses a pre-trained collaborative filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses. Furthermore, ve filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses.",
    "category": "long"
  },
  {
    "id": "long_0164",
    "text": "Optimize the following Python function that processes a list of 100,000 user records to compute personalized recommendations: def compute_recommendations(users: list[dict], products: list[dict], interactions: list[dict]) -> dict[str, list[str]]:     results = {}     for user in users:         user_interactions = [i for i in interactions                              if i['user_id'] == user['id']]         interacted_products = {i['product_id'] for i in user_interactions}         similar_users = []         for other_user in users:             if other_user['id'] == user['id']: continue             other_interactions = {i['product_id'] for i in interactions                                   if i['user_id'] == other_user['id']}             if len(interacted_products & other_interactions) >= 3:                 similar_users.append(other_user['id'])         recommended = []         for similar_user_id in similar_users[:10]:             similar_interactions = {i['product_id'] for i in interactions                                     if i['user_id'] == similar_user_id}             for prod_id in similar_interactions - interacted_products:                 if prod_id not in recommended:                     recommended.append(prod_id)         results[user['id']] = recommended[:20]     return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine. Furthermore, return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine.",
    "category": "long"
  },
  {
    "id": "long_0165",
    "text": "Design and implement a fault-tolerant data pipeline that ingests streaming events from Apache Kafka topics (user.clicked, user.purchased, user.viewed, user.searched) at a rate of 500,000 events per second, applies real-time feature engineering including session reconstruction with a 30-minute session timeout, rolling window aggregations over 1-minute, 5-minute, and 1-hour windows, user-level and item-level statistics, and categorical feature encoding using frequency-adaptive target encoding with Laplace smoothing; writes the enriched feature vectors to a Redis Feature Store with a 24-hour TTL for online serving and to Apache Iceberg tables on S3 for offline training; handles late arriving events up to 5 minutes behind watermark using Apache Flink's event-time processing with allowed lateness; implements exactly-once semantics via Kafka transactions and Flink checkpointing to S3 every 60 seconds; provides a Grafana dashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint. Furthermore, ashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint.",
    "category": "long"
  },
  {
    "id": "long_0166",
    "text": "use std::collections::HashMap; use std::sync::{Arc, RwLock}; use std::time::{Duration, Instant}; #[derive(Debug, Clone)] pub struct CacheEntry<V> { value: V, inserted_at: Instant, ttl: Duration, } impl<V> CacheEntry<V> {     pub fn is_expired(&self) -> bool {         self.inserted_at.elapsed() >= self.ttl } } #[derive(Debug)] pub struct TtlCache<K, V> {     inner: Arc<RwLock<HashMap<K, CacheEntry<V>>>>,     default_ttl: Duration, } impl<K, V> TtlCache<K, V> where K: Eq + std::hash::Hash + Clone, V: Clone, {     pub fn new(default_ttl: Duration) -> Self {         Self { inner: Arc::new(RwLock::new(HashMap::new())), default_ttl } }     pub fn insert(&self, key: K, value: V) {         let entry = CacheEntry { value, inserted_at: Instant::now(),             ttl: self.default_ttl };         self.inner.write().unwrap().insert(key, entry); }     pub fn get(&self, key: &K) -> Option<V> {         let guard = self.inner.read().unwrap();         guard.get(key).filter(|e| !e.is_expired()).map(|e| e.value.clone()) }     pub fn evict_expired(&self) -> usize {         let mut guard = self.inner.write().unwrap();         let before = guard.len();         guard.retain(|_, e| !e.is_expired());         before - guard.len() }     pub fn len(&self) -> usize { self.inner.read().unwrap().len() }     pub fn is_empty(&self) -> bool { self.len() == 0 } } Furthermore,  -> usize {         let mut guard = self.inner.write().unwrap();         let before = guard.len();         guard.retain(|_, e| !e.is_expired());         before - guard.len() }     pub fn len(&self) -> usize { self.inner.read().unwrap().len() }     pub fn is_empty(&self) -> bool { self.len() == 0 } }",
    "category": "long"
  },
  {
    "id": "long_0167",
    "text": "Design and implement a fault-tolerant data pipeline that ingests streaming events from Apache Kafka topics (user.clicked, user.purchased, user.viewed, user.searched) at a rate of 500,000 events per second, applies real-time feature engineering including session reconstruction with a 30-minute session timeout, rolling window aggregations over 1-minute, 5-minute, and 1-hour windows, user-level and item-level statistics, and categorical feature encoding using frequency-adaptive target encoding with Laplace smoothing; writes the enriched feature vectors to a Redis Feature Store with a 24-hour TTL for online serving and to Apache Iceberg tables on S3 for offline training; handles late arriving events up to 5 minutes behind watermark using Apache Flink's event-time processing with allowed lateness; implements exactly-once semantics via Kafka transactions and Flink checkpointing to S3 every 60 seconds; provides a Grafana dashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint. Furthermore, ashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint.",
    "category": "long"
  },
  {
    "id": "long_0168",
    "text": "Optimize the following Python function that processes a list of 100,000 user records to compute personalized recommendations: def compute_recommendations(users: list[dict], products: list[dict], interactions: list[dict]) -> dict[str, list[str]]:     results = {}     for user in users:         user_interactions = [i for i in interactions                              if i['user_id'] == user['id']]         interacted_products = {i['product_id'] for i in user_interactions}         similar_users = []         for other_user in users:             if other_user['id'] == user['id']: continue             other_interactions = {i['product_id'] for i in interactions                                   if i['user_id'] == other_user['id']}             if len(interacted_products & other_interactions) >= 3:                 similar_users.append(other_user['id'])         recommended = []         for similar_user_id in similar_users[:10]:             similar_interactions = {i['product_id'] for i in interactions                                     if i['user_id'] == similar_user_id}             for prod_id in similar_interactions - interacted_products:                 if prod_id not in recommended:                     recommended.append(prod_id)         results[user['id']] = recommended[:20]     return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine. Furthermore, return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine.",
    "category": "long"
  },
  {
    "id": "long_0169",
    "text": "package encoder import (     'context'     'crypto/sha256'     'encoding/binary'     'fmt'     'math'     'sync' ) const (     HiddenDim   = 384     NumHeads    = 12     HeadDim     = 32     Q16Scale    = 65536.0     LayerNormEps = 1e-12 ) type Q16 int32 func Float32ToQ16(f float32) Q16 {     scaled := float64(f) * Q16Scale     if scaled > math.MaxInt32 { return Q16(math.MaxInt32) }     if scaled < math.MinInt32 { return Q16(math.MinInt32) }     return Q16(math.Round(scaled)) } func (q Q16) ToFloat32() float32 { return float32(float64(q) / Q16Scale) } type Matrix struct { Data []Q16; Rows, Cols int } func NewMatrix(rows, cols int) *Matrix {     return &Matrix{Data: make([]Q16, rows*cols), Rows: rows, Cols: cols} } func (m *Matrix) At(row, col int) Q16 { return m.Data[row*m.Cols+col] } func (m *Matrix) Set(row, col int, v Q16) { m.Data[row*m.Cols+col] = v } func Matmul(a, b *Matrix) *Matrix {     if a.Cols != b.Rows { panic('dimension mismatch') }     c := NewMatrix(a.Rows, b.Cols)     var wg sync.WaitGroup     for i := 0; i < a.Rows; i++ {         wg.Add(1)         go func(row int) {             defer wg.Done()             for j := 0; j < b.Cols; j++ {                 var acc int64                 for k := 0; k < a.Cols; k++ {                     acc += int64(a.At(row, k)) * int64(b.At(k, j)) }                 c.Set(row, j, Q16(acc>>16)) } }(i) }     wg.Wait()     return c } func LayerNorm(x *Matrix, gamma, beta []Q16) *Matrix {     out := NewMatrix(x.Rows, x.Cols)     for i := 0; i < x.Rows; i++ {         var sum int64         for j := 0; j < x.Cols; j++ { sum += int64(x.At(i, j)) }         mean := Q16(sum / int64(x.Cols))         var varAcc int64         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             varAcc += d * d }         variance := float64(varAcc) / float64(x.Cols) / Q16Scale / Q16Scale         invStd := 1.0 / math.Sqrt(variance + LayerNormEps)         invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out }  Furthermore,     invStdQ16 := Float32ToQ16(float32(invStd))         for j := 0; j < x.Cols; j++ {             d := int64(x.At(i, j)) - int64(mean)             normalized := Q16(d * int64(invStdQ16) >> 16)             out.Set(i, j, Q16(int64(normalized)*int64(gamma[j])>>16 + int64(beta[j]))) } }     return out } ",
    "category": "long"
  },
  {
    "id": "long_0170",
    "text": "apiVersion: apps/v1 kind: Deployment metadata:   name: mind-nerve-encoder   namespace: production   labels:     app: mind-nerve     component: encoder     version: v0.4.0 spec:   replicas: 3   selector:     matchLabels:       app: mind-nerve       component: encoder   strategy:     type: RollingUpdate     rollingUpdate:       maxSurge: 1       maxUnavailable: 0   template:     metadata:       labels:         app: mind-nerve         component: encoder         version: v0.4.0       annotations:         prometheus.io/scrape: 'true'         prometheus.io/port: '9090'         prometheus.io/path: '/metrics'     spec:       securityContext:         runAsNonRoot: true         runAsUser: 1000         fsGroup: 1000       containers:       - name: encoder         image: ghcr.io/star-ga/mind-nerve:v0.4.0         ports:         - containerPort: 8080         - containerPort: 9090         resources:           requests:             cpu: 500m             memory: 512Mi           limits:             cpu: 2000m             memory: 2Gi         readinessProbe:           httpGet:             path: /health/ready             port: 8080           initialDelaySeconds: 10           periodSeconds: 5         livenessProbe:           httpGet:             path: /health/live             port: 8080           initialDelaySeconds: 30           periodSeconds: 10         env:         - name: MIND_NERVE_BACKEND           value: native         - name: MIND_NERVE_RUNTIME_DIR           value: /runtime         volumeMounts:         - name: runtime           mountPath: /runtime           readOnly: true       volumes:       - name: runtime         configMap:           name: mind-nerve-runtime Furthermore,  MIND_NERVE_BACKEND           value: native         - name: MIND_NERVE_RUNTIME_DIR           value: /runtime         volumeMounts:         - name: runtime           mountPath: /runtime           readOnly: true       volumes:       - name: runtime         configMap:           name: mind-nerve-runtime",
    "category": "long"
  },
  {
    "id": "long_0171",
    "text": "class DistributedEmbeddingCache: \"\"\"Thread-safe distributed embedding cache backed by Redis with LRU eviction, TTL support, and automatic serialization of numpy float32 arrays to Q16.16 fixed-point representation. The cache key is SHA-256(model_hash + tokenizer_hash + query_text). Cache entries expire after ttl_seconds (default 3600). The cache uses a two-level hierarchy: L1 is an in-process LRU (maxsize=1024) and L2 is the shared Redis cluster. Write-back from L1 to L2 is asynchronous via a background thread. The serialize method converts float32[384] to Q16.16 i32[384] bytes with deterministic rounding for cross-arch reproducibility.\"\"\" def __init__(self, redis_url: str, ttl_seconds: int = 3600,              l1_maxsize: int = 1024) -> None:     self._redis = redis.Redis.from_url(redis_url)     self._ttl = ttl_seconds     self._l1 = functools.lru_cache(maxsize=l1_maxsize)(self._get_l2)     self._write_queue: queue.Queue = queue.Queue(maxsize=4096)     self._writer = threading.Thread(target=self._write_worker, daemon=True)     self._writer.start() def _cache_key(self, model_hash: str, tokenizer_hash: str, query: str) -> str:     payload = f'{model_hash}:{tokenizer_hash}:{query}'.encode('utf-8')     return 'emb:' + hashlib.sha256(payload).hexdigest() def get(self, model_hash: str, tokenizer_hash: str, query: str) -> np.ndarray | None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._redis.get(key)     if raw is None:         return None     return self._deserialize(raw) def put(self, model_hash: str, tokenizer_hash: str, query: str,         embedding: np.ndarray) -> None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._serialize(embedding)     self._redis.setex(key, self._ttl, raw) def _serialize(self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0) Furthermore, (self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0)",
    "category": "long"
  },
  {
    "id": "long_0172",
    "text": "Write a PostgreSQL query to find all users who have placed more than 5 orders in the last 30 days, where each order total exceeds $100, and who have not been flagged for fraud, grouped by user_id with aggregated statistics including total order count, total spend, average order value, first order date, last order date, and the names of the top 3 most frequently ordered products for each user, sorted by total spend descending, with pagination support using cursor-based pagination on user_id for efficient handling of large result sets. The query should use CTEs for readability, include appropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring. Furthermore, ppropriate indexes on the orders table for (user_id, created_at, status) and a partial index on (user_id) WHERE status = 'completed', and should complete in under 100ms on a table with 10 million rows using PostgreSQL 16 with the pg_stat_statements extension enabled for query performance monitoring.",
    "category": "long"
  },
  {
    "id": "long_0173",
    "text": "use std::collections::HashMap; use std::sync::{Arc, RwLock}; use std::time::{Duration, Instant}; #[derive(Debug, Clone)] pub struct CacheEntry<V> { value: V, inserted_at: Instant, ttl: Duration, } impl<V> CacheEntry<V> {     pub fn is_expired(&self) -> bool {         self.inserted_at.elapsed() >= self.ttl } } #[derive(Debug)] pub struct TtlCache<K, V> {     inner: Arc<RwLock<HashMap<K, CacheEntry<V>>>>,     default_ttl: Duration, } impl<K, V> TtlCache<K, V> where K: Eq + std::hash::Hash + Clone, V: Clone, {     pub fn new(default_ttl: Duration) -> Self {         Self { inner: Arc::new(RwLock::new(HashMap::new())), default_ttl } }     pub fn insert(&self, key: K, value: V) {         let entry = CacheEntry { value, inserted_at: Instant::now(),             ttl: self.default_ttl };         self.inner.write().unwrap().insert(key, entry); }     pub fn get(&self, key: &K) -> Option<V> {         let guard = self.inner.read().unwrap();         guard.get(key).filter(|e| !e.is_expired()).map(|e| e.value.clone()) }     pub fn evict_expired(&self) -> usize {         let mut guard = self.inner.write().unwrap();         let before = guard.len();         guard.retain(|_, e| !e.is_expired());         before - guard.len() }     pub fn len(&self) -> usize { self.inner.read().unwrap().len() }     pub fn is_empty(&self) -> bool { self.len() == 0 } } Furthermore,  -> usize {         let mut guard = self.inner.write().unwrap();         let before = guard.len();         guard.retain(|_, e| !e.is_expired());         before - guard.len() }     pub fn len(&self) -> usize { self.inner.read().unwrap().len() }     pub fn is_empty(&self) -> bool { self.len() == 0 } }",
    "category": "long"
  },
  {
    "id": "long_0174",
    "text": "from __future__ import annotations import asyncio import contextlib import dataclasses import hashlib import json import logging import os import time from collections.abc import AsyncGenerator, Generator from pathlib import Path from typing import Any, ClassVar import aiohttp import numpy as np logger = logging.getLogger(__name__) @dataclasses.dataclass(frozen=True, slots=True) class EncoderConfig:     model_hash: str     tokenizer_hash: str     hidden_dim: int = 384     num_heads: int = 12     num_layers: int = 12     max_seq_len: int = 256     window_size: int = 256     stride: int = 192     top_k: int = 5     backend: str = 'native'     runtime_dir: Path = dataclasses.field(         default_factory=lambda: Path.home() / '.local/share/mind-nerve/runtime')     SCHEMA_VERSION: ClassVar[int] = 1     def to_cache_key(self) -> str:         payload = json.dumps(dataclasses.asdict(self), sort_keys=True, default=str)         return hashlib.sha256(payload.encode()).hexdigest() class EncoderError(Exception):     pass class BackendNotAvailableError(EncoderError):     pass class EncoderTimeoutError(EncoderError):     pass @contextlib.asynccontextmanager async def managed_encoder(config: EncoderConfig) -> AsyncGenerator[AsyncEncoder, None]:     encoder = AsyncEncoder(config)     try:         await encoder.initialize()         yield encoder     finally:         await encoder.shutdown() class AsyncEncoder:     def __init__(self, config: EncoderConfig) -> None:         self._config = config         self._session: aiohttp.ClientSession | None = None         self._initialized = False         self._request_count = 0         self._error_count = 0         self._total_latency_ms = 0.0     async def initialize(self) -> None:         connector = aiohttp.TCPConnector(limit=100, limit_per_host=20)         self._session = aiohttp.ClientSession(connector=connector)         self._initialized = True         logger.info('AsyncEncoder initialized: backend=%s', self._config.backend)     async def encode(self, query: str, timeout: float = 5.0) -> np.ndarray:         if not self._initialized:             raise EncoderError('Encoder not initialized')         start = time.perf_counter()         try:             result = await asyncio.wait_for(self._encode_impl(query), timeout=timeout)             self._total_latency_ms += (time.perf_counter() - start) * 1000             self._request_count += 1             return result         except asyncio.TimeoutError as exc:             self._error_count += 1             raise EncoderTimeoutError(f'Encode timed out after {timeout}s') from exc     async def _encode_impl(self, query: str) -> np.ndarray:         raise NotImplementedError     async def shutdown(self) -> None:         if self._session:             await self._session.close()         self._initialized = False     @property     def stats(self) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) }  Furthermore, elf) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) } ",
    "category": "long"
  },
  {
    "id": "long_0175",
    "text": "Design and implement a fault-tolerant data pipeline that ingests streaming events from Apache Kafka topics (user.clicked, user.purchased, user.viewed, user.searched) at a rate of 500,000 events per second, applies real-time feature engineering including session reconstruction with a 30-minute session timeout, rolling window aggregations over 1-minute, 5-minute, and 1-hour windows, user-level and item-level statistics, and categorical feature encoding using frequency-adaptive target encoding with Laplace smoothing; writes the enriched feature vectors to a Redis Feature Store with a 24-hour TTL for online serving and to Apache Iceberg tables on S3 for offline training; handles late arriving events up to 5 minutes behind watermark using Apache Flink's event-time processing with allowed lateness; implements exactly-once semantics via Kafka transactions and Flink checkpointing to S3 every 60 seconds; provides a Grafana dashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint. Furthermore, ashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint.",
    "category": "long"
  },
  {
    "id": "long_0176",
    "text": "Optimize the following Python function that processes a list of 100,000 user records to compute personalized recommendations: def compute_recommendations(users: list[dict], products: list[dict], interactions: list[dict]) -> dict[str, list[str]]:     results = {}     for user in users:         user_interactions = [i for i in interactions                              if i['user_id'] == user['id']]         interacted_products = {i['product_id'] for i in user_interactions}         similar_users = []         for other_user in users:             if other_user['id'] == user['id']: continue             other_interactions = {i['product_id'] for i in interactions                                   if i['user_id'] == other_user['id']}             if len(interacted_products & other_interactions) >= 3:                 similar_users.append(other_user['id'])         recommended = []         for similar_user_id in similar_users[:10]:             similar_interactions = {i['product_id'] for i in interactions                                     if i['user_id'] == similar_user_id}             for prod_id in similar_interactions - interacted_products:                 if prod_id not in recommended:                     recommended.append(prod_id)         results[user['id']] = recommended[:20]     return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine. Furthermore, return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine.",
    "category": "long"
  },
  {
    "id": "long_0177",
    "text": "Architectural Decision Record: Switching from synchronous REST API calls to an event-driven architecture using Apache Kafka for the order processing pipeline. Context: The current synchronous REST-based order processing system suffers from cascading failures when downstream services are slow or unavailable. Peak load of 10,000 orders per minute causes timeouts in the payment service which propagates back to the API gateway causing HTTP 503 errors visible to customers. The average order processing time is 2.3 seconds due to sequential API calls to inventory, payment, notification, and shipping services. Decision: Implement event-driven architecture using Apache Kafka as the message broker. Each service publishes domain events and subscribes to relevant topics. Order creation publishes to order.created topic. Payment service subscribes to order.created and publishes payment.processed or payment.failed. Inventory service subscribes to payment.processed and publishes inventory.reserved or inventory.insufficient. The API immediately returns 202 Accepted with an order ID. Customers poll a read model or receive webhook notifications when order status changes. Consequences: Orders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully. Furthermore, ders will be processed asynchronously within 5 seconds p99. Individual service failures will not cause customer-visible errors. We gain horizontal scalability for each consumer group independently. We introduce eventual consistency which requires UI changes to handle pending order states gracefully.",
    "category": "long"
  },
  {
    "id": "long_0178",
    "text": "Optimize the following Python function that processes a list of 100,000 user records to compute personalized recommendations: def compute_recommendations(users: list[dict], products: list[dict], interactions: list[dict]) -> dict[str, list[str]]:     results = {}     for user in users:         user_interactions = [i for i in interactions                              if i['user_id'] == user['id']]         interacted_products = {i['product_id'] for i in user_interactions}         similar_users = []         for other_user in users:             if other_user['id'] == user['id']: continue             other_interactions = {i['product_id'] for i in interactions                                   if i['user_id'] == other_user['id']}             if len(interacted_products & other_interactions) >= 3:                 similar_users.append(other_user['id'])         recommended = []         for similar_user_id in similar_users[:10]:             similar_interactions = {i['product_id'] for i in interactions                                     if i['user_id'] == similar_user_id}             for prod_id in similar_interactions - interacted_products:                 if prod_id not in recommended:                     recommended.append(prod_id)         results[user['id']] = recommended[:20]     return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine. Furthermore, return results The current implementation has O(n^3) complexity. Propose and implement an optimized version using numpy vectorized operations, sparse matrix representations, and approximate nearest neighbor search that reduces wall-clock time from 45 minutes to under 30 seconds on a 16-core machine.",
    "category": "long"
  },
  {
    "id": "long_0179",
    "text": "Analyze the following microservices architecture and identify potential single points of failure, bottlenecks, and security vulnerabilities: The system consists of an API gateway that routes requests to twelve backend services including user-service, auth-service, payment-service, notification-service, email-service, sms-service, recommendation-service, search-service, catalog-service, inventory-service, order-service, and shipping-service. Each service has its own PostgreSQL database with connection pooling configured via PgBouncer. Services communicate asynchronously via RabbitMQ for non-critical paths and synchronously via gRPC for critical paths like payment processing. The API gateway is implemented in Nginx with Lua scripting for rate limiting. Authentication uses JWT tokens with a 15-minute expiry refreshed automatically by the client. The recommendation service uses a pre-trained collaborative filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses. Furthermore, ve filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses.",
    "category": "long"
  },
  {
    "id": "long_0180",
    "text": "def process_batch(items: list[dict], batch_size: int = 32, timeout: float = 30.0, retry_count: int = 3, backoff_factor: float = 1.5) -> list[dict]: \"\"\"Process a batch of items with retry logic and exponential backoff. Each item is validated, transformed, and persisted to the database. Failed items are retried up to retry_count times with exponential backoff. Items that fail all retries are logged and returned in the error list. The function is thread-safe and can be called from multiple workers. Parameters: items (list[dict]) — the items to process; batch_size (int) — number of items per database transaction; timeout (float) — per-item timeout in seconds; retry_count (int) — maximum retries per item; backoff_factor (float) — exponential backoff multiplier. Returns: list of successfully processed items.\"\"\" results = [] errors = [] for i in range(0, len(items), batch_size):     chunk = items[i:i + batch_size]     with db.transaction():         for item in chunk:             for attempt in range(retry_count):                 try:                     validated = validate(item)                     transformed = transform(validated)                     persisted = persist(transformed, timeout=timeout)                     results.append(persisted)                     break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors Furthermore,                   break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors",
    "category": "long"
  },
  {
    "id": "long_0181",
    "text": "Implement a complete CI/CD pipeline for a Python machine learning project that includes the following stages: code quality checks using ruff, black, isort, mypy, and bandit; unit tests with pytest and coverage reporting; integration tests against a real PostgreSQL database using testcontainers; model training on a held-out validation set with automatic hyperparameter tuning using Optuna; model evaluation comparing the new model against the currently deployed baseline on a standardized benchmark dataset of 10,000 samples; model registration in MLflow with artifact storage on S3; Docker image build with multi-stage build optimized for production size under 500MB; security scanning using trivy and snyk; deployment to Kubernetes staging environment with smoke tests; automated load testing using k6 to validate p99 latency under 200ms at 1000 concurrent users; canary deployment to production with 5% traffic split; monitoring of key metrics for 30 minutes before full rollout; automatic rollback if error rate exceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials. Furthermore, xceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials.",
    "category": "long"
  },
  {
    "id": "long_0182",
    "text": "Analyze the following microservices architecture and identify potential single points of failure, bottlenecks, and security vulnerabilities: The system consists of an API gateway that routes requests to twelve backend services including user-service, auth-service, payment-service, notification-service, email-service, sms-service, recommendation-service, search-service, catalog-service, inventory-service, order-service, and shipping-service. Each service has its own PostgreSQL database with connection pooling configured via PgBouncer. Services communicate asynchronously via RabbitMQ for non-critical paths and synchronously via gRPC for critical paths like payment processing. The API gateway is implemented in Nginx with Lua scripting for rate limiting. Authentication uses JWT tokens with a 15-minute expiry refreshed automatically by the client. The recommendation service uses a pre-trained collaborative filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses. Furthermore, ve filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses.",
    "category": "long"
  },
  {
    "id": "long_0183",
    "text": "def process_batch(items: list[dict], batch_size: int = 32, timeout: float = 30.0, retry_count: int = 3, backoff_factor: float = 1.5) -> list[dict]: \"\"\"Process a batch of items with retry logic and exponential backoff. Each item is validated, transformed, and persisted to the database. Failed items are retried up to retry_count times with exponential backoff. Items that fail all retries are logged and returned in the error list. The function is thread-safe and can be called from multiple workers. Parameters: items (list[dict]) — the items to process; batch_size (int) — number of items per database transaction; timeout (float) — per-item timeout in seconds; retry_count (int) — maximum retries per item; backoff_factor (float) — exponential backoff multiplier. Returns: list of successfully processed items.\"\"\" results = [] errors = [] for i in range(0, len(items), batch_size):     chunk = items[i:i + batch_size]     with db.transaction():         for item in chunk:             for attempt in range(retry_count):                 try:                     validated = validate(item)                     transformed = transform(validated)                     persisted = persist(transformed, timeout=timeout)                     results.append(persisted)                     break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors Furthermore,                   break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors",
    "category": "long"
  },
  {
    "id": "long_0184",
    "text": "Implement a complete CI/CD pipeline for a Python machine learning project that includes the following stages: code quality checks using ruff, black, isort, mypy, and bandit; unit tests with pytest and coverage reporting; integration tests against a real PostgreSQL database using testcontainers; model training on a held-out validation set with automatic hyperparameter tuning using Optuna; model evaluation comparing the new model against the currently deployed baseline on a standardized benchmark dataset of 10,000 samples; model registration in MLflow with artifact storage on S3; Docker image build with multi-stage build optimized for production size under 500MB; security scanning using trivy and snyk; deployment to Kubernetes staging environment with smoke tests; automated load testing using k6 to validate p99 latency under 200ms at 1000 concurrent users; canary deployment to production with 5% traffic split; monitoring of key metrics for 30 minutes before full rollout; automatic rollback if error rate exceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials. Furthermore, xceeds 0.5% or p99 latency exceeds 500ms during canary phase. The pipeline should run on GitHub Actions with separate jobs for each stage, using job dependencies to ensure correct execution order. Secrets should be managed via GitHub OIDC authentication to AWS without storing long-lived credentials.",
    "category": "long"
  },
  {
    "id": "long_0185",
    "text": "#!/usr/bin/env bash set -euo pipefail IFS=$'\\n\\t' # Automated deployment script for mind-nerve v0.4.0 # Performs: build, test, security scan, docker build, push, deploy SCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\" REPO_ROOT=\"$(cd \"${SCRIPT_DIR}/..\" && pwd)\" VERSION=\"${1:-}\" ENVIRONMENT=\"${2:-staging}\" REGISTRY=\"ghcr.io/star-ga\" IMAGE=\"${REGISTRY}/mind-nerve\" if [[ -z \"${VERSION}\" ]]; then     echo 'ERROR: VERSION argument required' >&2     echo 'Usage: deploy.sh <version> [environment]' >&2     exit 1 fi echo \"==> Deploying mind-nerve ${VERSION} to ${ENVIRONMENT}\" cd \"${REPO_ROOT}\" echo '==> Running tests...' python -m pytest tests/ -x -q --tb=short echo '==> Running security scan...' bandit -r python/mind_nerve -ll echo '==> Building Docker image...' docker build --build-arg VERSION=\"${VERSION}\"              --build-arg BUILD_DATE=\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"              --build-arg VCS_REF=\"$(git rev-parse HEAD)\"              -t \"${IMAGE}:${VERSION}\"              -t \"${IMAGE}:latest\"              . echo '==> Scanning image for vulnerabilities...' trivy image --exit-code 1 --severity HIGH,CRITICAL \"${IMAGE}:${VERSION}\" echo '==> Pushing image...' docker push \"${IMAGE}:${VERSION}\" docker push \"${IMAGE}:latest\" echo '==> Deploying to Kubernetes...' kubectl set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\"  Furthermore, l set image deployment/mind-nerve-encoder         encoder=\"${IMAGE}:${VERSION}\"         --namespace=\"${ENVIRONMENT}\" kubectl rollout status deployment/mind-nerve-encoder         --namespace=\"${ENVIRONMENT}\"         --timeout=300s echo \"==> Deployment complete: ${IMAGE}:${VERSION} -> ${ENVIRONMENT}\" ",
    "category": "long"
  },
  {
    "id": "long_0186",
    "text": "from __future__ import annotations import asyncio import contextlib import dataclasses import hashlib import json import logging import os import time from collections.abc import AsyncGenerator, Generator from pathlib import Path from typing import Any, ClassVar import aiohttp import numpy as np logger = logging.getLogger(__name__) @dataclasses.dataclass(frozen=True, slots=True) class EncoderConfig:     model_hash: str     tokenizer_hash: str     hidden_dim: int = 384     num_heads: int = 12     num_layers: int = 12     max_seq_len: int = 256     window_size: int = 256     stride: int = 192     top_k: int = 5     backend: str = 'native'     runtime_dir: Path = dataclasses.field(         default_factory=lambda: Path.home() / '.local/share/mind-nerve/runtime')     SCHEMA_VERSION: ClassVar[int] = 1     def to_cache_key(self) -> str:         payload = json.dumps(dataclasses.asdict(self), sort_keys=True, default=str)         return hashlib.sha256(payload.encode()).hexdigest() class EncoderError(Exception):     pass class BackendNotAvailableError(EncoderError):     pass class EncoderTimeoutError(EncoderError):     pass @contextlib.asynccontextmanager async def managed_encoder(config: EncoderConfig) -> AsyncGenerator[AsyncEncoder, None]:     encoder = AsyncEncoder(config)     try:         await encoder.initialize()         yield encoder     finally:         await encoder.shutdown() class AsyncEncoder:     def __init__(self, config: EncoderConfig) -> None:         self._config = config         self._session: aiohttp.ClientSession | None = None         self._initialized = False         self._request_count = 0         self._error_count = 0         self._total_latency_ms = 0.0     async def initialize(self) -> None:         connector = aiohttp.TCPConnector(limit=100, limit_per_host=20)         self._session = aiohttp.ClientSession(connector=connector)         self._initialized = True         logger.info('AsyncEncoder initialized: backend=%s', self._config.backend)     async def encode(self, query: str, timeout: float = 5.0) -> np.ndarray:         if not self._initialized:             raise EncoderError('Encoder not initialized')         start = time.perf_counter()         try:             result = await asyncio.wait_for(self._encode_impl(query), timeout=timeout)             self._total_latency_ms += (time.perf_counter() - start) * 1000             self._request_count += 1             return result         except asyncio.TimeoutError as exc:             self._error_count += 1             raise EncoderTimeoutError(f'Encode timed out after {timeout}s') from exc     async def _encode_impl(self, query: str) -> np.ndarray:         raise NotImplementedError     async def shutdown(self) -> None:         if self._session:             await self._session.close()         self._initialized = False     @property     def stats(self) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) }  Furthermore, elf) -> dict[str, Any]:         return { 'request_count': self._request_count,                  'error_count': self._error_count,                  'error_rate': self._error_count / max(1, self._request_count),                  'avg_latency_ms': self._total_latency_ms / max(1, self._request_count) } ",
    "category": "long"
  },
  {
    "id": "long_0187",
    "text": "openapi: 3.1.0 info:   title: mind-nerve Encoding API   version: 0.4.0   description: |     Q16.16 native BERT encoder for intent classification and tool routing.     Encodes query strings into 384-dimensional L2-normalized embeddings     and returns top-K routing candidates from a precomputed catalog.     The encode endpoint accepts a JSON body with a query string and     optional parameters for top_k, backend selection, and runtime override.     All embeddings are returned as Q16.16 fixed-point integers to ensure     bit-identical results across x86_64 CPU backends. CUDA and ARM backends     are available but require explicit opt-in via the backend parameter.     Rate limiting is enforced at 1000 requests per minute per API key.     The catalog endpoint returns metadata about the currently loaded     routing catalog including version, size, and hash for cache validation. paths:   /v1/encode:     post:       operationId: encodeQuery       summary: Encode a query string and return top-K routing candidates       requestBody:         required: true         content:           application/json:             schema:               type: object               required: [query]               properties:                 query:                   type: string                   minLength: 1                   maxLength: 2048                   description: The query string to encode and route                 top_k:                   type: integer                   minimum: 1                   maximum: 100                   default: 5                 backend:                   type: string                   enum: [native, pytorch, cuda]                   default: native       responses:         '200':           description: Successful encoding and routing           content:             application/json:               schema:                 $ref: '#/components/schemas/RouteResult'         '400':           description: Invalid request (query too long, invalid backend)         '429':           description: Rate limit exceeded         '503':           description: Backend unavailable Furthermore, application/json:               schema:                 $ref: '#/components/schemas/RouteResult'         '400':           description: Invalid request (query too long, invalid backend)         '429':           description: Rate limit exceeded         '503':           description: Backend unavailable",
    "category": "long"
  },
  {
    "id": "long_0188",
    "text": "class DistributedEmbeddingCache: \"\"\"Thread-safe distributed embedding cache backed by Redis with LRU eviction, TTL support, and automatic serialization of numpy float32 arrays to Q16.16 fixed-point representation. The cache key is SHA-256(model_hash + tokenizer_hash + query_text). Cache entries expire after ttl_seconds (default 3600). The cache uses a two-level hierarchy: L1 is an in-process LRU (maxsize=1024) and L2 is the shared Redis cluster. Write-back from L1 to L2 is asynchronous via a background thread. The serialize method converts float32[384] to Q16.16 i32[384] bytes with deterministic rounding for cross-arch reproducibility.\"\"\" def __init__(self, redis_url: str, ttl_seconds: int = 3600,              l1_maxsize: int = 1024) -> None:     self._redis = redis.Redis.from_url(redis_url)     self._ttl = ttl_seconds     self._l1 = functools.lru_cache(maxsize=l1_maxsize)(self._get_l2)     self._write_queue: queue.Queue = queue.Queue(maxsize=4096)     self._writer = threading.Thread(target=self._write_worker, daemon=True)     self._writer.start() def _cache_key(self, model_hash: str, tokenizer_hash: str, query: str) -> str:     payload = f'{model_hash}:{tokenizer_hash}:{query}'.encode('utf-8')     return 'emb:' + hashlib.sha256(payload).hexdigest() def get(self, model_hash: str, tokenizer_hash: str, query: str) -> np.ndarray | None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._redis.get(key)     if raw is None:         return None     return self._deserialize(raw) def put(self, model_hash: str, tokenizer_hash: str, query: str,         embedding: np.ndarray) -> None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._serialize(embedding)     self._redis.setex(key, self._ttl, raw) def _serialize(self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0) Furthermore, (self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0)",
    "category": "long"
  },
  {
    "id": "long_0189",
    "text": "Design and implement a fault-tolerant data pipeline that ingests streaming events from Apache Kafka topics (user.clicked, user.purchased, user.viewed, user.searched) at a rate of 500,000 events per second, applies real-time feature engineering including session reconstruction with a 30-minute session timeout, rolling window aggregations over 1-minute, 5-minute, and 1-hour windows, user-level and item-level statistics, and categorical feature encoding using frequency-adaptive target encoding with Laplace smoothing; writes the enriched feature vectors to a Redis Feature Store with a 24-hour TTL for online serving and to Apache Iceberg tables on S3 for offline training; handles late arriving events up to 5 minutes behind watermark using Apache Flink's event-time processing with allowed lateness; implements exactly-once semantics via Kafka transactions and Flink checkpointing to S3 every 60 seconds; provides a Grafana dashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint. Furthermore, ashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint.",
    "category": "long"
  },
  {
    "id": "long_0190",
    "text": "class DistributedEmbeddingCache: \"\"\"Thread-safe distributed embedding cache backed by Redis with LRU eviction, TTL support, and automatic serialization of numpy float32 arrays to Q16.16 fixed-point representation. The cache key is SHA-256(model_hash + tokenizer_hash + query_text). Cache entries expire after ttl_seconds (default 3600). The cache uses a two-level hierarchy: L1 is an in-process LRU (maxsize=1024) and L2 is the shared Redis cluster. Write-back from L1 to L2 is asynchronous via a background thread. The serialize method converts float32[384] to Q16.16 i32[384] bytes with deterministic rounding for cross-arch reproducibility.\"\"\" def __init__(self, redis_url: str, ttl_seconds: int = 3600,              l1_maxsize: int = 1024) -> None:     self._redis = redis.Redis.from_url(redis_url)     self._ttl = ttl_seconds     self._l1 = functools.lru_cache(maxsize=l1_maxsize)(self._get_l2)     self._write_queue: queue.Queue = queue.Queue(maxsize=4096)     self._writer = threading.Thread(target=self._write_worker, daemon=True)     self._writer.start() def _cache_key(self, model_hash: str, tokenizer_hash: str, query: str) -> str:     payload = f'{model_hash}:{tokenizer_hash}:{query}'.encode('utf-8')     return 'emb:' + hashlib.sha256(payload).hexdigest() def get(self, model_hash: str, tokenizer_hash: str, query: str) -> np.ndarray | None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._redis.get(key)     if raw is None:         return None     return self._deserialize(raw) def put(self, model_hash: str, tokenizer_hash: str, query: str,         embedding: np.ndarray) -> None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._serialize(embedding)     self._redis.setex(key, self._ttl, raw) def _serialize(self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0) Furthermore, (self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0)",
    "category": "long"
  },
  {
    "id": "long_0191",
    "text": "class DistributedEmbeddingCache: \"\"\"Thread-safe distributed embedding cache backed by Redis with LRU eviction, TTL support, and automatic serialization of numpy float32 arrays to Q16.16 fixed-point representation. The cache key is SHA-256(model_hash + tokenizer_hash + query_text). Cache entries expire after ttl_seconds (default 3600). The cache uses a two-level hierarchy: L1 is an in-process LRU (maxsize=1024) and L2 is the shared Redis cluster. Write-back from L1 to L2 is asynchronous via a background thread. The serialize method converts float32[384] to Q16.16 i32[384] bytes with deterministic rounding for cross-arch reproducibility.\"\"\" def __init__(self, redis_url: str, ttl_seconds: int = 3600,              l1_maxsize: int = 1024) -> None:     self._redis = redis.Redis.from_url(redis_url)     self._ttl = ttl_seconds     self._l1 = functools.lru_cache(maxsize=l1_maxsize)(self._get_l2)     self._write_queue: queue.Queue = queue.Queue(maxsize=4096)     self._writer = threading.Thread(target=self._write_worker, daemon=True)     self._writer.start() def _cache_key(self, model_hash: str, tokenizer_hash: str, query: str) -> str:     payload = f'{model_hash}:{tokenizer_hash}:{query}'.encode('utf-8')     return 'emb:' + hashlib.sha256(payload).hexdigest() def get(self, model_hash: str, tokenizer_hash: str, query: str) -> np.ndarray | None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._redis.get(key)     if raw is None:         return None     return self._deserialize(raw) def put(self, model_hash: str, tokenizer_hash: str, query: str,         embedding: np.ndarray) -> None:     key = self._cache_key(model_hash, tokenizer_hash, query)     raw = self._serialize(embedding)     self._redis.setex(key, self._ttl, raw) def _serialize(self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0) Furthermore, (self, vec: np.ndarray) -> bytes:     q16 = np.clip(np.round(vec * 65536).astype(np.int32),                   -2147483648, 2147483647)     return q16.tobytes() def _deserialize(self, raw: bytes) -> np.ndarray:     q16 = np.frombuffer(raw, dtype=np.int32)     return (q16.astype(np.float32) / 65536.0)",
    "category": "long"
  },
  {
    "id": "long_0192",
    "text": "openapi: 3.1.0 info:   title: mind-nerve Encoding API   version: 0.4.0   description: |     Q16.16 native BERT encoder for intent classification and tool routing.     Encodes query strings into 384-dimensional L2-normalized embeddings     and returns top-K routing candidates from a precomputed catalog.     The encode endpoint accepts a JSON body with a query string and     optional parameters for top_k, backend selection, and runtime override.     All embeddings are returned as Q16.16 fixed-point integers to ensure     bit-identical results across x86_64 CPU backends. CUDA and ARM backends     are available but require explicit opt-in via the backend parameter.     Rate limiting is enforced at 1000 requests per minute per API key.     The catalog endpoint returns metadata about the currently loaded     routing catalog including version, size, and hash for cache validation. paths:   /v1/encode:     post:       operationId: encodeQuery       summary: Encode a query string and return top-K routing candidates       requestBody:         required: true         content:           application/json:             schema:               type: object               required: [query]               properties:                 query:                   type: string                   minLength: 1                   maxLength: 2048                   description: The query string to encode and route                 top_k:                   type: integer                   minimum: 1                   maximum: 100                   default: 5                 backend:                   type: string                   enum: [native, pytorch, cuda]                   default: native       responses:         '200':           description: Successful encoding and routing           content:             application/json:               schema:                 $ref: '#/components/schemas/RouteResult'         '400':           description: Invalid request (query too long, invalid backend)         '429':           description: Rate limit exceeded         '503':           description: Backend unavailable Furthermore, application/json:               schema:                 $ref: '#/components/schemas/RouteResult'         '400':           description: Invalid request (query too long, invalid backend)         '429':           description: Rate limit exceeded         '503':           description: Backend unavailable",
    "category": "long"
  },
  {
    "id": "long_0193",
    "text": "Analyze the following microservices architecture and identify potential single points of failure, bottlenecks, and security vulnerabilities: The system consists of an API gateway that routes requests to twelve backend services including user-service, auth-service, payment-service, notification-service, email-service, sms-service, recommendation-service, search-service, catalog-service, inventory-service, order-service, and shipping-service. Each service has its own PostgreSQL database with connection pooling configured via PgBouncer. Services communicate asynchronously via RabbitMQ for non-critical paths and synchronously via gRPC for critical paths like payment processing. The API gateway is implemented in Nginx with Lua scripting for rate limiting. Authentication uses JWT tokens with a 15-minute expiry refreshed automatically by the client. The recommendation service uses a pre-trained collaborative filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses. Furthermore, ve filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses.",
    "category": "long"
  },
  {
    "id": "long_0194",
    "text": "openapi: 3.1.0 info:   title: mind-nerve Encoding API   version: 0.4.0   description: |     Q16.16 native BERT encoder for intent classification and tool routing.     Encodes query strings into 384-dimensional L2-normalized embeddings     and returns top-K routing candidates from a precomputed catalog.     The encode endpoint accepts a JSON body with a query string and     optional parameters for top_k, backend selection, and runtime override.     All embeddings are returned as Q16.16 fixed-point integers to ensure     bit-identical results across x86_64 CPU backends. CUDA and ARM backends     are available but require explicit opt-in via the backend parameter.     Rate limiting is enforced at 1000 requests per minute per API key.     The catalog endpoint returns metadata about the currently loaded     routing catalog including version, size, and hash for cache validation. paths:   /v1/encode:     post:       operationId: encodeQuery       summary: Encode a query string and return top-K routing candidates       requestBody:         required: true         content:           application/json:             schema:               type: object               required: [query]               properties:                 query:                   type: string                   minLength: 1                   maxLength: 2048                   description: The query string to encode and route                 top_k:                   type: integer                   minimum: 1                   maximum: 100                   default: 5                 backend:                   type: string                   enum: [native, pytorch, cuda]                   default: native       responses:         '200':           description: Successful encoding and routing           content:             application/json:               schema:                 $ref: '#/components/schemas/RouteResult'         '400':           description: Invalid request (query too long, invalid backend)         '429':           description: Rate limit exceeded         '503':           description: Backend unavailable Furthermore, application/json:               schema:                 $ref: '#/components/schemas/RouteResult'         '400':           description: Invalid request (query too long, invalid backend)         '429':           description: Rate limit exceeded         '503':           description: Backend unavailable",
    "category": "long"
  },
  {
    "id": "long_0195",
    "text": "Design and implement a fault-tolerant data pipeline that ingests streaming events from Apache Kafka topics (user.clicked, user.purchased, user.viewed, user.searched) at a rate of 500,000 events per second, applies real-time feature engineering including session reconstruction with a 30-minute session timeout, rolling window aggregations over 1-minute, 5-minute, and 1-hour windows, user-level and item-level statistics, and categorical feature encoding using frequency-adaptive target encoding with Laplace smoothing; writes the enriched feature vectors to a Redis Feature Store with a 24-hour TTL for online serving and to Apache Iceberg tables on S3 for offline training; handles late arriving events up to 5 minutes behind watermark using Apache Flink's event-time processing with allowed lateness; implements exactly-once semantics via Kafka transactions and Flink checkpointing to S3 every 60 seconds; provides a Grafana dashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint. Furthermore, ashboard showing end-to-end latency p50/p95/p99, throughput, checkpoint success rate, and per-topic lag; auto-scales Flink TaskManagers from 4 to 64 based on Kafka consumer lag using KEDA; and recovers automatically from TaskManager failures within 30 seconds by restoring from the latest checkpoint.",
    "category": "long"
  },
  {
    "id": "long_0196",
    "text": "Analyze the following microservices architecture and identify potential single points of failure, bottlenecks, and security vulnerabilities: The system consists of an API gateway that routes requests to twelve backend services including user-service, auth-service, payment-service, notification-service, email-service, sms-service, recommendation-service, search-service, catalog-service, inventory-service, order-service, and shipping-service. Each service has its own PostgreSQL database with connection pooling configured via PgBouncer. Services communicate asynchronously via RabbitMQ for non-critical paths and synchronously via gRPC for critical paths like payment processing. The API gateway is implemented in Nginx with Lua scripting for rate limiting. Authentication uses JWT tokens with a 15-minute expiry refreshed automatically by the client. The recommendation service uses a pre-trained collaborative filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses. Furthermore, ve filtering model served via TorchServe. All services are deployed as Kubernetes pods with horizontal pod autoscaling configured based on CPU and custom Prometheus metrics. The system handles approximately 50,000 requests per second at peak with a p99 latency SLO of 500ms for API gateway responses.",
    "category": "long"
  },
  {
    "id": "long_0197",
    "text": "apiVersion: apps/v1 kind: Deployment metadata:   name: mind-nerve-encoder   namespace: production   labels:     app: mind-nerve     component: encoder     version: v0.4.0 spec:   replicas: 3   selector:     matchLabels:       app: mind-nerve       component: encoder   strategy:     type: RollingUpdate     rollingUpdate:       maxSurge: 1       maxUnavailable: 0   template:     metadata:       labels:         app: mind-nerve         component: encoder         version: v0.4.0       annotations:         prometheus.io/scrape: 'true'         prometheus.io/port: '9090'         prometheus.io/path: '/metrics'     spec:       securityContext:         runAsNonRoot: true         runAsUser: 1000         fsGroup: 1000       containers:       - name: encoder         image: ghcr.io/star-ga/mind-nerve:v0.4.0         ports:         - containerPort: 8080         - containerPort: 9090         resources:           requests:             cpu: 500m             memory: 512Mi           limits:             cpu: 2000m             memory: 2Gi         readinessProbe:           httpGet:             path: /health/ready             port: 8080           initialDelaySeconds: 10           periodSeconds: 5         livenessProbe:           httpGet:             path: /health/live             port: 8080           initialDelaySeconds: 30           periodSeconds: 10         env:         - name: MIND_NERVE_BACKEND           value: native         - name: MIND_NERVE_RUNTIME_DIR           value: /runtime         volumeMounts:         - name: runtime           mountPath: /runtime           readOnly: true       volumes:       - name: runtime         configMap:           name: mind-nerve-runtime Furthermore,  MIND_NERVE_BACKEND           value: native         - name: MIND_NERVE_RUNTIME_DIR           value: /runtime         volumeMounts:         - name: runtime           mountPath: /runtime           readOnly: true       volumes:       - name: runtime         configMap:           name: mind-nerve-runtime",
    "category": "long"
  },
  {
    "id": "long_0198",
    "text": "import { createServer } from 'http'; import { WebSocketServer, WebSocket } from 'ws'; import { EventEmitter } from 'events'; interface ClientMessage { type: 'subscribe' | 'unsubscribe' | 'publish';   channel: string; payload?: unknown; clientId: string; } interface ServerMessage { type: 'data' | 'error' | 'ack';   channel?: string; payload?: unknown; timestamp: number; } class PubSubServer extends EventEmitter {   private channels = new Map<string, Set<WebSocket>>();   private clientChannels = new Map<WebSocket, Set<string>>();   constructor(private readonly port: number) { super(); }   subscribe(ws: WebSocket, channel: string): void {     if (!this.channels.has(channel)) this.channels.set(channel, new Set());     this.channels.get(channel)!.add(ws);     if (!this.clientChannels.has(ws)) this.clientChannels.set(ws, new Set());     this.clientChannels.get(ws)!.add(channel); }   unsubscribe(ws: WebSocket, channel: string): void {     this.channels.get(channel)?.delete(ws);     this.clientChannels.get(ws)?.delete(channel); }   publish(channel: string, payload: unknown): void {     const msg: ServerMessage = { type: 'data', channel, payload,       timestamp: Date.now() };     const data = JSON.stringify(msg);     this.channels.get(channel)?.forEach(client => {       if (client.readyState === WebSocket.OPEN) client.send(data); }); }   cleanup(ws: WebSocket): void {     this.clientChannels.get(ws)?.forEach(ch => this.unsubscribe(ws, ch));     this.clientChannels.delete(ws); }   listen(): void {     const server = createServer();     const wss = new WebSocketServer({ server });     wss.on('connection', ws => {       ws.on('message', raw => {         const msg: ClientMessage = JSON.parse(raw.toString());         switch (msg.type) {           case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } } Furthermore,         case 'subscribe': this.subscribe(ws, msg.channel); break;           case 'unsubscribe': this.unsubscribe(ws, msg.channel); break;           case 'publish': this.publish(msg.channel, msg.payload); break; } });       ws.on('close', () => this.cleanup(ws)); });     server.listen(this.port); } }",
    "category": "long"
  },
  {
    "id": "long_0199",
    "text": "def process_batch(items: list[dict], batch_size: int = 32, timeout: float = 30.0, retry_count: int = 3, backoff_factor: float = 1.5) -> list[dict]: \"\"\"Process a batch of items with retry logic and exponential backoff. Each item is validated, transformed, and persisted to the database. Failed items are retried up to retry_count times with exponential backoff. Items that fail all retries are logged and returned in the error list. The function is thread-safe and can be called from multiple workers. Parameters: items (list[dict]) — the items to process; batch_size (int) — number of items per database transaction; timeout (float) — per-item timeout in seconds; retry_count (int) — maximum retries per item; backoff_factor (float) — exponential backoff multiplier. Returns: list of successfully processed items.\"\"\" results = [] errors = [] for i in range(0, len(items), batch_size):     chunk = items[i:i + batch_size]     with db.transaction():         for item in chunk:             for attempt in range(retry_count):                 try:                     validated = validate(item)                     transformed = transform(validated)                     persisted = persist(transformed, timeout=timeout)                     results.append(persisted)                     break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors Furthermore,                   break                 except TransientError as e:                     if attempt == retry_count - 1:                         errors.append({'item': item, 'error': str(e)})                     else:                         time.sleep(backoff_factor ** attempt) return results, errors",
    "category": "long"
  },
  {
    "id": "adv_0000",
    "text": "",
    "category": "adversarial"
  },
  {
    "id": "adv_0001",
    "text": "a",
    "category": "adversarial"
  },
  {
    "id": "adv_0002",
    "text": "b",
    "category": "adversarial"
  },
  {
    "id": "adv_0003",
    "text": "c",
    "category": "adversarial"
  },
  {
    "id": "adv_0004",
    "text": "d",
    "category": "adversarial"
  },
  {
    "id": "adv_0005",
    "text": "e",
    "category": "adversarial"
  },
  {
    "id": "adv_0006",
    "text": "f",
    "category": "adversarial"
  },
  {
    "id": "adv_0007",
    "text": "g",
    "category": "adversarial"
  },
  {
    "id": "adv_0008",
    "text": "h",
    "category": "adversarial"
  },
  {
    "id": "adv_0009",
    "text": "i",
    "category": "adversarial"
  },
  {
    "id": "adv_0010",
    "text": "j",
    "category": "adversarial"
  },
  {
    "id": "adv_0011",
    "text": "k",
    "category": "adversarial"
  },
  {
    "id": "adv_0012",
    "text": "l",
    "category": "adversarial"
  },
  {
    "id": "adv_0013",
    "text": "m",
    "category": "adversarial"
  },
  {
    "id": "adv_0014",
    "text": "n",
    "category": "adversarial"
  },
  {
    "id": "adv_0015",
    "text": "o",
    "category": "adversarial"
  },
  {
    "id": "adv_0016",
    "text": "p",
    "category": "adversarial"
  },
  {
    "id": "adv_0017",
    "text": "q",
    "category": "adversarial"
  },
  {
    "id": "adv_0018",
    "text": "r",
    "category": "adversarial"
  },
  {
    "id": "adv_0019",
    "text": "s",
    "category": "adversarial"
  },
  {
    "id": "adv_0020",
    "text": "t",
    "category": "adversarial"
  },
  {
    "id": "adv_0021",
    "text": " git status",
    "category": "adversarial"
  },
  {
    "id": "adv_0022",
    "text": "  git status",
    "category": "adversarial"
  },
  {
    "id": "adv_0023",
    "text": "   git status",
    "category": "adversarial"
  },
  {
    "id": "adv_0024",
    "text": "git status ",
    "category": "adversarial"
  },
  {
    "id": "adv_0025",
    "text": "git status  ",
    "category": "adversarial"
  },
  {
    "id": "adv_0026",
    "text": " git status ",
    "category": "adversarial"
  },
  {
    "id": "adv_0027",
    "text": "\tgit status",
    "category": "adversarial"
  },
  {
    "id": "adv_0028",
    "text": "\ngit status",
    "category": "adversarial"
  },
  {
    "id": "adv_0029",
    "text": "git status\n",
    "category": "adversarial"
  },
  {
    "id": "adv_0030",
    "text": "\tgit status\t",
    "category": "adversarial"
  },
  {
    "id": "adv_0031",
    "text": "          git status",
    "category": "adversarial"
  },
  {
    "id": "adv_0032",
    "text": "git status          ",
    "category": "adversarial"
  },
  {
    "id": "adv_0033",
    "text": "\r\ngit status",
    "category": "adversarial"
  },
  {
    "id": "adv_0034",
    "text": "git status\r\n",
    "category": "adversarial"
  },
  {
    "id": "adv_0035",
    "text": "         git status",
    "category": "adversarial"
  },
  {
    "id": "adv_0036",
    "text": "GIT STATUS",
    "category": "adversarial"
  },
  {
    "id": "adv_0037",
    "text": "git status",
    "category": "adversarial"
  },
  {
    "id": "adv_0038",
    "text": "Git Status",
    "category": "adversarial"
  },
  {
    "id": "adv_0039",
    "text": "git statusgit status",
    "category": "adversarial"
  },
  {
    "id": "adv_0040",
    "text": "git status git status",
    "category": "adversarial"
  },
  {
    "id": "adv_0041",
    "text": "!!!",
    "category": "adversarial"
  },
  {
    "id": "adv_0042",
    "text": "???",
    "category": "adversarial"
  },
  {
    "id": "adv_0043",
    "text": "---",
    "category": "adversarial"
  },
  {
    "id": "adv_0044",
    "text": "...",
    "category": "adversarial"
  },
  {
    "id": "adv_0045",
    "text": "///",
    "category": "adversarial"
  },
  {
    "id": "adv_0046",
    "text": "###",
    "category": "adversarial"
  },
  {
    "id": "adv_0047",
    "text": "@@@",
    "category": "adversarial"
  },
  {
    "id": "adv_0048",
    "text": "$$$",
    "category": "adversarial"
  },
  {
    "id": "adv_0049",
    "text": "%%%",
    "category": "adversarial"
  },
  {
    "id": "adv_0050",
    "text": "^^^",
    "category": "adversarial"
  },
  {
    "id": "adv_0051",
    "text": "0",
    "category": "adversarial"
  },
  {
    "id": "adv_0052",
    "text": "123",
    "category": "adversarial"
  },
  {
    "id": "adv_0053",
    "text": "1234567890",
    "category": "adversarial"
  },
  {
    "id": "adv_0054",
    "text": "3.14159265358979",
    "category": "adversarial"
  },
  {
    "id": "adv_0055",
    "text": "1e-10",
    "category": "adversarial"
  },
  {
    "id": "adv_0056",
    "text": "0xDEADBEEF",
    "category": "adversarial"
  },
  {
    "id": "adv_0057",
    "text": "0b10101010",
    "category": "adversarial"
  },
  {
    "id": "adv_0058",
    "text": "0o777",
    "category": "adversarial"
  },
  {
    "id": "adv_0059",
    "text": "1_000_000",
    "category": "adversarial"
  },
  {
    "id": "adv_0060",
    "text": "inf",
    "category": "adversarial"
  },
  {
    "id": "adv_0061",
    "text": "\u0001",
    "category": "adversarial"
  },
  {
    "id": "adv_0062",
    "text": "�",
    "category": "adversarial"
  },
  {
    "id": "adv_0063",
    "text": "​",
    "category": "adversarial"
  },
  {
    "id": "adv_0064",
    "text": "‌",
    "category": "adversarial"
  },
  {
    "id": "adv_0065",
    "text": "‍",
    "category": "adversarial"
  },
  {
    "id": "adv_0066",
    "text": "﻿",
    "category": "adversarial"
  },
  {
    "id": "adv_0067",
    "text": "é",
    "category": "adversarial"
  },
  {
    "id": "adv_0068",
    "text": "中文",
    "category": "adversarial"
  },
  {
    "id": "adv_0069",
    "text": "ال",
    "category": "adversarial"
  },
  {
    "id": "adv_0070",
    "text": "αβγ",
    "category": "adversarial"
  },
  {
    "id": "adv_0071",
    "text": "analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze analyze",
    "category": "adversarial"
  },
  {
    "id": "adv_0072",
    "text": "x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x = x =",
    "category": "adversarial"
  },
  {
    "id": "adv_0073",
    "text": "evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate evaluate",
    "category": "adversarial"
  },
  {
    "id": "adv_0074",
    "text": "result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c) result = compute(a, b, c)",
    "category": "adversarial"
  },
  {
    "id": "adv_0075",
    "text": "xyzzy qux zap blorp wibble",
    "category": "adversarial"
  },
  {
    "id": "adv_0076",
    "text": "aaaaaaaaaaaaaaaaaaaaaaaaa",
    "category": "adversarial"
  },
  {
    "id": "adv_0077",
    "text": "zzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzz",
    "category": "adversarial"
  },
  {
    "id": "adv_0078",
    "text": "bbbbbbbbbbbbbbbbbbbbbbbbb",
    "category": "adversarial"
  },
  {
    "id": "adv_0079",
    "text": "zzzzzzzzzzzzzzzzzzzzzzzzz",
    "category": "adversarial"
  },
  {
    "id": "adv_0080",
    "text": "the the the the the the the the the the the the the the the the the the the the the the the the the the the the the the",
    "category": "adversarial"
  },
  {
    "id": "adv_0081",
    "text": "is is is is is is is is is is is is is is is is is is is is is is is is is is is is is is",
    "category": "adversarial"
  },
  {
    "id": "adv_0082",
    "text": "a a a a a a a a a a a a a a a a a a a a a a a a a a a a a a",
    "category": "adversarial"
  },
  {
    "id": "adv_0083",
    "text": "of of of of of of of of of of of of of of of of of of of of of of of of of of of of of of",
    "category": "adversarial"
  },
  {
    "id": "adv_0084",
    "text": "and and and and and and and and and and and and and and and and and and and and and and and and and and and and and and",
    "category": "adversarial"
  },
  {
    "id": "adv_0085",
    "text": "to to to to to to to to to to to to to to to to to to to to to to to to to to to to to to",
    "category": "adversarial"
  },
  {
    "id": "adv_0086",
    "text": "in in in in in in in in in in in in in in in in in in in in in in in in in in in in in in",
    "category": "adversarial"
  },
  {
    "id": "adv_0087",
    "text": "that that that that that that that that that that that that that that that that that that that that",
    "category": "adversarial"
  },
  {
    "id": "adv_0088",
    "text": "it it it it it it it it it it it it it it it it it it it it it it it it it it it it it it",
    "category": "adversarial"
  },
  {
    "id": "adv_0089",
    "text": "with with with with with with with with with with with with with with with with with with with with",
    "category": "adversarial"
  },
  {
    "id": "adv_0090",
    "text": "def f(): pass",
    "category": "adversarial"
  },
  {
    "id": "adv_0091",
    "text": "class A(B): ...",
    "category": "adversarial"
  },
  {
    "id": "adv_0092",
    "text": "import sys; sys.exit(0)",
    "category": "adversarial"
  },
  {
    "id": "adv_0093",
    "text": "{ 'key': [1, 2, 3] }",
    "category": "adversarial"
  },
  {
    "id": "adv_0094",
    "text": "SELECT * FROM users WHERE id = 1",
    "category": "adversarial"
  },
  {
    "id": "adv_0095",
    "text": "curl -X POST http://localhost:8080/api",
    "category": "adversarial"
  },
  {
    "id": "adv_0096",
    "text": "kubectl get pods --all-namespaces",
    "category": "adversarial"
  },
  {
    "id": "adv_0097",
    "text": "docker run -it --rm ubuntu:22.04 bash",
    "category": "adversarial"
  },
  {
    "id": "adv_0098",
    "text": "python3 -c 'print(1+1)'",
    "category": "adversarial"
  },
  {
    "id": "adv_0099",
    "text": "bash -c 'for i in $(seq 10); do echo $i; done'",
    "category": "adversarial"
  },
  {
    "id": "adv_0100",
    "text": "start xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0101",
    "text": "update xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0102",
    "text": "how to xxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0103",
    "text": "deploy xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0104",
    "text": "get xxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0105",
    "text": "check xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0106",
    "text": "install xxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0107",
    "text": "find all xxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0108",
    "text": "validate xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0109",
    "text": "explain xxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0110",
    "text": "run xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0111",
    "text": "run xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0112",
    "text": "validate xxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0113",
    "text": "find all xxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0114",
    "text": "find all xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0115",
    "text": "validate xxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0116",
    "text": "how to xxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0117",
    "text": "deploy xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0118",
    "text": "configure xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0119",
    "text": "deploy xxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0120",
    "text": "build xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0121",
    "text": "help with xxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0122",
    "text": "restart xxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0123",
    "text": "deploy xx",
    "category": "adversarial"
  },
  {
    "id": "adv_0124",
    "text": "build xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0125",
    "text": "how to xxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0126",
    "text": "restart xxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0127",
    "text": "start xxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0128",
    "text": "build xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0129",
    "text": "stop xxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0130",
    "text": "update x",
    "category": "adversarial"
  },
  {
    "id": "adv_0131",
    "text": "validate xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0132",
    "text": "build xxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0133",
    "text": "show me xxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0134",
    "text": "run xxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0135",
    "text": "find all xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0136",
    "text": "stop xxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0137",
    "text": "deploy xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0138",
    "text": "restart xxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0139",
    "text": "deploy xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0140",
    "text": "list xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0141",
    "text": "install xxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0142",
    "text": "install xxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0143",
    "text": "stop xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0144",
    "text": "show me xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0145",
    "text": "deploy xxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0146",
    "text": "deploy xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0147",
    "text": "how to xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0148",
    "text": "show me xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0149",
    "text": "get xx",
    "category": "adversarial"
  },
  {
    "id": "adv_0150",
    "text": "configure ",
    "category": "adversarial"
  },
  {
    "id": "adv_0151",
    "text": "build xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0152",
    "text": "deploy xxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0153",
    "text": "deploy xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0154",
    "text": "show me xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0155",
    "text": "check xxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0156",
    "text": "show me xxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0157",
    "text": "check xxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0158",
    "text": "help with xxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0159",
    "text": "configure xxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0160",
    "text": "deploy xxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0161",
    "text": "update xxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0162",
    "text": "install xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0163",
    "text": "stop xxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0164",
    "text": "update xxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0165",
    "text": "explain xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0166",
    "text": "start xxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0167",
    "text": "install xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0168",
    "text": "configure xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0169",
    "text": "check xxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0170",
    "text": "what is xxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0171",
    "text": "deploy xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0172",
    "text": "start xxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0173",
    "text": "install xxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0174",
    "text": "stop xxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0175",
    "text": "stop xxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0176",
    "text": "help with xxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0177",
    "text": "check xxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0178",
    "text": "update xxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0179",
    "text": "run xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0180",
    "text": "show me xx",
    "category": "adversarial"
  },
  {
    "id": "adv_0181",
    "text": "help with xxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0182",
    "text": "deploy xxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0183",
    "text": "stop xxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0184",
    "text": "run xxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0185",
    "text": "install xxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0186",
    "text": "what is xxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0187",
    "text": "update xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0188",
    "text": "get xx",
    "category": "adversarial"
  },
  {
    "id": "adv_0189",
    "text": "list xxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0190",
    "text": "update xxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0191",
    "text": "start xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0192",
    "text": "how to xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0193",
    "text": "validate xxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0194",
    "text": "check ",
    "category": "adversarial"
  },
  {
    "id": "adv_0195",
    "text": "install xxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0196",
    "text": "what is xxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0197",
    "text": "list xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0198",
    "text": "configure xxxxxxxxxxxxxxxxxx",
    "category": "adversarial"
  },
  {
    "id": "adv_0199",
    "text": "test xxxxxxxxxxxx",
    "category": "adversarial"
  }
]